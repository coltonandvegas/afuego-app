(() => {
'use strict';
// A Fuego ERP v3.3.0 audited browser bundle — weekly analogue stocktake/production bridge plus v3.1 reporting.
const INGREDIENT_SEEDS = [
  ['Argentine Chorizo','Meat',18,1,'kg','Buenos Aires Deli'],['Baguette / Crusty Roll','Bread',6,12,'unit','Local Bakery'],['Beef Short Rib','Meat',26,1,'kg','Metro Cash & Carry'],['Wheat Empanada Pastry','Dry Goods',8,24,'unit',''],['Corn Empanada Pastry (GF)','Dry Goods',9,24,'unit',''],['Minced Beef','Meat',14,1,'kg',''],['Chicken Breast','Meat',10,1,'kg',''],['Potato (for fries)','Produce',2.5,1,'kg',''],['Parsley','Produce',2.5,1,'unit',''],['Garlic','Produce',4,1,'kg',''],['Tomato','Produce',3.5,1,'kg',''],['Red Onion','Produce',2.5,1,'kg',''],['Capsicum','Produce',4,1,'kg',''],['Spinach','Produce',3.5,1,'unit',''],['Ricotta','Dairy',5,500,'g',''],['Parmesan','Dairy',12,1,'kg',''],['Olive Oil','Oils & Vinegars',12,750,'ml',''],['Sunflower Oil (frying)','Oils & Vinegars',5,2,'L',''],['Red Wine Vinegar','Oils & Vinegars',4.5,500,'ml',''],['Salt (sea salt)','Condiments & Spices',3,1,'kg',''],['Black Pepper','Condiments & Spices',8,100,'g',''],['Paprika (sweet)','Condiments & Spices',9,100,'g',''],['Cumin','Condiments & Spices',8,100,'g',''],['Dried Chilli Flakes','Condiments & Spices',7,100,'g',''],['Dried Oregano','Condiments & Spices',6,50,'g',''],['Cooking Oil Spray','Oils & Vinegars',5,400,'ml','']
].map(([name,category,price,qty,unit,supplier])=>({name,category,price,qty,unit,supplier}));

const MENU_SEEDS = [
  {product_name:'Choripán Clásico',selling_price:14,yield_qty:1,notes:'Argentine chorizo in crusty bread with house-made chimichurri',components:[['Argentine Chorizo',150,'g'],['Baguette / Crusty Roll',1,'unit'],['Parsley',5,'g'],['Garlic',3,'g'],['Olive Oil',1,'tbsp'],['Red Wine Vinegar',1,'tsp'],['Salt (sea salt)',1,'pinch'],['Dried Chilli Flakes',1,'pinch'],['Dried Oregano',1,'pinch']]},
  {product_name:'Asado (Beef Short Rib)',selling_price:22,yield_qty:1,notes:'Slow-cooked Argentine-style beef short rib with chimichurri',components:[['Beef Short Rib',300,'g'],['Salt (sea salt)',2,'pinch'],['Black Pepper',1,'pinch'],['Parsley',3,'g'],['Garlic',2,'g'],['Olive Oil',1,'tbsp'],['Red Wine Vinegar',1,'tsp']]},
  {product_name:'Empanada de Carne',selling_price:6,yield_qty:1,notes:'Baked beef empanada with olives, egg & spices',components:[['Wheat Empanada Pastry',1,'unit'],['Minced Beef',80,'g'],['Red Onion',20,'g'],['Paprika (sweet)',1,'tsp'],['Cumin',1,'pinch'],['Salt (sea salt)',1,'pinch'],['Black Pepper',1,'pinch'],['Olive Oil',1,'tsp']]},
  {product_name:'Empanada de Verdura GF',selling_price:6,yield_qty:1,notes:'Corn pastry with spinach & ricotta',components:[['Corn Empanada Pastry (GF)',1,'unit'],['Spinach',50,'g'],['Ricotta',40,'g'],['Parmesan',10,'g'],['Garlic',2,'g'],['Salt (sea salt)',1,'pinch'],['Black Pepper',1,'pinch']]},
  {product_name:'Chimichurri Fries',selling_price:9,yield_qty:1,notes:'Crispy fries drizzled with house chimichurri',components:[['Potato (for fries)',200,'g'],['Sunflower Oil (frying)',3,'tbsp'],['Salt (sea salt)',2,'pinch'],['Parsley',3,'g'],['Garlic',2,'g'],['Olive Oil',2,'tsp'],['Red Wine Vinegar',1,'tsp'],['Dried Chilli Flakes',1,'pinch']]}
];

const OPERATING_COST_SEEDS = [
  {name:'Shopify Subscription',amount:79,category:'POS & Shopify',frequency:'monthly'},
  {name:'Shopify Finance Charges',amount:15,category:'Shopify Finance',frequency:'monthly'},
  {name:'Mobile Internet',amount:35,category:'Mobile Internet',frequency:'monthly'},
  {name:'Bank Fees',amount:12,category:'Bank Fees',frequency:'monthly'}
];


const { createClient } = window.supabase;

const SUPABASE_URL='https://nmqmbajdpqjubuwxmayg.supabase.co';
const SUPABASE_ANON_KEY='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5tcW1iYWpkcHFqdWJ1d3htYXlnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODExNzMwMDcsImV4cCI6MjA5Njc0OTAwN30.uWJe9AceZ6wztKrwu1m1tX2azgBW0bZMMkTpF1SNoUY';
const supabase=createClient(SUPABASE_URL,SUPABASE_ANON_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});

const app=document.getElementById('app');
const state={session:null,user:null,settings:null,taxSettings:null,taxReserveEntries:[],inventory:[],recipes:[],components:[],sales:[],saleItems:[],expenses:[],locations:[],recon:[],opCosts:[],capital:[],invoices:[],invoiceLines:[],purchaseMappings:[],stockMovements:[],preparations:[],prepComponents:[],supplies:[],shopifyOrders:[],shopifyItems:[],projects:[],tasks:[],taskDependencies:[],taskAttachments:[],opsSessions:[],opsLines:[],productionRuns:[],productionRunLines:[],route:'dashboard',plannerTab:'board',plannerArea:'all',plannerOwner:'all',foodTab:'menu',foodStatusFilter:'active',financeTab:'purchases',opsTab:'stocktake',loading:false};
const tableFlags={operating_costs:true,capital_items:true,invoices:true,invoice_lines:true,purchase_item_mappings:true,stock_movements:true,preparations:true,preparation_components:true,supplies:true,shopify_orders:true,shopify_order_items:true,tax_settings:true,tax_reserve_entries:true,erp_projects:true,erp_tasks:true,erp_task_dependencies:true,erp_task_attachments:true,ops_sheet_sessions:true,ops_sheet_lines:true,production_runs:true,production_run_lines:true};

const pages={dashboard:['Dashboard','The grill at a glance'],planner:['Priorities','Projects, ideas and the next actions that matter'],sales:['Sales','Daily takings and Shopify orders'],food:['Food','Ingredients, recipes and margins'],operations:['Weekly ops','Print, count, photograph and reconcile the real truck'],finance:['Money','Purchases, expenses and stock intake'],reports:['Reports','Month-by-month sales, costs, profit and funding'],tax:['Tax reserve','Financial-year tax buffer and cash target'],assistant:['A Fuego AI','Ask the numbers, not your memory'],settings:['Settings','Business and integrations']};
const nav=[['dashboard','⌂','Home'],['planner','✓','Priorities'],['sales','↗','Sales'],['food','◫','Food'],['operations','▤','Weekly ops'],['finance','$','Money'],['reports','▥','Reports'],['tax','%','Tax'],['assistant','✦','AI'],['settings','⚙','Settings']];

function esc(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function money(n=0){return new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:2}).format(Number(n)||0)}
function pct(n=0){return `${(Number(n)||0).toFixed(1)}%`}
function dateAU(v){if(!v)return '—';return new Date(`${v}T12:00:00`).toLocaleDateString('en-AU',{day:'2-digit',month:'short',year:'numeric'})}
function today(){return new Date().toISOString().slice(0,10)}
function toast(msg,error=false){const t=document.createElement('div');t.className=`toast${error?' error':''}`;t.textContent=msg;document.body.appendChild(t);setTimeout(()=>t.remove(),3300)}
function icon(name){return `<span aria-hidden="true">${name}</span>`}
function safeNum(v){return Number(v)||0}
function nullableNum(v){const raw=String(v??'').trim();if(raw==='')return null;const n=Number(raw);return Number.isFinite(n)?n:null}
function sqlMissing(err){return err&&(/does not exist|schema cache|Could not find the table/i.test(err.message||''))}

function normalizeBase(unit,qty){const q=safeNum(qty)||1;if(unit==='kg')return q*1000;if(unit==='L')return q*1000;return q}
function usageToBase(qty,unit){const q=safeNum(qty);const factors={kg:1000,L:1000,tbsp:15,tsp:5,pinch:.35,dash:.6,spray:.25};return q*(factors[unit]||1)}
function ingredientCost(item,qty,unit){if(!item)return 0;const useBase=usageToBase(qty,unit);return safeNum(item.unit_cost)*useBase}
function preparationComponentLineCost(component){const ing=state.inventory.find(i=>i.id===component.inventory_item_id);if(ing)return ingredientCost(ing,component.qty,component.unit);const nested=state.preparations.find(p=>p.id===component.nested_preparation_id);if(nested)return preparationUsageCost(nested,component.qty,component.unit);return safeNum(component.line_cost)}
function preparationBatchCost(prep,stack=new Set()){if(!prep)return 0;if(stack.has(prep.id))return safeNum(prep.total_cost);const next=new Set(stack);next.add(prep.id);const comps=state.prepComponents.filter(c=>c.preparation_id===prep.id);if(!comps.length)return safeNum(prep.total_cost);return comps.reduce((sum,c)=>{const ing=state.inventory.find(i=>i.id===c.inventory_item_id);if(ing)return sum+ingredientCost(ing,c.qty,c.unit);const nested=state.preparations.find(p=>p.id===c.nested_preparation_id);if(nested){const batch=preparationBatchCost(nested,next),yieldBase=usageToBase(nested.yield_qty,nested.yield_unit)||1;return sum+batch*(usageToBase(c.qty,c.unit)/yieldBase)}return sum+safeNum(c.line_cost)},0)}
function preparationUsageCost(prep,qty,unit){if(!prep)return 0;const batch=preparationBatchCost(prep),yieldBase=usageToBase(prep.yield_qty,prep.yield_unit)||1;return batch*(usageToBase(qty,unit)/yieldBase)}

function ingredientPurchaseQty(item){return safeNum(item?.purchase_qty??item?.current_qty??1)||1}
function purchasePrice(item){return safeNum(item?.unit_cost)*normalizeBase(item?.unit,ingredientPurchaseQty(item))}
function ingredientStockValue(item){return safeNum(item?.unit_cost)*normalizeBase(item?.unit,safeNum(item?.stock_qty))}
function preparationStockValue(prep){return preparationUsageCost(prep,safeNum(prep?.stock_qty),prep?.yield_unit||'g')}
function supplyUnitCost(item){return safeNum(item?.purchase_price)/(safeNum(item?.pack_size)||1)}
function supplyStockValue(item){return supplyUnitCost(item)*safeNum(item?.stock_qty)}
function stockCapital(){const ingredients=state.inventory.reduce((s,i)=>s+ingredientStockValue(i),0),preparations=state.preparations.reduce((s,p)=>s+preparationStockValue(p),0),supplies=state.supplies.reduce((s,x)=>s+supplyStockValue(x),0),finished=state.recipes.reduce((s,r)=>s+recipeCost(r)*safeNum(r.stock_qty),0);return{ingredients,preparations,supplies,finished,total:ingredients+preparations+supplies+finished}}
function isActive(x){return x?.active!==false}
function visibleByStatus(list){if(state.foodStatusFilter==='all')return list;if(state.foodStatusFilter==='inactive')return list.filter(x=>!isActive(x));return list.filter(isActive)}
function normalizeProductName(value=''){const stop=new Set(['a','an','and','con','de','del','el','la','las','los','the','with','y']);return String(value).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/&/g,' and ').replace(/[^a-z0-9]+/g,' ').trim().split(/\s+/).filter(x=>x&&!stop.has(x)).sort().join(' ')}
function recipeNames(recipe){return [recipe.product_name,...(Array.isArray(recipe.sales_aliases)?recipe.sales_aliases:[])].filter(Boolean)}
function matchSaleProduct(rawName=''){const needle=normalizeProductName(rawName);if(!needle)return null;let best=null;for(const recipe of state.recipes){for(const candidate of recipeNames(recipe)){const n=normalizeProductName(candidate);if(!n)continue;let score=0,method='';if(n===needle){score=1;method=candidate===recipe.product_name?'name':'alias'}else{const a=new Set(needle.split(' ')),b=new Set(n.split(' ')),inter=[...a].filter(x=>b.has(x)).length,union=new Set([...a,...b]).size,j=union?inter/union:0;if(j>=.8){score=.9+j/10;method='smart'}else if((needle.includes(n)||n.includes(needle))&&Math.min(a.size,b.size)>=2){score=.82;method='smart'}}if(!best||score>best.score)best={recipe,score,method,candidate}}}return best&&best.score>=.82?best:null}
function salesItemMatch(item){const raw=item.title||item.product||'';const withVariant=item.variant_title&&item.variant_title!=='Default Title'?`${raw} ${item.variant_title}`:raw;return matchSaleProduct(withVariant)||matchSaleProduct(raw)}
function canonicalSalesName(item){return salesItemMatch(item)?.recipe?.product_name||item.title||item.product||'Unknown product'}
function uniqueSalesProducts(){const src=state.shopifyItems.length?state.shopifyItems:state.saleItems,map=new Map();for(const item of src){const raw=item.title||item.product||'Unknown product',key=normalizeProductName(raw)||raw;if(!map.has(key))map.set(key,{raw,qty:0,match:salesItemMatch(item)});map.get(key).qty+=safeNum(item.quantity??item.qty)}return [...map.values()].sort((a,b)=>b.qty-a.qty)}
function seedComponentUsage(recipe,component){const seed=MENU_SEEDS.find(x=>x.product_name===recipe?.product_name);const row=seed?.components?.find(x=>x[0]===component?.component_name);return row?{qty:safeNum(row[1]),unit:row[2]}:null}
function componentUsage(recipe,component){if(component?.usage_qty!==null&&component?.usage_qty!==undefined&&component?.usage_unit)return{qty:safeNum(component.usage_qty),unit:component.usage_unit};return seedComponentUsage(recipe,component)}
function componentLineCost(recipe,component){const usage=componentUsage(recipe,component),prep=state.preparations.find(p=>p.id===component.preparation_id),ing=state.inventory.find(i=>i.id===component.inventory_item_id);if(prep&&usage?.qty)return preparationUsageCost(prep,usage.qty,usage.unit);if(ing&&usage?.qty)return ingredientCost(ing,usage.qty,usage.unit);return safeNum(component.qty)*safeNum(component.unit_cost)}
function recipeCost(recipe){return state.components.filter(c=>c.recipe_id===recipe.id).reduce((s,c)=>s+componentLineCost(recipe,c),0)/(safeNum(recipe.yield_qty)||1)}
function optionSelected(current,value){return String(current??'')===String(value)?'selected':''}
function recipeComponentPayload(recipeId,x,includeUsage=true){const row={user_id:state.user.id,recipe_id:recipeId,inventory_item_id:x.kind==='ingredient'?x.id:null,preparation_id:x.kind==='preparation'?x.id:null,component_name:x.name,qty:1,unit_cost:safeNum(x.cost)};if(includeUsage){row.usage_qty=safeNum(x.qty)||null;row.usage_unit=x.unit||null}return row}
async function replaceRecipeComponents(recipeId,selected){const old=state.components.filter(c=>c.recipe_id===recipeId);const {error:delErr}=await supabase.from('recipe_components').delete().eq('recipe_id',recipeId);if(delErr)throw delErr;if(!selected.length)return true;let rows=selected.map(x=>recipeComponentPayload(recipeId,x,true));let {error}=await supabase.from('recipe_components').insert(rows);if(error&&/usage_qty|usage_unit/i.test(error.message||'')){rows=selected.map(x=>recipeComponentPayload(recipeId,x,false));const retry=await supabase.from('recipe_components').insert(rows);error=retry.error}if(error){if(old.length){const restore=old.map(c=>({user_id:c.user_id,recipe_id:c.recipe_id,inventory_item_id:c.inventory_item_id,preparation_id:c.preparation_id||null,component_name:c.component_name,qty:c.qty,unit_cost:c.unit_cost,usage_qty:c.usage_qty??null,usage_unit:c.usage_unit??null}));await supabase.from('recipe_components').insert(restore)}throw error}return true}
async function replacePreparationComponents(preparationId,selected){const old=state.prepComponents.filter(c=>c.preparation_id===preparationId);const {error:delErr}=await supabase.from('preparation_components').delete().eq('preparation_id',preparationId);if(delErr)throw delErr;if(!selected.length)return true;const rows=selected.map(x=>({user_id:state.user.id,preparation_id:preparationId,inventory_item_id:x.id,nested_preparation_id:null,component_name:x.name,qty:safeNum(x.qty),unit:x.unit,line_cost:safeNum(x.cost)}));const {error}=await supabase.from('preparation_components').insert(rows);if(error){if(old.length){await supabase.from('preparation_components').insert(old.map(c=>({user_id:c.user_id,preparation_id:c.preparation_id,inventory_item_id:c.inventory_item_id,nested_preparation_id:c.nested_preparation_id,component_name:c.component_name,qty:c.qty,unit:c.unit,line_cost:c.line_cost})))}throw error}return true}
function costClass(v){return v<=30?'good':v<=36?'warn':'bad'}
function splitGST(total,claimable=true){if(!state.settings?.gst_registered||!claimable)return{net:safeNum(total),gst:0};const gst=safeNum(total)/11;return{net:safeNum(total)-gst,gst}}
function weeklyCost(c){const n=safeNum(c.amount);return c.frequency==='annual'?n/52:c.frequency==='monthly'?n*12/52:c.frequency==='per_event'?0:n}
function dateObj(v){const [y,m,d]=String(v).split('-').map(Number);return new Date(y,(m||1)-1,d||1)}
function dateKey(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function monthKey(v){return String(v||'').slice(0,7)}
function daysInclusive(a,b){return Math.max(0,Math.floor((dateObj(b)-dateObj(a))/86400000)+1)}
function maxDate(a,b){return dateObj(a)>=dateObj(b)?a:b}
function minDate(a,b){return dateObj(a)<=dateObj(b)?a:b}
function monthEndFor(v){const d=dateObj(v);return dateKey(new Date(d.getFullYear(),d.getMonth()+1,0))}
function monthStartFor(v){return `${monthKey(v)}-01`}
function opCostIncluded(c){return c?.active!==false&&c?.include_in_profit!==false&&c?.frequency!=='per_event'}
function operatingCostForRange(start,end){
  if(!start||!end||dateObj(start)>dateObj(end))return 0;
  let total=0;
  for(const c of state.opCosts.filter(opCostIncluded)){
    const autoStart=(c.created_at||'').slice(0,10)||start;let a=c.start_date?maxDate(start,c.start_date):maxDate(start,autoStart),b=c.end_date?minDate(end,c.end_date):end;
    if(dateObj(a)>dateObj(b))continue;
    const n=safeNum(c.amount);
    if(c.frequency==='weekly'){total+=n*daysInclusive(a,b)/7;continue}
    if(c.frequency==='annual'){total+=n*daysInclusive(a,b)/365.25;continue}
    if(c.frequency==='monthly'){
      let cursor=dateObj(monthStartFor(a)),last=dateObj(monthStartFor(b));
      while(cursor<=last){
        const ms=dateKey(cursor),me=monthEndFor(ms),segA=maxDate(a,ms),segB=minDate(b,me),dim=daysInclusive(ms,me);
        if(dateObj(segA)<=dateObj(segB))total+=n*daysInclusive(segA,segB)/dim;
        cursor=new Date(cursor.getFullYear(),cursor.getMonth()+1,1);
      }
    }
  }
  return total;
}
function manualSalesForRange(start,end){return state.sales.filter(x=>x.date>=start&&x.date<=end&&(!state.shopifyOrders.length||x.include_with_shopify===true)).reduce((s,x)=>s+safeNum(x.cash_sales)+safeNum(x.card_sales)+safeNum(x.other_sales),0)}
function shopifySalesForRange(start,end){return state.shopifyOrders.filter(o=>{const d=(o.processed_at||'').slice(0,10);return d>=start&&d<=end}).reduce((s,o)=>s+safeNum(o.total_price),0)}
function salesForRange(start,end){return shopifySalesForRange(start,end)+manualSalesForRange(start,end)}
function loggedExpensesForRange(start,end){return state.expenses.filter(e=>e.date>=start&&e.date<=end).reduce((s,e)=>s+safeNum(e.amount),0)}
function capitalCashForRange(start,end){return state.capital.filter(c=>c.investment_date>=start&&c.investment_date<=end).reduce((s,c)=>{const n=Math.abs(safeNum(c.amount));return s+(c.movement_type==='contribution'?n:c.movement_type==='withdrawal'?-n:0)},0)}
function monthlyPerformance(count=12){
  const now=new Date(),rows=[];
  for(let i=count-1;i>=0;i--){
    const d=new Date(now.getFullYear(),now.getMonth()-i,1),start=dateKey(d),fullEnd=dateKey(new Date(d.getFullYear(),d.getMonth()+1,0)),end=(d.getFullYear()===now.getFullYear()&&d.getMonth()===now.getMonth())?today():fullEnd;
    const sales=salesForRange(start,end),expenses=loggedExpensesForRange(start,end),recurring=operatingCostForRange(start,end),costs=expenses+recurring,net=sales-costs,capital=capitalCashForRange(start,end);
    rows.push({key:start.slice(0,7),label:d.toLocaleDateString('en-AU',{month:'short',year:'2-digit'}),start,end,sales,expenses,recurring,costs,net,capital,cash:net+capital});
  }
  return rows;
}
function groupedMonthlySvg(rows){if(!rows.length)return '<div class="empty">No monthly data yet.</div>';const w=980,h=270,pL=55,pR=20,pT=18,pB=44,max=Math.max(...rows.flatMap(r=>[r.sales,r.costs]),1),innerW=w-pL-pR,group=innerW/rows.length,barW=Math.min(25,group*.28);let bars='',labels='';rows.forEach((r,i)=>{const x=pL+i*group+group/2,hs=(r.sales/max)*(h-pT-pB),hc=(r.costs/max)*(h-pT-pB);bars+=`<rect class="monthly-sales" x="${x-barW-2}" y="${h-pB-hs}" width="${barW}" height="${hs}" rx="3"/><rect class="monthly-costs" x="${x+2}" y="${h-pB-hc}" width="${barW}" height="${hc}" rx="3"/>`;labels+=`<text x="${x}" y="${h-17}" text-anchor="middle">${esc(r.label)}</text>`});return `<div class="chart-legend"><span><i class="legend-sales"></i>Sales</span><span><i class="legend-costs"></i>Total operating costs</span></div><svg class="monthly-chart" viewBox="0 0 ${w} ${h}" role="img" aria-label="Monthly sales versus operating costs"><line class="axis-line" x1="${pL}" y1="${h-pB}" x2="${w-pR}" y2="${h-pB}"/>${bars}${labels}</svg>`}
function netMonthlySvg(rows){if(!rows.length)return '<div class="empty">No monthly data yet.</div>';const w=980,h=250,pL=55,pR=20,pT=18,pB=44,max=Math.max(...rows.map(r=>Math.abs(r.net)),1),mid=(h-pB+pT)/2,innerW=w-pL-pR,group=innerW/rows.length,barW=Math.min(38,group*.52);let bars='',labels='';rows.forEach((r,i)=>{const x=pL+i*group+group/2,hh=Math.abs(r.net)/max*((h-pT-pB)/2),y=r.net>=0?mid-hh:mid;bars+=`<rect class="${r.net>=0?'monthly-net-pos':'monthly-net-neg'}" x="${x-barW/2}" y="${y}" width="${barW}" height="${hh}" rx="3"/>`;labels+=`<text x="${x}" y="${h-17}" text-anchor="middle">${esc(r.label)}</text>`});return `<svg class="monthly-chart" viewBox="0 0 ${w} ${h}" role="img" aria-label="Monthly net operating profit"><line class="axis-line" x1="${pL}" y1="${mid}" x2="${w-pR}" y2="${mid}"/>${bars}${labels}</svg>`}

async function boot(){
  const {data:{session}}=await supabase.auth.getSession();state.session=session;state.user=session?.user||null;
  supabase.auth.onAuthStateChange((_event,session)=>{state.session=session;state.user=session?.user||null;if(session){loadAll()}else renderAuth()});
  if(state.user)await loadAll();else renderAuth();
  // Service worker intentionally disabled in v3.0.2 while clearing the stale v3 cache.
}

function markReady(){window.__AFUEGO_READY__=true;window.dispatchEvent(new Event('afuego-ready'));}

function renderAuth(){
  markReady();
  app.innerHTML=`<div class="auth-shell"><section class="auth-hero"><div class="wordmark">A FUEGO <span>ERP</span></div><div><div class="eyebrow">Food truck operations cockpit</div><h1>RUN THE FIRE.<br><em>Read the numbers.</em></h1><p>Sales, recipes, invoices, food cost and capital in one private cloud workspace for A Fuego.</p></div><div class="eyebrow">Sydney · NSW · Australia</div></section><section class="auth-card-wrap"><form class="auth-card" id="authForm"><div class="eyebrow">Private access</div><h2>WELCOME BACK</h2><p>Use your A Fuego account. The same login works on phone and computer.</p><div class="field"><label>Email</label><input class="input" type="email" id="authEmail" value="${esc(localStorage.getItem('afuego_email')||'')}" required autocomplete="email"></div><div class="field"><label>Password</label><input class="input" type="password" id="authPassword" required minlength="6" autocomplete="current-password"></div><button class="btn btn-primary btn-block" type="submit">🔥 Enter A Fuego OS</button><button class="btn btn-ghost btn-block" type="button" id="signupBtn" style="margin-top:8px">Create account</button><p id="authError" class="bad"></p></form></section></div>`;
  document.getElementById('authForm').onsubmit=async e=>{e.preventDefault();const email=authEmail.value.trim(),password=authPassword.value;localStorage.setItem('afuego_email',email);const {error}=await supabase.auth.signInWithPassword({email,password});if(error)authError.textContent=error.message};
  document.getElementById('signupBtn').onclick=async()=>{const email=authEmail.value.trim(),password=authPassword.value;if(!email||password.length<6){authError.textContent='Enter an email and a password of at least 6 characters.';return}const {error}=await supabase.auth.signUp({email,password,options:{data:{full_name:'A Fuego Owner'}}});authError.textContent=error?error.message:'Account created. Check your email if confirmation is enabled.'};
}

async function loadAll(){
  state.loading=true;renderLoading();
  const uid=state.user.id;
  const base=await Promise.all([
    supabase.from('business_settings').select('*').maybeSingle(),supabase.from('inventory_items').select('*').order('name'),supabase.from('recipes').select('*').order('product_name'),supabase.from('recipe_components').select('*'),supabase.from('sales_days').select('*,locations_events(name)').order('date',{ascending:false}).limit(1000),supabase.from('sale_items').select('*').limit(3000),supabase.from('expenses').select('*').order('date',{ascending:false}).limit(1000),supabase.from('locations_events').select('*').order('name'),supabase.from('cash_reconciliations').select('*')
  ]);
  if(base[0].data)state.settings=base[0].data;else await ensureSettings(uid);
  state.inventory=base[1].data||[];state.recipes=base[2].data||[];state.components=base[3].data||[];state.sales=base[4].data||[];state.saleItems=base[5].data||[];state.expenses=base[6].data||[];state.locations=base[7].data||[];state.recon=base[8].data||[];
  await loadOptional();
  await seedDefaultsIfEmpty();
  await seedPlannerIfEmpty();
  state.loading=false;renderShell();
}
async function ensureSettings(uid){const {data,error}=await supabase.from('business_settings').insert({user_id:uid}).select().single();if(error)throw error;state.settings=data}
async function loadOptional(){
  const defs=[['operating_costs','opCosts'],['capital_items','capital'],['invoices','invoices'],['invoice_lines','invoiceLines'],['purchase_item_mappings','purchaseMappings'],['stock_movements','stockMovements'],['preparations','preparations'],['preparation_components','prepComponents'],['supplies','supplies'],['shopify_orders','shopifyOrders'],['shopify_order_items','shopifyItems'],['tax_settings','taxSettings'],['tax_reserve_entries','taxReserveEntries'],['erp_projects','projects'],['erp_tasks','tasks'],['erp_task_dependencies','taskDependencies'],['erp_task_attachments','taskAttachments'],['ops_sheet_sessions','opsSessions'],['ops_sheet_lines','opsLines'],['production_runs','productionRuns'],['production_run_lines','productionRunLines']];
  await Promise.all(defs.map(async([table,key])=>{const {data,error}=await supabase.from(table).select('*').limit(2000);if(error&&sqlMissing(error)){tableFlags[table]=false;state[key]=key==='taxSettings'?null:[]}else if(error){console.warn(table,error)}else state[key]=key==='taxSettings'?(data?.[0]||null):(data||[])}));
}
async function seedDefaultsIfEmpty(){
  if(state.inventory.length||state.recipes.length)return;
  const rows=INGREDIENT_SEEDS.map(i=>({user_id:state.user.id,name:i.name,unit:i.unit,current_qty:i.qty,purchase_qty:i.qty,stock_qty:0,active:true,unit_cost:i.price/normalizeBase(i.unit,i.qty),notes:[i.category,i.supplier].filter(Boolean).join(' · ')}));
  const {data:ings,error}=await supabase.from('inventory_items').insert(rows).select();if(error){console.warn(error);return}state.inventory=ings||[];
  for(const seed of MENU_SEEDS){const {data:r,error:re}=await supabase.from('recipes').insert({user_id:state.user.id,product_name:seed.product_name,selling_price:seed.selling_price,yield_qty:seed.yield_qty,notes:seed.notes,active:true,sales_aliases:[]}).select().single();if(re)continue;const comps=seed.components.map(([name,qty,unit])=>{const ing=state.inventory.find(i=>i.name===name);return{user_id:state.user.id,recipe_id:r.id,inventory_item_id:ing?.id||null,component_name:name,qty:1,unit_cost:ingredientCost(ing,qty,unit),source_qty:qty,source_unit:unit}});const clean=comps.map(({source_qty,source_unit,...c})=>c);await supabase.from('recipe_components').insert(clean)}
  if(tableFlags.operating_costs)await supabase.from('operating_costs').insert(OPERATING_COST_SEEDS.map(c=>({...c,user_id:state.user.id})));
  const [r,c,o]=await Promise.all([supabase.from('recipes').select('*').order('product_name'),supabase.from('recipe_components').select('*'),tableFlags.operating_costs?supabase.from('operating_costs').select('*'):Promise.resolve({data:[]})]);state.recipes=r.data||[];state.components=c.data||[];state.opCosts=o.data||[];toast('A Fuego starter data loaded');
}
function renderLoading(){app.innerHTML=`<div class="boot"><div class="brand-flame">🔥</div><div>A FUEGO ERP</div><small>Syncing the cloud...</small></div>`}

function renderShell(){
  markReady();
  const p=pages[state.route]||pages.dashboard;
  app.innerHTML=`<div class="app-shell"><aside class="sidebar"><div class="sidebar-brand"><div class="wordmark">A FUEGO <span>ERP</span></div><div class="sub">Operations cockpit</div></div><nav class="nav">${nav.map(([r,i,l])=>`<button class="nav-btn ${state.route===r?'active':''}" data-route="${r}"><span class="nav-icon">${i}</span>${l}</button>`).join('')}</nav><div class="sidebar-foot"><div class="user-chip"><strong>${esc(state.settings?.business_name||'A Fuego')}</strong><small>${esc(state.user?.email||'')}</small></div><button class="nav-btn" id="signOutBtn"><span class="nav-icon">↪</span>Sign out</button></div></aside><main class="main"><header class="topbar"><div><h1>${p[0]}</h1><div class="page-sub">${p[1]}</div></div><div class="topbar-actions"><span class="live-pill">● Cloud live</span><button class="btn btn-secondary btn-sm" id="refreshBtn">↻ Sync</button></div></header><div class="content" id="page"></div></main><nav class="mobile-nav">${nav.filter(([r])=>['dashboard','planner','sales','food','operations','finance','reports','tax','assistant'].includes(r)).map(([r,i,l])=>`<button class="${state.route===r?'active':''}" data-route="${r}"><span>${i}</span>${l}</button>`).join('')}</nav></div>`;
  document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>{state.route=b.dataset.route;location.hash=state.route;renderShell()});
  signOutBtn.onclick=()=>supabase.auth.signOut();refreshBtn.onclick=()=>loadAll();renderPage();
}
function renderPage(){const el=document.getElementById('page');({dashboard:renderDashboard,planner:renderPlanner,sales:renderSales,food:renderFood,operations:renderOperations,finance:renderFinance,reports:renderReports,tax:renderTax,assistant:renderAssistant,settings:renderSettings}[state.route]||renderDashboard)(el)}


function computed(){
  const now=new Date(),startMonth=dateKey(new Date(now.getFullYear(),now.getMonth(),1)),todayKey=today(),weekStartDate=new Date(now);weekStartDate.setDate(now.getDate()-((now.getDay()+6)%7));const startWeek=dateKey(weekStartDate);
  const totalSales=salesForRange('2000-01-01',todayKey),todaySales=salesForRange(todayKey,todayKey),monthSales=salesForRange(startMonth,todayKey),monthExp=loggedExpensesForRange(startMonth,todayKey),monthOp=operatingCostForRange(startMonth,todayKey),monthCosts=monthExp+monthOp,weekOp=operatingCostForRange(startWeek,todayKey),netProfit=monthSales-monthCosts;const activeRecipes=state.recipes.filter(isActive);const avgFood=activeRecipes.length?activeRecipes.reduce((s,r)=>s+(safeNum(r.selling_price)?recipeCost(r)/safeNum(r.selling_price)*100:0),0)/activeRecipes.length:0;
  const top={};const sellerSource=state.shopifyItems.length?state.shopifyItems:state.saleItems;for(const i of sellerSource){const name=canonicalSalesName(i);top[name]=(top[name]||{qty:0,rev:0});top[name].qty+=safeNum(i.quantity??i.qty);top[name].rev+=state.shopifyItems.length?safeNum(i.line_total):safeNum(i.qty)*safeNum(i.unit_price)}const topSellers=Object.entries(top).sort((a,b)=>b[1].qty-a[1].qty).slice(0,5);const products=uniqueSalesProducts(),unmatchedProducts=products.filter(x=>!x.match);
  return{todaySales,monthSales,monthExp,monthOp,monthCosts,weekOp,netProfit,avgFood,totalSales,topSellers,unmatchedProducts,stock:stockCapital()};
}
function salesSeries(){const by={};state.shopifyOrders.forEach(o=>{const d=(o.processed_at||'').slice(0,10);if(d)by[d]=(by[d]||0)+safeNum(o.total_price)});state.sales.filter(x=>!state.shopifyOrders.length||x.include_with_shopify===true).forEach(s=>{by[s.date]=(by[s.date]||0)+safeNum(s.cash_sales)+safeNum(s.card_sales)+safeNum(s.other_sales)});return Object.entries(by).sort().slice(-14).map(([date,total])=>({date,total}))}
function sparkSvg(data){if(!data.length)return`<div class="empty">No sales yet. Add a sales day or connect Shopify.</div>`;const w=760,h=200,p=16,max=Math.max(...data.map(d=>d.total),1),min=Math.min(...data.map(d=>d.total),0);const pts=data.map((d,i)=>`${p+(i*(w-p*2)/Math.max(1,data.length-1))},${h-p-((d.total-min)/(max-min||1))*(h-p*2)}`).join(' ');const area=`${p},${h-p} ${pts} ${w-p},${h-p}`;return`<svg class="chart" viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><defs><linearGradient id="salesGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#ee7e19" stop-opacity=".32"/><stop offset="1" stop-color="#ee7e19" stop-opacity="0"/></linearGradient></defs><g class="chart-grid"><line x1="0" y1="50" x2="760" y2="50"/><line x1="0" y1="100" x2="760" y2="100"/><line x1="0" y1="150" x2="760" y2="150"/></g><polygon points="${area}" class="spark-area"/><polyline points="${pts}" class="spark-line"/></svg><div style="display:flex;justify-content:space-between;font-size:9px;color:var(--muted)"><span>${data[0].date.slice(5)}</span><span>${data.at(-1).date.slice(5)}</span></div>`}


function renderDashboard(el){const c=computed(),series=salesSeries(),ps=plannerStats(),months=monthlyPerformance(6);const contributions=state.capital.filter(x=>x.movement_type==='contribution').reduce((s,x)=>s+Math.abs(safeNum(x.amount)),0),withdrawals=state.capital.filter(x=>x.movement_type==='withdrawal').reduce((s,x)=>s+Math.abs(safeNum(x.amount)),0),netFunding=contributions-withdrawals;const alerts=state.recipes.filter(isActive).map(r=>{const cost=recipeCost(r),cp=safeNum(r.selling_price)?cost/safeNum(r.selling_price)*100:0;return{r,cost,cp}}).sort((a,b)=>b.cp-a.cp).slice(0,5);const maxTop=Math.max(...c.topSellers.map(x=>x[1].qty),1);
  el.innerHTML=`<section class="hero-grid"><div class="hero-panel"><div class="eyebrow">Today · ${new Date().toLocaleDateString('en-AU',{weekday:'long',day:'numeric',month:'long'})}</div><h2>THE FIRE IS ON.</h2><div class="hero-total">${money(c.todaySales)} <small>TODAY'S REVENUE</small></div><span class="hero-delta">${state.shopifyOrders.length?'Shopify + approved additional manual sales':'Manual sales mode'}</span></div><div class="hero-aside"><div class="signal-card"><div class="label">Month revenue</div><div class="value">${money(c.monthSales)}</div><div class="hint">Live month-to-date takings.</div></div><div class="signal-card"><div class="label">Food cost pulse</div><div class="value ${costClass(c.avgFood)}">${pct(c.avgFood)}</div><div class="hint">Average theoretical cost of active menu items.</div></div></div></section>
  <div class="kpi-grid"><div class="kpi"><div class="label">Est. net operating profit</div><div class="value ${c.netProfit>=0?'good':'bad'}">${money(c.netProfit)}</div><div class="meta">Sales − logged expenses − accrued recurring overhead</div></div><div class="kpi"><div class="label">Logged expenses</div><div class="value">${money(c.monthExp)}</div><div class="meta">This month to date</div></div><div class="kpi"><div class="label">Recurring overhead accrued</div><div class="value warn">${money(c.monthOp)}</div><div class="meta">Weekly / monthly / annual costs accrued to today · this week ${money(c.weekOp)}</div></div><div class="kpi"><div class="label">Capital in stock</div><div class="value ember">${money(c.stock.total)}</div><div class="meta">Ingredients + preparations + supplies</div></div></div>
  <div class="kpi-grid planner-kpis"><div class="kpi clickable-kpi" data-go="planner"><div class="label">Open priorities</div><div class="value ember">${ps.open}</div><div class="meta">Across every business area</div></div><div class="kpi clickable-kpi" data-go="planner"><div class="label">Overdue</div><div class="value ${ps.overdue?'bad':''}">${ps.overdue}</div><div class="meta">Needs a new date or action</div></div><div class="kpi clickable-kpi" data-go="reports"><div class="label">Net owner funding</div><div class="value">${money(netFunding)}</div><div class="meta">Capital contributions − withdrawals · never counted as sales</div></div><div class="kpi clickable-kpi" data-go="reports"><div class="label">Total operating costs</div><div class="value ${c.monthCosts>c.monthSales?'bad':''}">${money(c.monthCosts)}</div><div class="meta">${money(c.monthExp)} logged + ${money(c.monthOp)} recurring MTD</div></div></div>
  <div class="grid-2"><section class="panel"><div class="panel-head"><div><div class="panel-title">Sales pulse</div><div class="panel-sub">Last 14 active sales days</div></div></div>${sparkSvg(series)}</section><section class="panel"><div class="panel-head"><div><div class="panel-title">Top sellers</div><div class="panel-sub">Matched to menu names when possible</div></div></div>${c.topSellers.length?`<div class="list">${c.topSellers.map(([name,v],i)=>`<div class="list-row"><div class="rank">0${i+1}</div><div class="grow"><strong>${esc(name)}</strong><small>${v.qty} sold</small><div class="progress"><span style="width:${v.qty/maxTop*100}%"></span></div></div><div class="big-number good">${money(v.rev)}</div></div>`).join('')}</div>`:`<div class="empty">Item-level sales will appear here.</div>`}</section></div>
  <section class="panel" style="margin-top:15px"><div class="panel-head"><div><div class="panel-title">Six-month operating comparison</div><div class="panel-sub">Sales versus logged expenses + recurring overhead. Capital contributions stay outside profit.</div></div><button class="btn btn-secondary btn-sm" data-go="reports">Open reports</button></div>${groupedMonthlySvg(months)}</section>
  <div class="grid-2" style="margin-top:15px"><section class="panel"><div class="panel-head"><div><div class="panel-title">Food cost alerts</div><div class="panel-sub">Active menu only · highest theoretical cost first</div></div><button class="btn btn-ghost btn-sm" data-go="food">Open food</button></div>${alerts.length?alerts.map(a=>`<div class="list-row"><div class="grow"><strong>${esc(a.r.product_name)}</strong><small>${money(a.cost)} theoretical cost</small></div><span class="tag ${a.cp>36?'tag-red':a.cp>30?'tag-orange':'tag-green'}">${pct(a.cp)}</span></div>`).join(''):`<div class="empty">Build recipes to monitor margins.</div>`}</section><section class="panel"><div class="panel-head"><div><div class="panel-title">Operational signal</div><div class="panel-sub">What deserves attention now</div></div></div>${renderSignal(c)}</section></div>`;
  el.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{state.route=b.dataset.go;renderShell()});
}

function renderSignal(c){const issues=[],ps=plannerStats();if(ps.overdue)issues.push(['Priorities are overdue',`${ps.overdue} open task${ps.overdue===1?' is':'s are'} past due.`,'bad']);if(ps.blocked)issues.push(['Work is blocked',`${ps.blocked} task${ps.blocked===1?' is':'s are'} waiting on a decision or dependency.`,'warn']);if(!state.shopifyOrders.length)issues.push(['Shopify is not connected','Sales automation is waiting for the server-side connection.','warn']);if(c.avgFood>36)issues.push(['Food cost is above 36%',`Average theoretical cost is ${pct(c.avgFood)}.`,'bad']);if(c.unmatchedProducts?.length)issues.push(['Sales products need matching',`${c.unmatchedProducts.length} Shopify/menu names are not linked to a recipe yet.`,'warn']);if(state.expenses.some(e=>!e.receipt_url&&!e.receipt_filename))issues.push(['Missing receipts',`${state.expenses.filter(e=>!e.receipt_url&&!e.receipt_filename).length} expenses have no receipt attached.`,'warn']);if(!issues.length)issues.push(['No critical alerts','The recorded data is inside the current operating thresholds.','good']);return issues.map(x=>`<div class="list-row"><div style="width:9px;height:9px;border-radius:50%;background:${x[2]==='good'?'var(--green)':x[2]==='bad'?'var(--danger)':'var(--yellow)'}"></div><div class="grow"><strong>${x[0]}</strong><small>${x[1]}</small></div></div>`).join('')}


// ============================================================
// A FUEGO ERP v3.0 — PROJECTS, PRIORITIES AND TASKS
// ============================================================

const PLANNER_AREAS=['Food truck','Uber Eats','Employees','Marketing','Compliance','Production','Online sales','Finance','Events','Equipment','General'];
const TASK_STATUS_LABELS={idea:'Idea',backlog:'Backlog',todo:'To do',doing:'In progress',blocked:'Blocked',done:'Done',cancelled:'Cancelled'};
const PROJECT_STATUS_LABELS={planned:'Planned',active:'Active',on_hold:'On hold',completed:'Completed',cancelled:'Cancelled'};
function isoPlusDays(days){const d=new Date();d.setDate(d.getDate()+days);return d.toISOString().slice(0,10)}
function taskClosed(t){return ['done','cancelled'].includes(t?.status)}
function plannerOpenTasks(){return state.tasks.filter(t=>t.active!==false&&!taskClosed(t))}
function plannerStats(){const open=plannerOpenTasks(),todayKey=today(),week=isoPlusDays(7);return{open:open.length,overdue:open.filter(t=>t.due_date&&t.due_date<todayKey).length,dueWeek:open.filter(t=>t.due_date&&t.due_date>=todayKey&&t.due_date<=week).length,high:open.filter(t=>['high','urgent'].includes(t.priority)).length,blocked:open.filter(t=>t.status==='blocked'||taskBlockedByDependency(t)).length,ideas:state.tasks.filter(t=>t.active!==false&&t.status==='idea').length,done:state.tasks.filter(t=>t.active!==false&&t.status==='done').length,projects:state.projects.filter(p=>p.active!==false&&!['completed','cancelled'].includes(p.status)).length}}
function taskBlockedByDependency(task){const deps=state.taskDependencies.filter(d=>d.task_id===task.id);return deps.some(d=>{const t=state.tasks.find(x=>x.id===d.depends_on_task_id);return t&&!taskClosed(t)})}
function taskDependencies(task){return state.taskDependencies.filter(d=>d.task_id===task.id).map(d=>state.tasks.find(t=>t.id===d.depends_on_task_id)).filter(Boolean)}
function taskProjectFor(task){return state.projects.find(p=>p.id===task.project_id)||null}
function taskAttachments(task){return state.taskAttachments.filter(a=>a.task_id===task.id)}
function taskSort(a,b){const weight={urgent:0,high:1,medium:2,low:3};const aw=weight[a.priority]??2,bw=weight[b.priority]??2;if(aw!==bw)return aw-bw;const ad=a.due_date||'9999-12-31',bd=b.due_date||'9999-12-31';return ad.localeCompare(bd)||safeNum(a.sort_order)-safeNum(b.sort_order)}
function plannerAreas(){return [...new Set([...PLANNER_AREAS,...state.projects.map(p=>p.area),...state.tasks.map(t=>t.area)].filter(Boolean))].sort()}
function plannerOwners(){return [...new Set([...state.projects.map(p=>p.owner_name),...state.tasks.map(t=>t.owner_name)].filter(Boolean))].sort()}
function filteredPlannerTasks(){return state.tasks.filter(t=>t.active!==false&&(state.plannerArea==='all'||t.area===state.plannerArea)&&(state.plannerOwner==='all'||t.owner_name===state.plannerOwner)).sort(taskSort)}
function priorityTag(priority){const cls=priority==='urgent'?'tag-red':priority==='high'?'tag-orange':priority==='low'?'':'tag-green';return `<span class="tag ${cls}">${esc(priority||'medium')}</span>`}
function taskStatusTag(status){const cls=status==='done'?'tag-green':status==='blocked'?'tag-red':['doing','idea'].includes(status)?'tag-orange':'';return `<span class="tag ${cls}">${esc(TASK_STATUS_LABELS[status]||status)}</span>`}
function projectStatusTag(status){const cls=status==='completed'?'tag-green':status==='on_hold'?'tag-orange':status==='cancelled'?'tag-red':'';return `<span class="tag ${cls}">${esc(PROJECT_STATUS_LABELS[status]||status)}</span>`}
function taskDueMarkup(t){if(!t.due_date)return '<span class="muted">No due date</span>';const overdue=!taskClosed(t)&&t.due_date<today();return `<span class="${overdue?'bad':''}">${dateAU(t.due_date)}${overdue?' · overdue':''}</span>`}

async function seedPlannerIfEmpty(){
  if(!tableFlags.erp_projects||!tableFlags.erp_tasks||state.projects.length||state.tasks.length)return;
  const projects=[
    {name:'Uber Eats menu launch',area:'Uber Eats',description:'Prepare the delivery menu, delivery-safe recipes and pricing.',status:'active',priority:'high',notes:'Imported from Prioridades y brainstorming.'},
    {name:'Monthly Sunday activation',area:'Events',description:'Develop a recurring first-or-last-Sunday customer event.',status:'planned',priority:'medium',notes:'Imported from Prioridades y brainstorming.'},
    {name:'New employee setup',area:'Employees',description:'Insurance, registration, onboarding and operational readiness.',status:'active',priority:'urgent',notes:'Imported from Prioridades y brainstorming.'}
  ].map(x=>({...x,user_id:state.user.id,active:true}));
  const {data:created,error}=await supabase.from('erp_projects').insert(projects).select();if(error){console.warn(error);return}state.projects=created||[];
  const byName=n=>state.projects.find(p=>p.name===n)?.id||null;
  const tasks=[
    {project_id:byName('Uber Eats menu launch'),title:'Review the Uber Eats menu',area:'Uber Eats',status:'todo',priority:'high',impact:'high',description:'Align it with the food-truck menu and identify delivery-specific products and prices.'},
    {project_id:byName('Uber Eats menu launch'),title:'Add classic potato salad',area:'Production',status:'idea',priority:'high',impact:'medium',description:'Create the preparation, recipe cost and delivery portion.'},
    {project_id:byName('Uber Eats menu launch'),title:'Add complete Argentine salad',area:'Production',status:'idea',priority:'high',impact:'high',description:'Include egg, beetroot and the full Argentine-style combination.'},
    {project_id:byName('Monthly Sunday activation'),title:'Choose first or last Sunday cadence',area:'Events',status:'idea',priority:'medium',impact:'medium'},
    {project_id:byName('Monthly Sunday activation'),title:'Prepare the event graphic',area:'Marketing',status:'backlog',priority:'medium',impact:'medium',description:'Create the visual once the event format and date are confirmed.'},
    {project_id:byName('New employee setup'),title:'Arrange insurance for the new employee',area:'Employees',status:'todo',priority:'urgent',impact:'high'},
    {project_id:byName('New employee setup'),title:'Register and onboard the new employee',area:'Employees',status:'todo',priority:'urgent',impact:'high'}
  ].map((x,i)=>({...x,user_id:state.user.id,active:true,sort_order:i}));
  const {data,error:te}=await supabase.from('erp_tasks').insert(tasks).select();if(te)console.warn(te);else state.tasks=data||[];
  toast('Priorities & brainstorming starter plan loaded');
}

function renderPlanner(el){
  if(!tableFlags.erp_projects||!tableFlags.erp_tasks)return schemaNotice(el,'Projects & Tasks');
  const ps=plannerStats(),tabs=[['board','Priority board'],['projects','Projects'],['ideas','Ideas inbox']];
  el.innerHTML=`<div class="toolbar"><div class="tabs">${tabs.map(t=>`<button class="tab ${state.plannerTab===t[0]?'active':''}" data-planner-tab="${t[0]}">${t[1]}</button>`).join('')}</div><select class="select compact-select" id="plannerArea"><option value="all">All areas</option>${plannerAreas().map(a=>`<option value="${esc(a)}" ${optionSelected(state.plannerArea,a)}>${esc(a)}</option>`).join('')}</select><select class="select compact-select" id="plannerOwner"><option value="all">All owners</option>${plannerOwners().map(a=>`<option value="${esc(a)}" ${optionSelected(state.plannerOwner,a)}>${esc(a)}</option>`).join('')}</select><button class="btn btn-secondary" id="addProjectBtn">+ Project</button><button class="btn btn-primary" id="addTaskBtn">+ Task / idea</button></div>
  <div class="kpi-grid"><div class="kpi"><div class="label">Open priorities</div><div class="value ember">${ps.open}</div></div><div class="kpi"><div class="label">Overdue</div><div class="value ${ps.overdue?'bad':''}">${ps.overdue}</div><div class="meta">Past due and still open</div></div><div class="kpi"><div class="label">Due in 7 days</div><div class="value ${ps.dueWeek?'warn':''}">${ps.dueWeek}</div></div><div class="kpi"><div class="label">Blocked</div><div class="value ${ps.blocked?'warn':''}">${ps.blocked}</div><div class="meta">Status or dependency</div></div></div><div id="plannerPanel"></div>`;
  el.querySelectorAll('[data-planner-tab]').forEach(b=>b.onclick=()=>{state.plannerTab=b.dataset.plannerTab;renderPlanner(el)});
  plannerArea.onchange=()=>{state.plannerArea=plannerArea.value;renderPlanner(el)};plannerOwner.onchange=()=>{state.plannerOwner=plannerOwner.value;renderPlanner(el)};
  addProjectBtn.onclick=()=>openProjectModal();addTaskBtn.onclick=()=>openTaskModal();
  if(state.plannerTab==='projects')renderProjectList(document.getElementById('plannerPanel'));else if(state.plannerTab==='ideas')renderIdeasInbox(document.getElementById('plannerPanel'));else renderPriorityBoard(document.getElementById('plannerPanel'));
}

function taskCard(t){const p=taskProjectFor(t),deps=taskDependencies(t),files=taskAttachments(t),blocked=taskBlockedByDependency(t);return `<article class="task-card ${taskClosed(t)?'task-closed':''}" data-edit-task="${t.id}"><div class="task-card-top"><div>${priorityTag(t.priority)} ${taskStatusTag(t.status)}</div>${files.length?`<span class="tag">📎 ${files.length}</span>`:''}</div><h4>${esc(t.title)}</h4><p>${esc(t.description||'')}</p><div class="task-meta"><span>${esc(t.area||'General')}</span>${p?`<span>${esc(p.name)}</span>`:''}${t.owner_name?`<span>👤 ${esc(t.owner_name)}</span>`:''}${t.recurring?`<span>↻ ${esc(t.recurrence_rule||'Recurring')}</span>`:''}</div><div class="task-card-foot"><span>${taskDueMarkup(t)}</span>${blocked?'<span class="tag tag-red">Dependency blocked</span>':''}</div>${deps.length?`<div class="task-deps">Waiting on: ${deps.map(d=>esc(d.title)).join(', ')}</div>`:''}</article>`}
function renderPriorityBoard(el){const tasks=filteredPlannerTasks(),columns=[['idea','Ideas',tasks.filter(t=>t.status==='idea'&&!taskBlockedByDependency(t))],['todo','To do',tasks.filter(t=>['backlog','todo'].includes(t.status)&&!taskBlockedByDependency(t))],['doing','In progress',tasks.filter(t=>t.status==='doing'&&!taskBlockedByDependency(t))],['blocked','Blocked',tasks.filter(t=>t.status==='blocked'||taskBlockedByDependency(t))],['done','Done',tasks.filter(t=>t.status==='done')]];el.innerHTML=`<div class="planner-board">${columns.map(([key,label,rows])=>`<section class="board-column"><div class="board-head"><strong>${label}</strong><span>${rows.length}</span></div><div class="board-stack">${rows.map(taskCard).join('')||'<div class="board-empty">Nothing here.</div>'}</div></section>`).join('')}</div>`;el.querySelectorAll('[data-edit-task]').forEach(c=>c.onclick=()=>openTaskModal(state.tasks.find(t=>t.id===c.dataset.editTask)))}
function renderIdeasInbox(el){const ideas=filteredPlannerTasks().filter(t=>t.status==='idea');el.innerHTML=`<section class="panel"><div class="panel-head"><div><div class="panel-title">Brainstorming inbox</div><div class="panel-sub">Capture first. Promote to Backlog or To do when the next action is clear.</div></div><button class="btn btn-primary btn-sm" id="ideaBtn">+ Capture idea</button></div>${ideas.length?`<div class="planner-list">${ideas.map(taskCard).join('')}</div>`:'<div class="empty">No ideas waiting. Capture the next thought before it disappears.</div>'}</section>`;ideaBtn.onclick=()=>openTaskModal({status:'idea',priority:'medium',impact:'medium',active:true});el.querySelectorAll('[data-edit-task]').forEach(c=>c.onclick=()=>openTaskModal(state.tasks.find(t=>t.id===c.dataset.editTask)))}
function renderProjectList(el){const rows=state.projects.filter(p=>p.active!==false&&(state.plannerArea==='all'||p.area===state.plannerArea)&&(state.plannerOwner==='all'||p.owner_name===state.plannerOwner));el.innerHTML=`<div class="project-grid">${rows.map(p=>{const tasks=state.tasks.filter(t=>t.project_id===p.id&&t.active!==false),done=tasks.filter(taskClosed).length,progress=tasks.length?done/tasks.length*100:0;return `<article class="project-card"><div class="project-card-head"><div>${projectStatusTag(p.status)} ${priorityTag(p.priority)}</div><span class="tag">${esc(p.area)}</span></div><h3>${esc(p.name)}</h3><p>${esc(p.description||'')}</p><div class="task-meta">${p.owner_name?`<span>👤 ${esc(p.owner_name)}</span>`:''}${p.due_date?`<span>Due ${dateAU(p.due_date)}</span>`:''}<span>${tasks.length} tasks</span></div><div class="progress project-progress"><span style="width:${progress}%"></span></div><div class="project-foot"><small>${done}/${tasks.length} complete</small><div class="row-actions"><button class="btn btn-secondary btn-sm" data-new-project-task="${p.id}">+ Task</button><button class="btn btn-secondary btn-sm" data-edit-project="${p.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-project="${p.id}">Delete</button></div></div></article>`}).join('')}</div>${rows.length?'':'<div class="empty">No projects in this filter.</div>'}`;el.querySelectorAll('[data-new-project-task]').forEach(b=>b.onclick=()=>openTaskModal({project_id:b.dataset.newProjectTask,status:'todo',priority:'medium',impact:'medium',active:true}));el.querySelectorAll('[data-edit-project]').forEach(b=>b.onclick=()=>openProjectModal(state.projects.find(p=>p.id===b.dataset.editProject)));el.querySelectorAll('[data-del-project]').forEach(b=>b.onclick=()=>deleteRow('erp_projects',b.dataset.delProject,'project'))}

function projectOptions(selected=''){return `<option value="">No project / standalone</option>${state.projects.filter(p=>p.active!==false).map(p=>`<option value="${p.id}" ${optionSelected(selected,p.id)}>${esc(p.name)} · ${esc(p.area)}</option>`).join('')}`}
function areaOptions(selected='General'){return plannerAreas().map(a=>`<option value="${esc(a)}" ${optionSelected(selected,a)}>${esc(a)}</option>`).join('')}
function statusOptions(map,selected){return Object.entries(map).map(([v,l])=>`<option value="${v}" ${optionSelected(selected,v)}>${l}</option>`).join('')}
function priorityOptions(selected='medium'){return ['low','medium','high','urgent'].map(v=>`<option value="${v}" ${optionSelected(selected,v)}>${v}</option>`).join('')}

function openProjectModal(record=null){const editing=!!record,r=record||{};const m=modal(`<div class="eyebrow">A Fuego ERP</div><h3>${editing?'EDIT':'NEW'} PROJECT</h3><form id="projectForm"><div class="form-grid"><div class="field span-2"><label>Name</label><input class="input" id="projectName" value="${esc(r.name||'')}" required></div><div class="field"><label>Area</label><select class="select" id="projectArea">${areaOptions(r.area||'General')}</select></div><div class="field"><label>Status</label><select class="select" id="projectStatus">${statusOptions(PROJECT_STATUS_LABELS,r.status||'active')}</select></div><div class="field"><label>Priority</label><select class="select" id="projectPriority">${priorityOptions(r.priority)}</select></div><div class="field"><label>Responsible</label><input class="input" id="projectOwner" value="${esc(r.owner_name||'')}" placeholder="Diego, employee, accountant..."></div><div class="field"><label>Start date</label><input class="input" type="date" id="projectStart" value="${esc(r.start_date||'')}"></div><div class="field"><label>Due date</label><input class="input" type="date" id="projectDue" value="${esc(r.due_date||'')}"></div><div class="field span-4"><label>Description</label><textarea class="textarea" id="projectDescription" rows="3">${esc(r.description||'')}</textarea></div><div class="field span-4"><label>Notes</label><textarea class="textarea" id="projectNotes" rows="2">${esc(r.notes||'')}</textarea></div><div class="field"><label>Visibility</label><select class="select" id="projectActive"><option value="true" ${r.active!==false?'selected':''}>Active</option><option value="false" ${r.active===false?'selected':''}>Inactive / archived</option></select></div></div><div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Create project'}</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;projectForm.onsubmit=async e=>{e.preventDefault();const row={name:projectName.value.trim(),area:projectArea.value,status:projectStatus.value,priority:projectPriority.value,owner_name:projectOwner.value.trim()||null,start_date:projectStart.value||null,due_date:projectDue.value||null,description:projectDescription.value.trim()||null,notes:projectNotes.value.trim()||null,active:projectActive.value==='true'};const q=editing?supabase.from('erp_projects').update(row).eq('id',r.id):supabase.from('erp_projects').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Project updated':'Project created');closeModal();await loadAll()}}

function existingAttachmentMarkup(taskId){const rows=state.taskAttachments.filter(a=>a.task_id===taskId);return rows.length?`<div class="attachment-list">${rows.map(a=>`<div class="attachment-row"><div class="grow"><strong>${esc(a.file_name)}</strong><small>${a.size_bytes?`${Math.ceil(a.size_bytes/1024)} KB`:esc(a.mime_type||'File')}</small></div><button type="button" class="btn btn-secondary btn-sm" data-open-attachment="${a.id}">Open</button><button type="button" class="btn btn-danger btn-sm" data-delete-attachment="${a.id}">Delete</button></div>`).join('')}</div>`:'<div class="muted" style="font-size:11px">No files attached yet.</div>'}
function openTaskModal(record=null){const editing=!!record?.id,r=record||{},selectedDeps=editing?state.taskDependencies.filter(d=>d.task_id===r.id).map(d=>d.depends_on_task_id):[];const m=modal(`<div class="eyebrow">Priorities & brainstorming</div><h3>${editing?'EDIT':'NEW'} TASK / IDEA</h3><form id="taskForm"><div class="form-grid"><div class="field span-2"><label>Title</label><input class="input" id="taskTitle" value="${esc(r.title||'')}" required></div><div class="field span-2"><label>Project</label><select class="select" id="taskProject">${projectOptions(r.project_id||'')}</select></div><div class="field"><label>Area</label><select class="select" id="taskArea">${areaOptions(r.area||taskProjectFor(r)?.area||'General')}</select></div><div class="field"><label>Status</label><select class="select" id="taskStatus">${statusOptions(TASK_STATUS_LABELS,r.status||'todo')}</select></div><div class="field"><label>Priority</label><select class="select" id="taskPriority">${priorityOptions(r.priority)}</select></div><div class="field"><label>Impact</label><select class="select" id="taskImpact">${['low','medium','high'].map(v=>`<option value="${v}" ${optionSelected(r.impact||'medium',v)}>${v}</option>`).join('')}</select></div><div class="field"><label>Responsible</label><input class="input" id="taskOwner" value="${esc(r.owner_name||'')}"></div><div class="field"><label>Due date</label><input class="input" type="date" id="taskDue" value="${esc(r.due_date||'')}"></div><div class="field"><label>Recurring</label><select class="select" id="taskRecurring"><option value="false" ${!r.recurring?'selected':''}>No</option><option value="true" ${r.recurring?'selected':''}>Yes</option></select></div><div class="field"><label>Cadence / recurrence</label><input class="input" id="taskRecurrence" value="${esc(r.recurrence_rule||'')}" placeholder="First Sunday monthly"></div><div class="field span-4"><label>Description / desired outcome</label><textarea class="textarea" id="taskDescription" rows="3">${esc(r.description||'')}</textarea></div><div class="field span-2"><label>Blocked reason</label><input class="input" id="taskBlocked" value="${esc(r.blocked_reason||'')}" placeholder="Waiting on quote, permit, decision..."></div><div class="field span-2"><label>Depends on</label><select class="select multi-select" id="taskDeps" multiple size="4">${state.tasks.filter(t=>t.id!==r.id&&!taskClosed(t)).map(t=>`<option value="${t.id}" ${selectedDeps.includes(t.id)?'selected':''}>${esc(t.title)}</option>`).join('')}</select><small class="muted">Ctrl/Cmd-click to select more than one.</small></div><div class="field span-4"><label>Notes</label><textarea class="textarea" id="taskNotes" rows="2">${esc(r.notes||'')}</textarea></div><div class="field"><label>Visibility</label><select class="select" id="taskActive"><option value="true" ${r.active!==false?'selected':''}>Active</option><option value="false" ${r.active===false?'selected':''}>Inactive / archived</option></select></div><div class="field span-2"><label>Attach files</label><input class="input" type="file" id="taskFiles" multiple></div></div>${editing?`<section class="attachment-panel"><div class="panel-title">Attached documents</div>${existingAttachmentMarkup(r.id)}</section>`:''}<div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button>${editing?'<button class="btn btn-danger" id="deleteTaskBtn" type="button">Delete task</button>':''}<button class="btn btn-primary">${editing?'Save changes':'Create task'}</button></div></form>`);
  m.querySelector('[data-close]').onclick=closeModal;
  m.querySelectorAll('[data-open-attachment]').forEach(b=>b.onclick=()=>openTaskAttachment(b.dataset.openAttachment));m.querySelectorAll('[data-delete-attachment]').forEach(b=>b.onclick=()=>deleteTaskAttachment(b.dataset.deleteAttachment));
  if(editing)deleteTaskBtn.onclick=async()=>{closeModal();await deleteRow('erp_tasks',r.id,'task')};
  taskProject.onchange=()=>{const p=state.projects.find(x=>x.id===taskProject.value);if(p)taskArea.value=p.area};
  taskStatus.onchange=()=>{if(taskStatus.value!=='blocked')taskBlocked.value=''};
  taskForm.onsubmit=async e=>{e.preventDefault();const nowIso=new Date().toISOString(),row={project_id:taskProject.value||null,title:taskTitle.value.trim(),area:taskArea.value,status:taskStatus.value,priority:taskPriority.value,impact:taskImpact.value,owner_name:taskOwner.value.trim()||null,due_date:taskDue.value||null,recurring:taskRecurring.value==='true',recurrence_rule:taskRecurrence.value.trim()||null,description:taskDescription.value.trim()||null,blocked_reason:taskBlocked.value.trim()||null,notes:taskNotes.value.trim()||null,active:taskActive.value==='true',completed_at:taskStatus.value==='done'?(r.completed_at||nowIso):null};let taskId=r.id;if(editing){const {error}=await supabase.from('erp_tasks').update(row).eq('id',r.id);if(error)return toast(error.message,true)}else{const {data,error}=await supabase.from('erp_tasks').insert({...row,user_id:state.user.id}).select().single();if(error)return toast(error.message,true);taskId=data.id}
    await supabase.from('erp_task_dependencies').delete().eq('task_id',taskId);const deps=[...taskDeps.selectedOptions].map(o=>o.value).filter(Boolean);if(deps.length){const {error}=await supabase.from('erp_task_dependencies').insert(deps.map(id=>({user_id:state.user.id,task_id:taskId,depends_on_task_id:id})));if(error)return toast(error.message,true)}
    const files=[...taskFiles.files];for(const file of files){const clean=file.name.replace(/[^a-zA-Z0-9._-]+/g,'-'),path=`${state.user.id}/${taskId}/${Date.now()}-${clean}`;const {error:upErr}=await supabase.storage.from('erp-documents').upload(path,file,{contentType:file.type||'application/octet-stream',upsert:false});if(upErr){toast(`Task saved, but ${file.name} failed: ${upErr.message}`,true);continue}const {error:metaErr}=await supabase.from('erp_task_attachments').insert({user_id:state.user.id,task_id:taskId,file_name:file.name,file_path:path,mime_type:file.type||null,size_bytes:file.size});if(metaErr)toast(`File uploaded but metadata failed: ${metaErr.message}`,true)}
    toast(editing?'Task updated':'Task created');closeModal();await loadAll()};
}
async function openTaskAttachment(id){const a=state.taskAttachments.find(x=>x.id===id);if(!a)return;const {data,error}=await supabase.storage.from('erp-documents').createSignedUrl(a.file_path,60);if(error)return toast(error.message,true);window.open(data.signedUrl,'_blank','noopener')}
async function deleteTaskAttachment(id){const a=state.taskAttachments.find(x=>x.id===id);if(!a||!confirm(`Delete ${a.file_name}?`))return;const {error:se}=await supabase.storage.from('erp-documents').remove([a.file_path]);if(se)return toast(se.message,true);const {error}=await supabase.from('erp_task_attachments').delete().eq('id',id);if(error)return toast(error.message,true);toast('Attachment deleted');closeModal();await loadAll()}


function renderSales(el){
  const total=state.sales.reduce((a,s)=>a+safeNum(s.cash_sales)+safeNum(s.card_sales)+safeNum(s.other_sales),0),products=uniqueSalesProducts(),matched=products.filter(x=>x.match),unmatched=products.filter(x=>!x.match);el.innerHTML=`<div class="toolbar"><button class="btn btn-primary" id="addSalesBtn">+ Add sales day</button><button class="btn btn-secondary" id="shopifyBtn">${state.shopifyOrders.length?'↻ Sync Shopify':'Connect Shopify'}</button><span class="tag ${state.shopifyOrders.length?'tag-green':'tag-orange'}">${state.shopifyOrders.length?'Shopify live':'Manual mode'}</span></div><div class="kpi-grid"><div class="kpi"><div class="label">Manual sales</div><div class="value">${money(total)}</div></div><div class="kpi"><div class="label">Shopify orders</div><div class="value ember">${state.shopifyOrders.length}</div></div><div class="kpi"><div class="label">Menu matches</div><div class="value good">${matched.length}</div></div><div class="kpi"><div class="label">Unmatched products</div><div class="value ${unmatched.length?'warn':''}">${unmatched.length}</div></div></div><section class="panel"><div class="panel-head"><div><div class="panel-title">Sales → menu matching</div><div class="panel-sub">Word order is ignored: “Sandwich de Brisket” can match “Brisket Sandwich”. Add aliases in Edit recipe when names are genuinely different.</div></div></div>${products.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Sales product</th><th>Qty</th><th>Menu recipe</th><th>Match</th></tr></thead><tbody>${products.map(x=>`<tr><td><strong>${esc(x.raw)}</strong></td><td>${x.qty}</td><td>${x.match?esc(x.match.recipe.product_name):'<span class="warn">Not matched</span>'}</td><td><span class="tag ${x.match?'tag-green':'tag-orange'}">${x.match?esc(x.match.method):'Add alias'}</span></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">Item-level products will appear after Shopify sync or manual sale items.</div>'}</section><section class="panel" style="margin-top:15px"><div class="panel-head"><div><div class="panel-title">Sales register</div><div class="panel-sub">Manual sales days can be corrected. Shopify orders stay source-controlled by Shopify.</div></div></div>${state.sales.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Location</th><th>Cash</th><th>Card</th><th>Other</th><th>Shopify treatment</th><th>Total</th><th></th></tr></thead><tbody>${state.sales.map(s=>`<tr><td>${dateAU(s.date)}</td><td>${esc(s.locations_events?.name||'Unassigned')}</td><td>${money(s.cash_sales)}</td><td>${money(s.card_sales)}</td><td>${money(s.other_sales)}</td><td>${state.shopifyOrders.length?`<span class="tag ${s.include_with_shopify===true?'tag-green':''}">${s.include_with_shopify===true?'Additional sale':'Reference only'}</span>`:'<span class="tag">Manual</span>'}</td><td class="money">${money(safeNum(s.cash_sales)+safeNum(s.card_sales)+safeNum(s.other_sales))}</td><td><div class="row-actions"><button class="btn btn-secondary btn-sm" data-edit-sale="${s.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-sale="${s.id}">Delete</button></div></td></tr>`).join('')}</tbody></table></div>`:`<div class="empty">No manual sales days yet.</div>`}</section>`;
  addSalesBtn.onclick=()=>openSalesModal();shopifyBtn.onclick=()=>state.shopifyOrders.length?syncShopify():openShopifyInfo();el.querySelectorAll('[data-edit-sale]').forEach(b=>b.onclick=()=>openSalesModal(state.sales.find(x=>x.id===b.dataset.editSale)));el.querySelectorAll('[data-del-sale]').forEach(b=>b.onclick=()=>deleteRow('sales_days',b.dataset.delSale,'sales day'));
}
function openSalesModal(record=null){const editing=!!record,includeDefault=editing?record?.include_with_shopify===true:true;const m=modal(`<div class="eyebrow">Manual sales</div><h3>${editing?'EDIT':'ADD'} SALES DAY</h3><form id="salesForm"><div class="form-grid"><div class="field"><label>Date</label><input class="input" type="date" id="saleDate" value="${esc(record?.date||today())}" required></div><div class="field"><label>Location / event</label><select class="select" id="saleLoc"><option value="">Unassigned</option>${state.locations.map(l=>`<option value="${l.id}" ${optionSelected(record?.location_event_id,l.id)}>${esc(l.name)}</option>`).join('')}</select></div><div class="field"><label>Cash</label><input class="input" type="number" step=".01" id="saleCash" value="${safeNum(record?.cash_sales)}"></div><div class="field"><label>Card</label><input class="input" type="number" step=".01" id="saleCard" value="${safeNum(record?.card_sales)}"></div><div class="field"><label>Other / delivery</label><input class="input" type="number" step=".01" id="saleOther" value="${safeNum(record?.other_sales)}"></div><div class="field"><label>Tips</label><input class="input" type="number" step=".01" id="saleTips" value="${safeNum(record?.tips)}"></div><div class="field"><label>When Shopify is connected</label><select class="select" id="saleInclude"><option value="true" ${optionSelected(includeDefault,true)}>Additional / non-Shopify sale</option><option value="false" ${optionSelected(includeDefault,false)}>Reference only · do not add to Shopify</option></select><small class="muted">Prevents accidental double counting.</small></div><div class="field span-2"><label>Notes</label><input class="input" id="saleNotes" value="${esc(record?.notes||'')}" placeholder="Rain, sold out, event notes..."></div></div><div class="inline-actions"><button class="btn btn-ghost" type="button" data-close>Cancel</button><button class="btn btn-primary" type="submit">${editing?'Save changes':'Save sales'}</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;m.querySelector('#salesForm').onsubmit=async e=>{e.preventDefault();const row={date:saleDate.value,location_event_id:saleLoc.value||null,cash_sales:safeNum(saleCash.value),card_sales:safeNum(saleCard.value),other_sales:safeNum(saleOther.value),tips:safeNum(saleTips.value),include_with_shopify:saleInclude.value==='true',notes:saleNotes.value.trim()};const q=editing?supabase.from('sales_days').update(row).eq('id',record.id):supabase.from('sales_days').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Sales day updated':'Sales day added');closeModal();await loadAll()}}
async function syncShopify(){
  if(!tableFlags.shopify_orders)return openShopifyInfo('Shopify tables are not ready.');
  toast('Syncing Shopify orders...');
  const {data,error}=await supabase.functions.invoke('shopify-sync',{body:{mode:'incremental'}});
  if(error){openShopifyInfo(error.message);return}
  const count=data?.ordersSynced ?? data?.orders ?? 0;
  toast(`Shopify sync complete: ${count} orders synced`);
  closeModal();
  await loadAll();
}
async function registerShopifyWebhooks(btn){
  if(btn){btn.disabled=true;btn.textContent='Registering...'}
  const {data,error}=await supabase.functions.invoke('shopify-register-webhooks',{body:{}});
  if(error){if(btn){btn.disabled=false;btn.textContent='Register webhooks'};return openShopifyInfo(error.message)}
  const failures=(data?.results||[]).flatMap(r=>r?.userErrors||[]);
  if(failures.length){if(btn){btn.disabled=false;btn.textContent='Register webhooks'};return openShopifyInfo(failures.map(e=>e.message).join(' · '))}
  toast('Shopify webhooks registered');
  if(btn){btn.textContent='Webhooks registered ✓'}
}
function openShopifyInfo(err=''){
  const m=modal(`<div class="eyebrow">Server-side integration</div><h3>SHOPIFY CONNECTION</h3>${err?`<div class="notice bad">${esc(err)}</div>`:''}<p class="section-intro">A Fuego OS connects to Shopify only through secure server-side Edge Functions. Your Shopify Client Secret never reaches the browser.</p><div class="notice"><strong>One-time connection:</strong><br>1. Register Shopify webhooks once.<br>2. Run Sync now to import existing orders.<br>3. New and updated orders then arrive automatically through Shopify webhooks.<br><br><strong>Important:</strong> Do not press “Register webhooks” more than once unless we are troubleshooting the integration.</div><div class="inline-actions" style="margin-top:16px"><button class="btn btn-secondary" id="registerWebhooksBtn">Register webhooks</button><button class="btn btn-primary" id="syncShopifyNowBtn">Sync now</button><button class="btn btn-ghost" data-close>Close</button></div>`);
  m.querySelector('[data-close]').onclick=closeModal;
  m.querySelector('#registerWebhooksBtn').onclick=e=>registerShopifyWebhooks(e.currentTarget);
  m.querySelector('#syncShopifyNowBtn').onclick=syncShopify;
}


function renderFood(el){const tabs=[['menu','Menu & recipes'],['ingredients','Ingredients'],['preparations','Preparations'],['supplies','Supplies & packaging'],['stock','General stock & buying']];el.innerHTML=`<div class="toolbar"><div class="tabs">${tabs.map(t=>`<button class="tab ${state.foodTab===t[0]?'active':''}" data-food-tab="${t[0]}">${t[1]}</button>`).join('')}</div><select class="select compact-select" id="foodStatus"><option value="active" ${optionSelected(state.foodStatusFilter,'active')}>Active only</option><option value="all" ${optionSelected(state.foodStatusFilter,'all')}>All items</option><option value="inactive" ${optionSelected(state.foodStatusFilter,'inactive')}>Inactive only</option></select>${state.foodTab==='stock'?'':'<button class="btn btn-primary" id="foodAddBtn">+ Add</button>'}</div><div id="foodPanel"></div>`;el.querySelectorAll('[data-food-tab]').forEach(b=>b.onclick=()=>{state.foodTab=b.dataset.foodTab;renderFood(el)});foodStatus.onchange=()=>{state.foodStatusFilter=foodStatus.value;renderFood(el)};const add=document.getElementById('foodAddBtn');if(add)add.onclick=()=>({menu:openRecipeModal,ingredients:openIngredientModal,preparations:openPreparationModal,supplies:openSupplyModal}[state.foodTab]||openIngredientModal)();renderFoodTab(document.getElementById('foodPanel'))}

function renderFoodTab(el){if(state.foodTab==='ingredients')return renderIngredients(el);if(state.foodTab==='preparations')return renderPreparations(el);if(state.foodTab==='supplies')return renderSupplies(el);if(state.foodTab==='stock')return renderStockOverview(el);renderMenu(el)}

function renderMenu(el){const rows=visibleByStatus(state.recipes),active=state.recipes.filter(isActive),avg=active.length?active.reduce((s,r)=>s+(safeNum(r.selling_price)?recipeCost(r)/safeNum(r.selling_price)*100:0),0)/active.length:0;el.innerHTML=`<div class="kpi-grid"><div class="kpi"><div class="label">Avg food cost</div><div class="value ${costClass(avg)}">${pct(avg)}</div><div class="meta">Active menu only</div></div><div class="kpi"><div class="label">Avg gross margin</div><div class="value good">${pct(100-avg)}</div></div><div class="kpi"><div class="label">Active products</div><div class="value ember">${active.length}</div><div class="meta">${state.recipes.length-active.length} inactive</div></div><div class="kpi"><div class="label">Margin alerts</div><div class="value bad">${active.filter(r=>safeNum(r.selling_price)&&recipeCost(r)/safeNum(r.selling_price)*100>36).length}</div></div></div><div class="menu-cards">${rows.map(r=>{const cost=recipeCost(r),cp=safeNum(r.selling_price)?cost/safeNum(r.selling_price)*100:0,aliases=Array.isArray(r.sales_aliases)?r.sales_aliases:[];return`<article class="dish-card ${isActive(r)?'':'inactive-card'}"><div class="dish-art"></div><div class="dish-body"><div class="dish-row"><div><div class="dish-title">${esc(r.product_name)}</div><div class="dish-desc">${esc(r.notes||'Costed A Fuego product')}</div></div><div class="dish-price">${money(r.selling_price)}</div></div><div class="status-line"><span class="tag ${isActive(r)?'tag-green':'tag-orange'}">${isActive(r)?'Active':'Inactive'}</span>${aliases.length?`<span class="tag">${aliases.length} sales alias${aliases.length===1?'':'es'}</span>`:''}</div><div class="metrics-mini"><div class="metric-mini"><label>Cost</label><b>${money(cost)}</b></div><div class="metric-mini"><label>Food cost</label><b class="${costClass(cp)}">${pct(cp)}</b></div><div class="metric-mini"><label>Margin</label><b class="good">${pct(100-cp)}</b></div></div><div class="inline-actions" style="margin-top:12px"><button class="btn btn-primary btn-sm" data-start-production="${r.id}">Start production</button><button class="btn btn-secondary btn-sm" data-edit-recipe="${r.id}">Edit recipe</button><button class="btn btn-danger btn-sm" data-del-recipe="${r.id}">Delete</button></div></div></article>`}).join('')}</div>${rows.length?'':'<div class="empty">No products in this status filter.</div>'}`;el.querySelectorAll('[data-start-production]').forEach(b=>b.onclick=()=>openProductionCalculator(state.recipes.find(x=>x.id===b.dataset.startProduction)));el.querySelectorAll('[data-edit-recipe]').forEach(b=>b.onclick=()=>openRecipeModal(state.recipes.find(x=>x.id===b.dataset.editRecipe)));el.querySelectorAll('[data-del-recipe]').forEach(b=>b.onclick=()=>deleteRow('recipes',b.dataset.delRecipe,'recipe'))}

function renderIngredients(el){const rows=visibleByStatus(state.inventory),cats={};rows.forEach(i=>{const cat=(i.notes||'Other').split(' · ')[0]||'Other';(cats[cat]??=[]).push(i)});const stock=stockCapital();el.innerHTML=`<div class="kpi-grid"><div class="kpi"><div class="label">Ingredient stock value</div><div class="value ember">${money(stock.ingredients)}</div></div><div class="kpi"><div class="label">Active ingredients</div><div class="value">${state.inventory.filter(isActive).length}</div></div><div class="kpi"><div class="label">Inactive ingredients</div><div class="value">${state.inventory.filter(x=>!isActive(x)).length}</div></div><div class="kpi"><div class="label">Beverage stock</div><div class="value good">${money(state.inventory.filter(i=>(i.notes||'').split(' · ')[0]==='Beverages').reduce((s,i)=>s+ingredientStockValue(i),0))}</div></div></div>${Object.entries(cats).map(([cat,items])=>`<section class="panel" style="margin-bottom:12px"><div class="panel-head"><div class="panel-title">${esc(cat)}</div><span class="tag">${items.length} items</span></div><div class="table-wrap"><table class="table"><thead><tr><th>Ingredient</th><th>Status</th><th>Purchase unit</th><th>Purchase price</th><th>Stock</th><th>Stock value</th><th></th></tr></thead><tbody>${items.map(i=>`<tr><td><strong>${esc(i.name)}</strong><div class="muted" style="font-size:9px">${esc((i.notes||'').split(' · ').slice(1).join(' · '))}</div></td><td><span class="tag ${isActive(i)?'tag-green':'tag-orange'}">${isActive(i)?'Active':'Inactive'}</span></td><td>${ingredientPurchaseQty(i)} ${esc(i.unit)}</td><td class="money">${money(purchasePrice(i))}</td><td><strong>${safeNum(i.stock_qty)} ${esc(i.unit)}</strong></td><td class="money">${money(ingredientStockValue(i))}</td><td><div class="row-actions"><button class="btn btn-secondary btn-sm" data-edit-ing="${i.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-ing="${i.id}">Delete</button></div></td></tr>`).join('')}</tbody></table></div></section>`).join('')||'<div class="empty">No ingredients in this status filter.</div>'}`;el.querySelectorAll('[data-edit-ing]').forEach(b=>b.onclick=()=>openIngredientModal(state.inventory.find(x=>x.id===b.dataset.editIng)));el.querySelectorAll('[data-del-ing]').forEach(b=>b.onclick=()=>deleteRow('inventory_items',b.dataset.delIng,'ingredient'))}

function openIngredientModal(item=null){const editing=!!item,parts=(item?.notes||'Other').split(' · '),cat=parts[0]||'Other',supplier=parts.slice(1).join(' · ');const cats=['Meat','Bread','Produce','Dairy','Beverages','Dry Goods','Condiments & Spices','Oils & Vinegars','Sauces','Packaging','Other'];const units=['kg','g','unit','L','ml','pack','box','tray','bottle','can'];const m=modal(`<div class="eyebrow">Food database & stock</div><h3>${editing?'EDIT':'ADD'} INGREDIENT</h3><form id="ingForm"><div class="form-grid"><div class="field span-2"><label>Name</label><input class="input" id="ingName" value="${esc(item?.name||'')}" required></div><div class="field"><label>Status</label><select class="select" id="ingActive"><option value="true" ${isActive(item)?'selected':''}>Active / current season</option><option value="false" ${!isActive(item)?'selected':''}>Inactive / hidden</option></select></div><div class="field"><label>Category</label><select class="select" id="ingCat">${cats.map(x=>`<option ${optionSelected(cat,x)}>${x}</option>`).join('')}</select></div><div class="field span-2"><label>Supplier</label><input class="input" id="ingSupplier" value="${esc(supplier)}"></div><div class="field"><label>Purchase price</label><input class="input" type="number" step=".01" id="ingPrice" value="${editing?purchasePrice(item).toFixed(2):''}" required></div><div class="field"><label>Purchase qty</label><input class="input" type="number" step=".001" id="ingQty" value="${editing?ingredientPurchaseQty(item):1}" required></div><div class="field"><label>Unit</label><select class="select" id="ingUnit">${units.map(x=>`<option ${optionSelected(item?.unit||'kg',x)}>${x}</option>`).join('')}</select></div><div class="field"><label>Stock on hand</label><input class="input" type="number" step=".001" id="ingStock" value="${editing?safeNum(item.stock_qty):0}"></div><div class="field"><label>Weekly minimum / par</label><input class="input" type="number" step=".001" id="ingMin" value="${editing?safeNum(item.minimum_stock_qty):0}"></div></div><div class="notice">Stock uses the same unit as the purchase unit. Example: Brisket 12.5 kg, Coke 48 unit, milk 6 L. Stock value is calculated automatically.</div>${editing?'<div class="notice" style="margin-top:8px">Changing the purchase price automatically recosts recipes where the component quantity is known.</div>':''}<div class="inline-actions"><button class="btn btn-ghost" type="button" data-close>Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Save ingredient'}</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;ingForm.onsubmit=async e=>{e.preventDefault();const qty=safeNum(ingQty.value),unit=ingUnit.value,price=safeNum(ingPrice.value),stock=safeNum(ingStock.value);if(qty<=0)return toast('Purchase quantity must be greater than zero.',true);if(stock<0)return toast('Stock cannot be negative.',true);const minimum=safeNum(ingMin.value);if(minimum<0)return toast('Minimum stock cannot be negative.',true);const row={name:ingName.value.trim(),unit,current_qty:qty,purchase_qty:qty,stock_qty:stock,minimum_stock_qty:minimum,active:ingActive.value==='true',unit_cost:price/normalizeBase(unit,qty),notes:[ingCat.value,ingSupplier.value.trim()].filter(Boolean).join(' · ')};const q=editing?supabase.from('inventory_items').update(row).eq('id',item.id):supabase.from('inventory_items').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Ingredient updated':'Ingredient added');closeModal();await loadAll()}}

function openRecipeModal(recipe=null){const editing=!!recipe;let editingCompIndex=null;let selected=editing?state.components.filter(c=>c.recipe_id===recipe.id).map(c=>{const prep=state.preparations.find(p=>p.id===c.preparation_id),ing=state.inventory.find(i=>i.id===c.inventory_item_id),usage=componentUsage(recipe,c),kind=prep?'preparation':'ingredient';return{id:prep?.id||ing?.id||c.inventory_item_id||c.preparation_id,name:c.component_name,qty:usage?.qty||null,unit:usage?.unit||'g',cost:componentLineCost(recipe,c),legacy:!usage,kind,sourceFound:!!(prep||ing)}}):[];const units=['g','kg','ml','L','unit','tbsp','tsp','pinch'];const selectedIds=new Set(selected.map(x=>`${x.kind}:${x.id}`)),ingredientOptions=state.inventory.filter(i=>isActive(i)||selectedIds.has(`ingredient:${i.id}`)).map(i=>`<option value="ingredient:${i.id}">${esc(i.name)}${isActive(i)?'':' · inactive'}</option>`).join(''),preparationOptions=state.preparations.filter(p=>isActive(p)||selectedIds.has(`preparation:${p.id}`)).map(p=>`<option value="preparation:${p.id}">${esc(p.name)}${isActive(p)?'':' · inactive'}</option>`).join(''),aliases=(Array.isArray(recipe?.sales_aliases)?recipe.sales_aliases:[]).join('\n');const m=modal(`<div class="eyebrow">Menu costing & sales matching</div><h3>${editing?'EDIT':'NEW'} RECIPE</h3><form id="recipeForm"><div class="form-grid"><div class="field span-2"><label>Product name</label><input class="input" id="recipeName" value="${esc(recipe?.product_name||'')}" required></div><div class="field"><label>Status</label><select class="select" id="recipeActive"><option value="true" ${isActive(recipe)?'selected':''}>Active / selling now</option><option value="false" ${!isActive(recipe)?'selected':''}>Inactive / seasonal</option></select></div><div class="field"><label>Selling price</label><input class="input" type="number" step=".01" id="recipePrice" value="${editing?safeNum(recipe.selling_price):''}" required></div><div class="field"><label>Yield</label><input class="input" type="number" step=".01" id="recipeYield" value="${editing?safeNum(recipe.yield_qty):1}"></div><div class="field"><label>Finished stock on hand</label><input class="input" type="number" step=".001" id="recipeStock" value="${editing?safeNum(recipe.stock_qty):0}"></div><div class="field"><label>Weekly minimum / par</label><input class="input" type="number" step=".001" id="recipeMin" value="${editing?safeNum(recipe.minimum_stock_qty):0}"></div><div class="field"><label>Stock unit</label><select class="select" id="recipeStockUnit">${['unit','tray','kg','g','L','ml'].map(x=>`<option ${optionSelected(recipe?.stock_unit||'unit',x)}>${x}</option>`).join('')}</select></div><div class="field span-2"><label>Sales aliases — one per line</label><textarea class="textarea" id="recipeAliases" placeholder="Brisket sandwich\nSandwich de brisket">${esc(aliases)}</textarea></div><div class="field span-4"><label>Description</label><input class="input" id="recipeNotes" value="${esc(recipe?.notes||'')}"></div></div><div class="notice">A Fuego OS automatically ignores word order and small connector words when matching sales. “Sandwich de Brisket” and “Brisket Sandwich” match automatically. Use aliases for names such as “The Smokey One” that are genuinely different.</div><div class="panel" style="margin:12px 0"><div class="panel-title">Components</div><div id="componentList"></div><div class="component-row"><div class="field component-name"><label>Ingredient or preparation</label><select class="select" id="compSource"><option value="">Select component</option><optgroup label="ACTIVE INGREDIENTS">${ingredientOptions}</optgroup><optgroup label="ACTIVE PREPARATIONS">${preparationOptions}</optgroup></select></div><div class="field"><label>Qty</label><input class="input" type="number" step=".001" id="compQty"></div><div class="field"><label>Unit</label><select class="select" id="compUnit">${units.map(x=>`<option>${x}</option>`).join('')}</select></div><button type="button" class="btn btn-secondary" id="addComp">Add</button></div><button type="button" class="btn btn-ghost btn-sm" id="cancelCompEdit" style="display:none;margin-top:8px">Cancel component edit</button><div class="notice" style="margin-top:10px">Recipes can use raw ingredients or a preparation such as Salsa Criolla. Inactive seasonal items stay hidden from the selector unless this recipe already uses them.</div></div><div class="inline-actions"><button class="btn btn-ghost" type="button" data-close>Cancel</button><button class="btn btn-primary">${editing?'Save recipe changes':'Save recipe'}</button></div></form>`);const list=m.querySelector('#componentList'),addBtn=m.querySelector('#addComp'),cancelEdit=m.querySelector('#cancelCompEdit'),source=m.querySelector('#compSource');const resetComp=()=>{editingCompIndex=null;source.value='';compQty.value='';compUnit.value='g';addBtn.textContent='Add';cancelEdit.style.display='none'};const redraw=()=>{list.innerHTML=selected.length?selected.map((x,i)=>`<div class="list-row"><div class="grow"><strong>${esc(x.name)}</strong><small>${x.qty??'Legacy'} ${esc(x.unit||'')} · ${x.kind==='preparation'?'preparation':'ingredient'} · ${money(x.cost)}</small></div><button type="button" class="btn btn-secondary btn-sm" data-edit-comp="${i}">Edit</button><button type="button" class="btn btn-danger btn-sm" data-rm-comp="${i}">×</button></div>`).join(''):'<div class="empty">No components added yet.</div>';list.querySelectorAll('[data-edit-comp]').forEach(b=>b.onclick=()=>{editingCompIndex=Number(b.dataset.editComp);const x=selected[editingCompIndex];source.value=`${x.kind}:${x.id}`;compQty.value=x.qty??'';compUnit.value=x.unit||'g';addBtn.textContent='Update component';cancelEdit.style.display='inline-flex';compQty.focus()});list.querySelectorAll('[data-rm-comp]').forEach(b=>b.onclick=()=>{const idx=Number(b.dataset.rmComp);selected.splice(idx,1);if(editingCompIndex===idx)resetComp();redraw()})};redraw();m.querySelector('[data-close]').onclick=closeModal;cancelEdit.onclick=resetComp;addBtn.onclick=()=>{const [kind,id]=source.value.split(':'),qty=safeNum(compQty.value),unit=compUnit.value;if(!kind||!id||qty<=0)return toast('Choose a component and enter a quantity greater than zero.',true);const item=kind==='preparation'?state.preparations.find(p=>p.id===id):state.inventory.find(i=>i.id===id);if(!item)return toast('Component not found.',true);const row={id:item.id,name:item.name,qty,unit,cost:kind==='preparation'?preparationUsageCost(item,qty,unit):ingredientCost(item,qty,unit),legacy:false,kind,sourceFound:true};if(editingCompIndex===null)selected.push(row);else selected[editingCompIndex]=row;resetComp();redraw()};m.querySelector('#recipeForm').onsubmit=async e=>{e.preventDefault();const sales_aliases=recipeAliases.value.split(/\n|,/).map(x=>x.trim()).filter(Boolean),row={product_name:recipeName.value.trim(),selling_price:safeNum(recipePrice.value),yield_qty:safeNum(recipeYield.value)||1,stock_qty:safeNum(recipeStock.value),minimum_stock_qty:safeNum(recipeMin.value),stock_unit:recipeStockUnit.value,notes:recipeNotes.value.trim(),active:recipeActive.value==='true',sales_aliases};if(row.stock_qty<0||row.minimum_stock_qty<0)return toast('Stock and minimum must be zero or greater.',true);let r,error;if(editing){const res=await supabase.from('recipes').update(row).eq('id',recipe.id).select().single();r=res.data;error=res.error}else{const res=await supabase.from('recipes').insert({...row,user_id:state.user.id}).select().single();r=res.data;error=res.error}if(error)return toast(error.message,true);try{await replaceRecipeComponents(r.id,selected)}catch(err){return toast(`Recipe details saved, but components failed: ${err.message}`,true)}toast(editing?'Recipe updated':'Recipe added');closeModal();await loadAll()}}

function renderPreparations(el){if(!tableFlags.preparations)return schemaNotice(el,'Preparations');const rows=visibleByStatus(state.preparations);el.innerHTML=`<div class="kpi-grid"><div class="kpi"><div class="label">Preparation stock</div><div class="value ember">${money(state.preparations.reduce((s,p)=>s+preparationStockValue(p),0))}</div></div><div class="kpi"><div class="label">Active preparations</div><div class="value">${state.preparations.filter(isActive).length}</div></div><div class="kpi"><div class="label">Inactive</div><div class="value">${state.preparations.filter(x=>!isActive(x)).length}</div></div><div class="kpi"><div class="label">Total prep batches</div><div class="value good">${state.preparations.length}</div></div></div>${rows.length?`<div class="menu-cards">${rows.map(p=>{const cost=preparationBatchCost(p),comps=state.prepComponents.filter(c=>c.preparation_id===p.id);return`<article class="dish-card ${isActive(p)?'':'inactive-card'}"><div class="dish-body"><div class="dish-row"><div><div class="dish-title">${esc(p.name)}</div><div class="dish-desc">Yield ${p.yield_qty} ${esc(p.yield_unit)} · ${comps.length} ingredient${comps.length===1?'':'s'}</div></div><span class="tag ${isActive(p)?'tag-green':'tag-orange'}">${isActive(p)?'Active':'Inactive'}</span></div><div class="metrics-mini"><div class="metric-mini"><label>Batch cost</label><b>${money(cost)}</b></div><div class="metric-mini"><label>Stock</label><b>${safeNum(p.stock_qty)} ${esc(p.yield_unit)}</b></div><div class="metric-mini"><label>Stock value</label><b class="good">${money(preparationStockValue(p))}</b></div></div>${comps.length?`<div class="muted" style="margin-top:10px;font-size:10px">${comps.map(c=>`${esc(c.component_name)} ${c.qty} ${esc(c.unit)}`).join(' · ')}</div>`:'<div class="muted" style="margin-top:10px;font-size:10px">Legacy/manual cost. Edit to add ingredients and enable live costing.</div>'}<div class="inline-actions" style="margin-top:12px"><button class="btn btn-secondary btn-sm" data-edit-prep="${p.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-prep="${p.id}">Delete</button></div></div></article>`}).join('')}</div>`:'<div class="empty">No preparations in this status filter.</div>'}`;el.querySelectorAll('[data-edit-prep]').forEach(b=>b.onclick=()=>openPreparationModal(state.preparations.find(x=>x.id===b.dataset.editPrep)));el.querySelectorAll('[data-del-prep]').forEach(b=>b.onclick=()=>deleteRow('preparations',b.dataset.delPrep,'preparation'))}

function openPreparationModal(record=null){if(!tableFlags.preparations)return toast('Apply the included Supabase migration first.',true);const editing=!!record,units=['g','kg','ml','L','unit','tbsp','tsp','pinch'];let editingCompIndex=null;let selected=editing?state.prepComponents.filter(c=>c.preparation_id===record.id).map(c=>{const ing=state.inventory.find(i=>i.id===c.inventory_item_id);return{id:c.inventory_item_id,name:c.component_name,qty:safeNum(c.qty),unit:c.unit||'g',cost:ing?ingredientCost(ing,c.qty,c.unit):safeNum(c.line_cost),ingFound:!!ing}}):[];const selectedIds=new Set(selected.map(x=>x.id)),availableIngredients=state.inventory.filter(i=>isActive(i)||selectedIds.has(i.id));const m=modal(`<div class="eyebrow">Sauces and bases</div><h3>${editing?'EDIT':'NEW'} PREPARATION</h3><form id="prepForm"><div class="form-grid"><div class="field span-2"><label>Name</label><input class="input" id="prepName" value="${esc(record?.name||'')}" required></div><div class="field"><label>Status</label><select class="select" id="prepActive"><option value="true" ${isActive(record)?'selected':''}>Active / current season</option><option value="false" ${!isActive(record)?'selected':''}>Inactive / hidden</option></select></div><div class="field"><label>Stock on hand</label><input class="input" type="number" step=".001" id="prepStock" value="${editing?safeNum(record.stock_qty):0}"></div><div class="field"><label>Weekly minimum / par</label><input class="input" type="number" step=".001" id="prepMin" value="${editing?safeNum(record.minimum_stock_qty):0}"></div><div class="field"><label>Yield qty</label><input class="input" type="number" step=".01" id="prepYield" value="${editing?safeNum(record.yield_qty):''}" required></div><div class="field"><label>Yield unit</label><select class="select" id="prepUnit">${['ml','g','L','kg','unit'].map(x=>`<option ${optionSelected(record?.yield_unit||'ml',x)}>${x}</option>`).join('')}</select></div></div><div class="panel" style="margin:12px 0"><div class="panel-head"><div><div class="panel-title">Preparation ingredients</div><div class="panel-sub">Only active ingredients are shown, except ingredients already used by this preparation.</div></div><div class="tag" id="prepLiveCost">${money(editing?preparationBatchCost(record):0)}</div></div><div id="prepComponentList"></div><div class="component-row"><div class="field component-name"><label>Ingredient</label><select class="select" id="prepCompIng"><option value="">Select ingredient</option>${availableIngredients.map(i=>`<option value="${i.id}">${esc(i.name)}${isActive(i)?'':' · inactive'}</option>`).join('')}</select></div><div class="field"><label>Qty</label><input class="input" type="number" step=".001" id="prepCompQty"></div><div class="field"><label>Unit</label><select class="select" id="prepCompUnit">${units.map(x=>`<option>${x}</option>`).join('')}</select></div><button type="button" class="btn btn-secondary" id="addPrepComp">Add</button></div><button type="button" class="btn btn-ghost btn-sm" id="cancelPrepCompEdit" style="display:none;margin-top:8px">Cancel ingredient edit</button></div><div class="notice">Total batch cost is calculated automatically. Stock on hand uses the yield unit: for Salsa Criolla with yield in g, enter current stock in g.</div><div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Save preparation'}</button></div></form>`);const list=m.querySelector('#prepComponentList'),addBtn=m.querySelector('#addPrepComp'),cancelEdit=m.querySelector('#cancelPrepCompEdit'),costEl=m.querySelector('#prepLiveCost');const batchCost=()=>selected.length?selected.reduce((sum,x)=>sum+safeNum(x.cost),0):(editing?safeNum(record.total_cost):0);const resetComp=()=>{editingCompIndex=null;prepCompIng.value='';prepCompQty.value='';prepCompUnit.value='g';addBtn.textContent='Add';cancelEdit.style.display='none'};const redraw=()=>{costEl.textContent=money(batchCost());list.innerHTML=selected.length?selected.map((x,i)=>`<div class="list-row"><div class="grow"><strong>${esc(x.name)}</strong><small>${x.qty} ${esc(x.unit)} · live cost ${money(x.cost)}</small></div><button type="button" class="btn btn-secondary btn-sm" data-edit-prep-comp="${i}">Edit</button><button type="button" class="btn btn-danger btn-sm" data-rm-prep-comp="${i}">×</button></div>`).join(''):`<div class="empty">No ingredients added yet.${editing&&safeNum(record.total_cost)?` Current legacy cost is ${money(record.total_cost)}.`:''}</div>`;list.querySelectorAll('[data-edit-prep-comp]').forEach(b=>b.onclick=()=>{editingCompIndex=Number(b.dataset.editPrepComp);const x=selected[editingCompIndex];prepCompIng.value=x.id||'';prepCompQty.value=x.qty;prepCompUnit.value=x.unit;addBtn.textContent='Update ingredient';cancelEdit.style.display='inline-flex';prepCompQty.focus()});list.querySelectorAll('[data-rm-prep-comp]').forEach(b=>b.onclick=()=>{const idx=Number(b.dataset.rmPrepComp);selected.splice(idx,1);if(editingCompIndex===idx)resetComp();redraw()})};redraw();m.querySelector('[data-close]').onclick=closeModal;cancelEdit.onclick=resetComp;addBtn.onclick=()=>{const ing=state.inventory.find(i=>i.id===prepCompIng.value),qty=safeNum(prepCompQty.value),unit=prepCompUnit.value;if(!ing||qty<=0)return toast('Choose an ingredient and enter a quantity greater than zero.',true);const row={id:ing.id,name:ing.name,qty,unit,cost:ingredientCost(ing,qty,unit),ingFound:true};if(editingCompIndex===null)selected.push(row);else selected[editingCompIndex]=row;resetComp();redraw()};prepForm.onsubmit=async e=>{e.preventDefault();const totalCost=batchCost(),stock=safeNum(prepStock.value);if(stock<0)return toast('Stock cannot be negative.',true);const minimum=safeNum(prepMin.value);if(minimum<0)return toast('Minimum stock cannot be negative.',true);const row={name:prepName.value.trim(),yield_qty:safeNum(prepYield.value),yield_unit:prepUnit.value,total_cost:totalCost,stock_qty:stock,minimum_stock_qty:minimum,active:prepActive.value==='true'};let prep,error;if(editing){const res=await supabase.from('preparations').update(row).eq('id',record.id).select().single();prep=res.data;error=res.error}else{const res=await supabase.from('preparations').insert({...row,user_id:state.user.id}).select().single();prep=res.data;error=res.error}if(error)return toast(error.message,true);try{await replacePreparationComponents(prep.id,selected)}catch(err){return toast(`Preparation saved, but ingredients failed: ${err.message}`,true)}if(selected.length){const liveCost=selected.reduce((sum,x)=>sum+safeNum(x.cost),0);await supabase.from('preparations').update({total_cost:liveCost}).eq('id',prep.id)}toast(editing?'Preparation updated':'Preparation added');closeModal();await loadAll()}}

function renderSupplies(el){if(!tableFlags.supplies)return schemaNotice(el,'Supplies');const rows=visibleByStatus(state.supplies);el.innerHTML=`<div class="kpi-grid"><div class="kpi"><div class="label">Supply stock value</div><div class="value ember">${money(state.supplies.reduce((s,x)=>s+supplyStockValue(x),0))}</div></div><div class="kpi"><div class="label">Active supplies</div><div class="value">${state.supplies.filter(isActive).length}</div></div><div class="kpi"><div class="label">Inactive</div><div class="value">${state.supplies.filter(x=>!isActive(x)).length}</div></div><div class="kpi"><div class="label">Stock units</div><div class="value good">${state.supplies.reduce((s,x)=>s+safeNum(x.stock_qty),0).toFixed(0)}</div></div></div>${rows.length?`<section class="panel"><div class="table-wrap"><table class="table"><thead><tr><th>Supply</th><th>Status</th><th>Category</th><th>Pack</th><th>Cost</th><th>Stock</th><th>Stock value</th><th></th></tr></thead><tbody>${rows.map(s=>`<tr><td><strong>${esc(s.name)}</strong><div class="muted" style="font-size:9px">${esc(s.supplier||'')}</div></td><td><span class="tag ${isActive(s)?'tag-green':'tag-orange'}">${isActive(s)?'Active':'Inactive'}</span></td><td>${esc(s.category)}</td><td>${s.pack_size} ${esc(s.unit)}</td><td>${money(s.purchase_price)}</td><td><strong>${safeNum(s.stock_qty)} ${esc(s.unit)}</strong></td><td class="money">${money(supplyStockValue(s))}</td><td><div class="row-actions"><button class="btn btn-secondary btn-sm" data-edit-sup="${s.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-sup="${s.id}">Delete</button></div></td></tr>`).join('')}</tbody></table></div></section>`:'<div class="empty">No supplies in this status filter.</div>'}`;el.querySelectorAll('[data-edit-sup]').forEach(b=>b.onclick=()=>openSupplyModal(state.supplies.find(x=>x.id===b.dataset.editSup)));el.querySelectorAll('[data-del-sup]').forEach(b=>b.onclick=()=>deleteRow('supplies',b.dataset.delSup,'supply'))}

function openSupplyModal(record=null){if(!tableFlags.supplies)return toast('Apply the included Supabase migration first.',true);const editing=!!record,units=['unit','pack','roll','box','bag','bottle','can','tray'];const m=modal(`<div class="eyebrow">Packaging and disposables</div><h3>${editing?'EDIT':'ADD'} SUPPLY</h3><form id="supForm"><div class="form-grid"><div class="field span-2"><label>Name</label><input class="input" id="supName" value="${esc(record?.name||'')}" required></div><div class="field"><label>Status</label><select class="select" id="supActive"><option value="true" ${isActive(record)?'selected':''}>Active / current season</option><option value="false" ${!isActive(record)?'selected':''}>Inactive / hidden</option></select></div><div class="field"><label>Category</label><input class="input" id="supCat" value="${esc(record?.category||'Packaging')}"></div><div class="field span-2"><label>Supplier</label><input class="input" id="supSupplier" value="${esc(record?.supplier||'')}"></div><div class="field"><label>Purchase price</label><input class="input" type="number" step=".01" id="supPrice" value="${editing?safeNum(record.purchase_price):''}"></div><div class="field"><label>Pack size</label><input class="input" type="number" step=".001" id="supQty" value="${editing?safeNum(record.pack_size):100}"></div><div class="field"><label>Unit</label><select class="select" id="supUnit">${units.map(x=>`<option ${optionSelected(record?.unit||'unit',x)}>${x}</option>`).join('')}</select></div><div class="field"><label>Stock on hand</label><input class="input" type="number" step=".001" id="supStock" value="${editing?safeNum(record.stock_qty):0}"></div><div class="field"><label>Weekly minimum / par</label><input class="input" type="number" step=".001" id="supMin" value="${editing?safeNum(record.minimum_stock_qty):0}"></div></div><div class="notice">Example: a box costs $28 and contains 500 napkins. If you have roughly 750 napkins left, stock is 750 unit and the capital value is calculated automatically.</div><div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Save supply'}</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;supForm.onsubmit=async e=>{e.preventDefault();const stock=safeNum(supStock.value);if(stock<0)return toast('Stock cannot be negative.',true);const minimum=safeNum(supMin.value);if(minimum<0)return toast('Minimum stock cannot be negative.',true);const row={name:supName.value.trim(),category:supCat.value.trim(),supplier:supSupplier.value.trim(),purchase_price:safeNum(supPrice.value),pack_size:safeNum(supQty.value),unit:supUnit.value,stock_qty:stock,minimum_stock_qty:minimum,active:supActive.value==='true'};const q=editing?supabase.from('supplies').update(row).eq('id',record.id):supabase.from('supplies').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Supply updated':'Supply added');closeModal();await loadAll()}}


function purchasePlanningRows(){
  const rows=[];
  state.inventory.filter(isActive).forEach(i=>{
    const category=(i.notes||'Other').split(' · ')[0]||'Other',supplier=(i.notes||'').split(' · ').slice(1).join(' · '),stock=safeNum(i.stock_qty),minimum=safeNum(i.minimum_stock_qty),need=Math.max(0,minimum-stock),lot=Math.max(.0001,ingredientPurchaseQty(i)||1),lots=need>0?Math.ceil((need-1e-9)/lot):0;
    rows.push({type:category==='Beverages'?'Beverage':'Ingredient',category,name:i.name,supplier,unit:i.unit||'unit',stock,minimum,need,suggested:lots*lot,estimated:lots*purchasePrice(i),packLabel:`${lot} ${i.unit||'unit'}`})
  });
  state.supplies.filter(isActive).forEach(i=>{
    const stock=safeNum(i.stock_qty),minimum=safeNum(i.minimum_stock_qty),need=Math.max(0,minimum-stock),lot=Math.max(.0001,safeNum(i.pack_size)||1),lots=need>0?Math.ceil((need-1e-9)/lot):0;
    rows.push({type:(i.category||'Packaging').toLowerCase().includes('pack')?'Packaging':'Supply',category:i.category||'Packaging',name:i.name,supplier:i.supplier||'',unit:i.unit||'unit',stock,minimum,need,suggested:lots*lot,estimated:lots*safeNum(i.purchase_price),packLabel:`${lot} ${i.unit||'unit'}`})
  });
  return rows.sort((a,b)=>(b.need>0)-(a.need>0)||a.category.localeCompare(b.category)||a.name.localeCompare(b.name))
}
function printWeeklyBuyList(){
  const rows=purchasePlanningRows().filter(r=>r.need>0),win=window.open('','_blank','noopener,noreferrer');if(!win)return toast('Allow pop-ups to print the weekly buy list.',true);
  const est=rows.reduce((s,r)=>s+r.estimated,0),groups={};rows.forEach(r=>(groups[r.category]??=[]).push(r));
  const body=Object.entries(groups).map(([cat,items])=>`<tr class="section"><td colspan="8">${esc(cat)}</td></tr>${items.map(r=>`<tr><td>${esc(r.name)}</td><td>${esc(r.supplier||'')}</td><td>${r.stock} ${esc(r.unit)}</td><td>${r.minimum} ${esc(r.unit)}</td><td>${r.need.toFixed(3).replace(/\.000$/,'')} ${esc(r.unit)}</td><td><strong>${r.suggested.toFixed(3).replace(/\.000$/,'')} ${esc(r.unit)}</strong></td><td>${money(r.estimated)}</td><td class="write"></td></tr>`).join('')}`).join('');
  win.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>Weekly Purchase List</title><style>@page{size:A4 portrait;margin:9mm}*{box-sizing:border-box}body{font-family:Arial,Helvetica,sans-serif;color:#343434}.head{border-bottom:4px solid #b12524;padding-bottom:7px;margin-bottom:8px;display:flex;justify-content:space-between}.brand{font-size:24px;font-weight:900}.brand span{color:#b12524}h1{font-size:18px;margin:0}.meta{text-align:right;font-size:9px}.notice{background:#FCF9F6;border-left:4px solid #ED891C;padding:7px;font-size:9px;margin-bottom:8px}table{border-collapse:collapse;width:100%;font-size:8.5px}th{background:#343434;color:#fff;text-align:left;padding:5px;border:1px solid #343434}td{border:1px solid #aaa;padding:5px;height:24px}.section td{background:#f0e8e1;color:#b12524;font-weight:800;text-transform:uppercase}.write{min-width:22mm;background:#fffef5}.foot{margin-top:8px;font-size:8px;display:flex;justify-content:space-between}@media print{button{display:none}}</style></head><body><div class="head"><div><div class="brand">A <span>FUEGO</span></div><h1>WEEKLY PURCHASE LIST</h1></div><div class="meta">Printed ${dateAU(today())}<br>Estimated restock: <strong>${money(est)}</strong><br>Purchased by: __________________</div></div><div class="notice">Suggested order rounds shortages up to the configured purchase pack / purchase quantity. Cross out anything you do not intend to buy this week.</div><table><thead><tr><th>Item</th><th>Supplier</th><th>On hand</th><th>Par</th><th>Short</th><th>Suggested order</th><th>Est. cost</th><th>Actual bought</th></tr></thead><tbody>${body||'<tr><td colspan="8">Nothing is currently below minimum.</td></tr>'}</tbody></table><div class="foot"><span>Ingredients + beverages + packaging + consumable supplies</span><span>Total estimated: ${money(est)}</span></div><button onclick="window.print()">Print</button><script>setTimeout(()=>window.print(),350)<\/script></body></html>`);win.document.close()
}
function renderStockOverview(el){
  const all=[];visibleByStatus(state.inventory).forEach(i=>all.push({type:(i.notes||'Other').split(' · ')[0]==='Beverages'?'Beverage':'Ingredient',category:(i.notes||'Other').split(' · ')[0]||'Other',name:i.name,status:isActive(i),qty:safeNum(i.stock_qty),unit:i.unit,value:ingredientStockValue(i),minimum:safeNum(i.minimum_stock_qty)}));visibleByStatus(state.preparations).forEach(p=>all.push({type:'Preparation',category:'Preparations',name:p.name,status:isActive(p),qty:safeNum(p.stock_qty),unit:p.yield_unit,value:preparationStockValue(p),minimum:safeNum(p.minimum_stock_qty)}));visibleByStatus(state.recipes).forEach(r=>all.push({type:'Finished product',category:'Finished products',name:r.product_name,status:isActive(r),qty:safeNum(r.stock_qty),unit:r.stock_unit||'unit',value:recipeCost(r)*safeNum(r.stock_qty),minimum:safeNum(r.minimum_stock_qty)}));visibleByStatus(state.supplies).forEach(x=>all.push({type:(x.category||'Packaging').toLowerCase().includes('pack')?'Packaging':'Supply',category:x.category||'Packaging',name:x.name,status:isActive(x),qty:safeNum(x.stock_qty),unit:x.unit,value:supplyStockValue(x),minimum:safeNum(x.minimum_stock_qty)}));all.sort((a,b)=>a.category.localeCompare(b.category)||a.name.localeCompare(b.name));
  const c=stockCapital(),buy=purchasePlanningRows(),short=buy.filter(x=>x.need>0),packShort=short.filter(x=>x.type==='Packaging').length,est=short.reduce((s,x)=>s+x.estimated,0);
  el.innerHTML=`<div class="kpi-grid"><div class="kpi"><div class="label">Total capital in stock</div><div class="value ember">${money(c.total)}</div></div><div class="kpi"><div class="label">Items to buy</div><div class="value ${short.length?'warn':'good'}">${short.length}</div><div class="meta">Ingredients + beverages + packaging + supplies below par</div></div><div class="kpi"><div class="label">Packaging shortages</div><div class="value ${packShort?'warn':'good'}">${packShort}</div></div><div class="kpi"><div class="label">Estimated restock</div><div class="value">${money(est)}</div><div class="meta">Rounded to configured purchase packs</div></div></div>
  <section class="panel"><div class="panel-head"><div><div class="panel-title">General stock register</div><div class="panel-sub">One list for food, beverages, preparations, finished stock, packaging and operating consumables.</div></div><div class="inline-actions"><button class="btn btn-secondary btn-sm" data-go="operations">Weekly physical count</button><button class="btn btn-primary btn-sm" id="printBuyList">🖨 Weekly buy list</button></div></div>${all.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Category</th><th>Type</th><th>Item</th><th>Status</th><th>On hand</th><th>Minimum</th><th>Need</th><th>Capital value</th></tr></thead><tbody>${all.map(x=>{const need=Math.max(0,x.minimum-x.qty);return`<tr><td>${esc(x.category)}</td><td><span class="tag">${esc(x.type)}</span></td><td><strong>${esc(x.name)}</strong></td><td><span class="tag ${x.status?'tag-green':'tag-orange'}">${x.status?'Active':'Inactive'}</span></td><td>${x.qty} ${esc(x.unit)}</td><td>${x.minimum} ${esc(x.unit)}</td><td class="${need>0?'warn':'good'}">${need.toFixed(3).replace(/\.000$/,'')} ${esc(x.unit)}</td><td class="money">${money(x.value)}</td></tr>`}).join('')}</tbody></table></div>`:'<div class="empty">No stock items in this status filter.</div>'}</section>
  <section class="panel" style="margin-top:15px"><div class="panel-head"><div><div class="panel-title">Weekly purchase requirement</div><div class="panel-sub">Only purchased items appear here. Production items belong in Weekly ops → Production.</div></div><span class="tag ${short.length?'tag-orange':'tag-green'}">${short.length} to buy</span></div>${short.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Category</th><th>Item</th><th>Supplier</th><th>On hand</th><th>Par</th><th>Short</th><th>Suggested order</th><th>Est. cost</th></tr></thead><tbody>${short.map(r=>`<tr><td><span class="tag">${esc(r.category)}</span></td><td><strong>${esc(r.name)}</strong><small class="muted">${esc(r.type)}</small></td><td>${esc(r.supplier||'—')}</td><td>${r.stock} ${esc(r.unit)}</td><td>${r.minimum} ${esc(r.unit)}</td><td class="warn">${r.need.toFixed(3).replace(/\.000$/,'')} ${esc(r.unit)}</td><td><strong>${r.suggested.toFixed(3).replace(/\.000$/,'')} ${esc(r.unit)}</strong><small class="muted">purchase lot ${esc(r.packLabel)}</small></td><td class="money">${money(r.estimated)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">Everything is currently at or above the configured weekly minimum.</div>'}</section>`;
  el.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{state.route=b.dataset.go;renderShell()});document.getElementById('printBuyList').onclick=printWeeklyBuyList
}
function schemaNotice(el,name){el.innerHTML=`<div class="notice"><strong>${name} is designed and included in A Fuego OS.</strong><br>The current Fuego Books database does not have this table yet. Apply the latest included <code>RUN_THIS_ONE_AFUEGO_ERP_*.sql</code> migration once and this module becomes cloud-backed automatically.</div>`}



// v3.3 — target-scaled production planning and printable production pack
function convertProductionQty(qty,fromUnit,toUnit){
  const a=(fromUnit||'').trim(),b=(toUnit||'').trim();if(a===b)return safeNum(qty);
  const mass={g:1,kg:1000},vol={ml:1,L:1000};if(mass[a]&&mass[b])return safeNum(qty)*mass[a]/mass[b];if(vol[a]&&vol[b])return safeNum(qty)*vol[a]/vol[b];return null
}
function scaledRecipeLines(recipe,targetQty){
  const factor=(safeNum(targetQty)||0)/(safeNum(recipe?.yield_qty)||1);
  return state.components.filter(c=>c.recipe_id===recipe.id).map(c=>{
    const usage=componentUsage(recipe,c),prep=state.preparations.find(p=>p.id===c.preparation_id),ing=state.inventory.find(i=>i.id===c.inventory_item_id),kind=prep?'preparation':'ingredient',source=prep||ing,qty=(safeNum(usage?.qty)||0)*factor,unit=usage?.unit||'',stockRaw=kind==='preparation'?safeNum(prep?.stock_qty):safeNum(ing?.stock_qty),stockUnit=kind==='preparation'?(prep?.yield_unit||unit):(ing?.unit||unit),available=convertProductionQty(stockRaw,stockUnit,unit),shortage=available===null?null:Math.max(0,qty-available);
    return{kind,id:source?.id||null,name:c.component_name||source?.name||'Component',qty,unit,stockRaw,stockUnit,available,shortage,cost:kind==='preparation'?preparationUsageCost(prep,qty,unit):ingredientCost(ing,qty,unit)}
  })
}
function productionBatchCode(recipe,dateValue=today()){
  const key=(recipe?.product_name||'BATCH').replace(/[^A-Za-z0-9]/g,'').slice(0,4).toUpperCase()||'BATCH',stamp=new Date().toTimeString().slice(0,5).replace(':','');return `${key}-${String(dateValue).replaceAll('-','')}-${stamp}`
}
function openProductionCalculator(recipe){
  if(!recipe)return;const base=safeNum(recipe.yield_qty)||1,defaultTarget=Math.max(base,opsNeed({minimum:safeNum(recipe.minimum_stock_qty),stock:safeNum(recipe.stock_qty)})||base);const m=modal(`<div class="eyebrow">Production planning</div><h3>START PRODUCTION · ${esc(recipe.product_name)}</h3><div class="form-grid"><div class="field"><label>Production date</label><input class="input" id="prodDate" type="date" value="${today()}"></div><div class="field"><label>Target output</label><input class="input" id="prodTarget" type="number" step=".001" min=".001" value="${defaultTarget}"></div><div class="field"><label>Output unit</label><input class="input" value="${esc(recipe.stock_unit||'unit')}" disabled></div><div class="field"><label>Base recipe yield</label><input class="input" value="${base} ${esc(recipe.stock_unit||'unit')}" disabled></div><div class="field span-4"><label>Batch notes</label><input class="input" id="prodNotes" placeholder="Event prep, Tuesday production, etc."></div></div><div id="prodScalePanel"></div><div class="inline-actions"><button class="btn btn-ghost" type="button" data-close>Cancel</button><button class="btn btn-secondary" id="prodPrintOnly" type="button">🖨 Print production pack</button><button class="btn btn-primary" id="prodCreate" type="button">Start batch & print</button></div>`);
  m.querySelector('[data-close]').onclick=closeModal;
  const redraw=()=>{const target=safeNum(prodTarget.value),factor=target/base,lines=scaledRecipeLines(recipe,target),shorts=lines.filter(x=>x.shortage!==null&&x.shortage>1e-9);m.querySelector('#prodScalePanel').innerHTML=`<div class="kpi-grid" style="margin-top:14px"><div class="kpi"><div class="label">Scaling factor</div><div class="value">${factor.toFixed(2)}×</div></div><div class="kpi"><div class="label">Target</div><div class="value ember">${target} ${esc(recipe.stock_unit||'unit')}</div></div><div class="kpi"><div class="label">Ingredient shortages</div><div class="value ${shorts.length?'bad':'good'}">${shorts.length}</div></div><div class="kpi"><div class="label">Theoretical batch cost</div><div class="value">${money(lines.reduce((s,x)=>s+x.cost,0))}</div></div></div><section class="panel"><div class="panel-title">Scaled recipe for this batch</div><div class="table-wrap"><table class="table"><thead><tr><th>Ingredient / preparation</th><th>Required</th><th>Available</th><th>Short</th><th>Type</th></tr></thead><tbody>${lines.map(x=>`<tr><td><strong>${esc(x.name)}</strong></td><td>${x.qty.toFixed(3).replace(/\.000$/,'')} ${esc(x.unit)}</td><td>${x.available===null?`${x.stockRaw} ${esc(x.stockUnit)} · different unit`:x.available.toFixed(3).replace(/\.000$/,'')+' '+esc(x.unit)}</td><td class="${x.shortage>0?'bad':'good'}">${x.shortage===null?'check units':x.shortage.toFixed(3).replace(/\.000$/,'')+' '+esc(x.unit)}</td><td><span class="tag">${x.kind==='preparation'?'Preparation':'Ingredient'}</span></td></tr>`).join('')}</tbody></table></div></section>`};
  prodTarget.oninput=redraw;redraw();
  prodPrintOnly.onclick=()=>printProductionPack(recipe,safeNum(prodTarget.value),prodDate.value||today(),prodNotes.value.trim(),null);
  prodCreate.onclick=async()=>{const target=safeNum(prodTarget.value);if(target<=0)return toast('Target must be greater than zero.',true);if(!tableFlags.production_runs||!tableFlags.production_run_lines)return toast('Run the v3.3 SQL migration first.',true);const code=productionBatchCode(recipe,prodDate.value||today()),lines=scaledRecipeLines(recipe,target),snapshot={recipe_id:recipe.id,product_name:recipe.product_name,yield_qty:recipe.yield_qty,stock_unit:recipe.stock_unit,components:lines.map(x=>({name:x.name,kind:x.kind,qty:x.qty,unit:x.unit}))};const ins=await supabase.from('production_runs').insert({user_id:state.user.id,batch_code:code,batch_date:prodDate.value||today(),recipe_id:recipe.id,recipe_name:recipe.product_name,target_qty:target,target_unit:recipe.stock_unit||'unit',base_yield_qty:base,status:'planned',notes:prodNotes.value.trim(),recipe_snapshot:snapshot}).select().single();if(ins.error)return toast(ins.error.message,true);const rows=lines.map(x=>({user_id:state.user.id,run_id:ins.data.id,item_type:x.kind,inventory_item_id:x.kind==='ingredient'?x.id:null,preparation_id:x.kind==='preparation'?x.id:null,item_name:x.name,unit:x.unit,theoretical_qty:x.qty,stock_before:x.available}));if(rows.length){const lr=await supabase.from('production_run_lines').insert(rows);if(lr.error)return toast(`Batch created, but lines failed: ${lr.error.message}`,true)}toast(`Batch ${code} created`);printProductionPack(recipe,target,prodDate.value||today(),prodNotes.value.trim(),code);closeModal();await loadAll()}
}
function printProductionPack(recipe,target,dateValue,notes,batchCode){
  const lines=scaledRecipeLines(recipe,target),base=safeNum(recipe.yield_qty)||1,factor=target/base,win=window.open('','_blank','noopener,noreferrer');if(!win)return toast('Allow pop-ups to print the production pack.',true);const code=batchCode||productionBatchCode(recipe,dateValue),rows=lines.map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.unit)}</td><td><strong>${x.qty.toFixed(3).replace(/\.000$/,'')}</strong></td><td class="write"></td><td class="write"></td><td class="write"></td><td class="notes"></td></tr>`).join('');
  win.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>${esc(recipe.product_name)} Production Pack</title><style>@page{size:A4 portrait;margin:9mm}*{box-sizing:border-box}body{font-family:Arial,Helvetica,sans-serif;color:#343434}.header{border-bottom:4px solid #b12524;padding-bottom:7px;margin-bottom:8px;display:flex;justify-content:space-between}.brand{font-size:24px;font-weight:900}.brand span{color:#b12524}h1{font-size:18px;margin:0}.meta{text-align:right;font-size:9px}.summary{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin:8px 0}.box{border:1px solid #999;padding:6px}.box small{display:block;color:#666;text-transform:uppercase;font-size:7px}.box b{font-size:13px}.notice{background:#FCF9F6;border-left:4px solid #ED891C;padding:7px;font-size:9px;margin:8px 0}table{width:100%;border-collapse:collapse;font-size:8.5px}th{background:#343434;color:white;text-align:left;padding:5px;border:1px solid #343434}td{border:1px solid #aaa;padding:5px;height:28px}.write{background:#fffef5;min-width:18mm}.notes{min-width:25mm}.result{margin-top:10px;border:2px solid #343434;padding:8px}.result-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.line{height:25px;border-bottom:1px solid #777}.foot{font-size:8px;margin-top:8px}@media print{button{display:none}.page-break{page-break-before:always}}</style></head><body><div class="header"><div><div class="brand">A <span>FUEGO</span></div><h1>${esc(recipe.product_name)} · PRODUCTION PACK</h1></div><div class="meta">Date: ${dateAU(dateValue)}<br>Batch: <strong>${esc(code)}</strong><br>Operator: __________________</div></div><div class="summary"><div class="box"><small>Base recipe yield</small><b>${base} ${esc(recipe.stock_unit||'unit')}</b></div><div class="box"><small>Target today</small><b>${target} ${esc(recipe.stock_unit||'unit')}</b></div><div class="box"><small>Scaling factor</small><b>${factor.toFixed(2)}×</b></div><div class="box"><small>Finished stock before</small><b>${safeNum(recipe.stock_qty)} ${esc(recipe.stock_unit||'unit')}</b></div></div><div class="notice">Theoretical quantities are scaled automatically for today's target. Write what you actually remove, trim/waste and use. The ERP should only post real quantities after review.${notes?` Notes: ${esc(notes)}`:''}</div><table><thead><tr><th>Ingredient / preparation</th><th>Unit</th><th>Recipe theoretical</th><th>Qty taken</th><th>Trim / waste</th><th>Actual used</th><th>Notes</th></tr></thead><tbody>${rows}</tbody></table><div class="result"><h3 style="margin:0 0 7px">BATCH RESULT</h3><div class="result-grid"><div><small>Target</small><div class="line">${target} ${esc(recipe.stock_unit||'unit')}</div></div><div><small>Good output</small><div class="line"></div></div><div><small>Rejected / waste</small><div class="line"></div></div><div><small>Final weight</small><div class="line"></div></div><div><small>Average unit weight</small><div class="line"></div></div><div><small>Finished stock after</small><div class="line"></div></div></div><p style="font-size:8px">Quality / food safety / corrective action:</p><div class="line"></div></div><div class="foot">Recipe target generated from the live ERP recipe. Keep this sheet with the batch until the actual quantities are posted.</div><button onclick="window.print()">Print</button><script>setTimeout(()=>window.print(),350)<\/script></body></html>`);win.document.close()
}

// v3.2 — analogue ↔ ERP weekly stocktake and production bridge
function mondayOfWeek(value=new Date()){
  const d=value instanceof Date?new Date(value):new Date(`${value}T12:00:00`);const day=d.getDay(),diff=day===0?-6:1-day;d.setDate(d.getDate()+diff);return d.toISOString().slice(0,10)
}
function opsStockRows(){
  const rows=[];
  state.inventory.filter(isActive).forEach(i=>rows.push({type:'ingredient',id:i.id,name:i.name,category:(i.notes||'Other').split(' · ')[0]||'Other',unit:i.unit||'unit',stock:safeNum(i.stock_qty),minimum:safeNum(i.minimum_stock_qty)}));
  state.supplies.filter(isActive).forEach(i=>rows.push({type:'supply',id:i.id,name:i.name,category:i.category||'Supplies',unit:i.unit||'unit',stock:safeNum(i.stock_qty),minimum:safeNum(i.minimum_stock_qty)}));
  return rows.sort((a,b)=>a.category.localeCompare(b.category)||a.name.localeCompare(b.name));
}
function opsProductionRows(){
  const rows=[];
  state.preparations.filter(isActive).forEach(i=>rows.push({type:'preparation',id:i.id,name:i.name,category:'Preparations',unit:i.yield_unit||'g',stock:safeNum(i.stock_qty),minimum:safeNum(i.minimum_stock_qty),unitCost:preparationUsageCost(i,1,i.yield_unit||'g')}));
  state.recipes.filter(isActive).forEach(i=>rows.push({type:'recipe',id:i.id,name:i.product_name,category:'Finished menu stock',unit:i.stock_unit||'unit',stock:safeNum(i.stock_qty),minimum:safeNum(i.minimum_stock_qty),unitCost:recipeCost(i)}));
  return rows.sort((a,b)=>a.category.localeCompare(b.category)||a.name.localeCompare(b.name));
}
function opsNeed(row,qty=row.stock){return Math.max(0,safeNum(row.minimum)-safeNum(qty))}
function opsStatusTag(status){const map={draft:['Draft',''],scanning:['AI reading','tag-orange'],review:['Needs review','tag-orange'],posted:['Posted','tag-green'],error:['Scan error','tag-red']},x=map[status]||[status,''];return `<span class="tag ${x[1]}">${esc(x[0])}</span>`}
function renderOperations(el){
  if(!tableFlags.ops_sheet_sessions||!tableFlags.ops_sheet_lines)return schemaNotice(el,'Weekly ops');
  const tabs=[['stocktake','Stock count'],['production','Weekly plan'],['batches','Daily batches'],['history','History']];
  el.innerHTML=`<div class="toolbar"><div class="tabs">${tabs.map(t=>`<button class="tab ${state.opsTab===t[0]?'active':''}" data-ops-tab="${t[0]}">${t[1]}</button>`).join('')}</div></div><div id="opsPanel"></div>`;
  el.querySelectorAll('[data-ops-tab]').forEach(b=>b.onclick=()=>{state.opsTab=b.dataset.opsTab;renderOperations(el)});renderOperationsTab(document.getElementById('opsPanel'))
}
function renderOperationsTab(el){if(state.opsTab==='production')return renderProductionPlan(el);if(state.opsTab==='batches')return renderProductionBatches(el);if(state.opsTab==='history')return renderOpsHistory(el);renderWeeklyStocktake(el)}

function renderProductionBatches(el){
  if(!tableFlags.production_runs)return schemaNotice(el,'Daily production batches');
  const runs=state.productionRuns.slice().sort((a,b)=>String(b.batch_date||'').localeCompare(String(a.batch_date||''))||String(b.created_at||'').localeCompare(String(a.created_at||''))),todayRuns=runs.filter(r=>r.batch_date===today()),todayTarget=todayRuns.reduce((s,r)=>s+safeNum(r.target_qty),0);
  el.innerHTML=`<section class="hero-grid"><div class="hero-panel"><div class="eyebrow">Daily production register</div><h2>PLAN THE BATCH.<br><em>Then record reality.</em></h2><p class="section-intro">Starting a batch from a recipe freezes today's target and theoretical ingredient requirement. No stock is consumed until actual production is reviewed and posted.</p></div><div class="hero-aside"><div class="signal-card"><div class="label">Batches today</div><div class="value">${todayRuns.length}</div></div><div class="signal-card"><div class="label">Today's target units</div><div class="value ember">${todayTarget.toFixed(3).replace(/\.000$/,'')}</div><div class="hint">Across all planned recipe batches.</div></div></div></section>
  <section class="panel"><div class="panel-head"><div><div class="panel-title">Production batches</div><div class="panel-sub">Use Food → Menu & recipes → Start production to create a scaled batch and print its production pack.</div></div></div>${runs.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Batch</th><th>Product</th><th>Target</th><th>Status</th><th>Theoretical inputs</th><th>Notes</th></tr></thead><tbody>${runs.map(r=>{const lines=state.productionRunLines.filter(x=>x.run_id===r.id);return`<tr><td>${dateAU(r.batch_date)}</td><td><strong>${esc(r.batch_code)}</strong></td><td>${esc(r.recipe_name)}</td><td>${safeNum(r.target_qty)} ${esc(r.target_unit||'unit')}</td><td><span class="tag ${r.status==='completed'?'tag-green':r.status==='cancelled'?'tag-orange':''}">${esc(r.status||'planned')}</span></td><td>${lines.length} line${lines.length===1?'':'s'}</td><td>${esc(r.notes||'')}</td></tr>`}).join('')}</tbody></table></div>`:'<div class="empty">No production batches yet. Start one from a menu recipe.</div>'}</section>`
}
function renderWeeklyStocktake(el){
  const rows=opsStockRows(),below=rows.filter(r=>r.stock<r.minimum).length,last=state.opsSessions.filter(s=>s.sheet_type==='stocktake'&&s.status==='posted').sort((a,b)=>String(b.sheet_date).localeCompare(String(a.sheet_date)))[0];
  el.innerHTML=`<section class="hero-grid"><div class="hero-panel"><div class="eyebrow">Weekly physical stock bridge</div><h2>COUNT IT.<br><em>Photograph it.</em></h2><p class="section-intro">Print the sheet, count the real truck with pen, photograph the completed page and let AI prepare the reconciliation. Nothing changes until you approve it.</p></div><div class="hero-aside"><div class="signal-card"><div class="label">Active count lines</div><div class="value">${rows.length}</div><div class="hint">Ingredients, beverages, packaging and supplies.</div></div><div class="signal-card"><div class="label">Below minimum now</div><div class="value ${below?'warn':'good'}">${below}</div><div class="hint">Based on ERP stock before the physical count.</div></div></div></section>
  <div class="toolbar ops-actions"><div class="grow"><div class="panel-title">Week commencing ${dateAU(mondayOfWeek())}</div><div class="panel-sub">${last?`Last posted physical count: ${dateAU(last.sheet_date)}`:'No weekly count posted yet.'}</div></div><button class="btn btn-secondary" id="printStockSheet">🖨 Print A4 stock sheet</button><button class="btn btn-secondary" id="printStockBuyList">🛒 Print buy list</button><button class="btn btn-primary" id="uploadStockSheet">📷 Upload completed photo</button><button class="btn btn-ghost" id="saveStockMinimums">Save minimums</button></div>
  <section class="panel"><div class="panel-head"><div><div class="panel-title">Active weekly count list</div><div class="panel-sub">Edit minimum/par levels here. The printed page includes ERP stock but leaves the physical count blank for handwriting.</div></div></div><div class="table-wrap"><table class="table ops-table"><thead><tr><th>Category</th><th>Item</th><th>ERP stock</th><th>Weekly minimum</th><th>Need to minimum</th></tr></thead><tbody>${rows.map(r=>`<tr><td><span class="tag">${esc(r.category)}</span></td><td><strong>${esc(r.name)}</strong><small class="muted">${esc(r.type)}</small></td><td>${r.stock} ${esc(r.unit)}</td><td><input class="input ops-min-input" type="number" step=".001" min="0" value="${r.minimum}" data-min-type="${r.type}" data-min-id="${r.id}"></td><td class="${opsNeed(r)>0?'warn':'good'}">${opsNeed(r).toFixed(3).replace(/\.000$/,'')} ${esc(r.unit)}</td></tr>`).join('')}</tbody></table></div></section>`;
  printStockSheet.onclick=()=>printWeeklySheet('stocktake');printStockBuyList.onclick=printWeeklyBuyList;uploadStockSheet.onclick=()=>openOpsUpload('stocktake');saveStockMinimums.onclick=()=>saveOpsMinimums(el,'stocktake')
}
function renderProductionPlan(el){
  const rows=opsProductionRows(),need=rows.filter(r=>opsNeed(r)>0).length,last=state.opsSessions.filter(s=>s.sheet_type==='production'&&s.status==='posted').sort((a,b)=>String(b.sheet_date).localeCompare(String(a.sheet_date)))[0];
  el.innerHTML=`<section class="hero-grid"><div class="hero-panel"><div class="eyebrow">Weekly production plan</div><h2>BUILD THE BUFFER.</h2><p class="section-intro">Your minimum/par is manual. ERP shows the starting stock and calculates what should be produced to reach that buffer. Write the real start and actual production on paper, then photograph it.</p></div><div class="hero-aside"><div class="signal-card"><div class="label">Active production lines</div><div class="value">${rows.length}</div></div><div class="signal-card"><div class="label">Need production now</div><div class="value ${need?'warn':'good'}">${need}</div><div class="hint">Preparations + finished menu stock below par.</div></div></div></section>
  <div class="toolbar ops-actions"><div class="grow"><div class="panel-title">Week commencing ${dateAU(mondayOfWeek())}</div><div class="panel-sub">${last?`Last posted production sheet: ${dateAU(last.sheet_date)}`:'No production sheet posted yet.'}</div></div><button class="btn btn-secondary" id="printProductionSheet">🖨 Print A4 production sheet</button><button class="btn btn-primary" id="uploadProductionSheet">📷 Upload completed photo</button><button class="btn btn-ghost" id="saveProductionMinimums">Save minimums</button></div>
  <section class="panel"><div class="panel-head"><div><div class="panel-title">Production par levels</div><div class="panel-sub">Minimums are deliberately manual so you can change the buffer as demand and season change.</div></div></div><div class="table-wrap"><table class="table ops-table"><thead><tr><th>Type</th><th>Item</th><th>ERP stock</th><th>Weekly minimum</th><th>Need to produce</th></tr></thead><tbody>${rows.map(r=>`<tr><td><span class="tag">${esc(r.category)}</span></td><td><strong>${esc(r.name)}</strong></td><td>${r.stock} ${esc(r.unit)}</td><td><input class="input ops-min-input" type="number" step=".001" min="0" value="${r.minimum}" data-min-type="${r.type}" data-min-id="${r.id}"></td><td class="${opsNeed(r)>0?'warn':'good'}"><strong>${opsNeed(r).toFixed(3).replace(/\.000$/,'')} ${esc(r.unit)}</strong></td></tr>`).join('')}</tbody></table></div></section>`;
  printProductionSheet.onclick=()=>printWeeklySheet('production');uploadProductionSheet.onclick=()=>openOpsUpload('production');saveProductionMinimums.onclick=()=>saveOpsMinimums(el,'production')
}
async function saveOpsMinimums(el,kind){
  const inputs=[...el.querySelectorAll('[data-min-type]')],updates=inputs.map(async input=>{const value=Math.max(0,safeNum(input.value)),type=input.dataset.minType,id=input.dataset.minId,table=type==='ingredient'?'inventory_items':type==='supply'?'supplies':type==='preparation'?'preparations':'recipes';const {error}=await supabase.from(table).update({minimum_stock_qty:value}).eq('id',id);if(error)throw error});
  try{await Promise.all(updates);toast('Weekly minimums saved');await loadAll();state.route='operations';state.opsTab=kind;renderShell()}catch(err){toast(err.message||String(err),true)}
}
function printWeeklySheet(kind){
  const win=window.open('','_blank','noopener,noreferrer');if(!win)return toast('Allow pop-ups to print the weekly sheet.',true);
  const week=mondayOfWeek(),business=esc(state.settings?.business_name||'A Fuego'),brand='#b12524',orange='#ED891C',dark='#343434',cream='#FCF9F6';
  const stockRows=opsStockRows(),prodRows=opsProductionRows();
  const grouped=rows=>{const groups={};rows.forEach(r=>(groups[r.category]??=[]).push(r));return groups};
  let body='';
  if(kind==='stocktake'){
    body=Object.entries(grouped(stockRows)).map(([cat,rows])=>`<tr class="section"><td colspan="7">${esc(cat)}</td></tr>${rows.map(r=>`<tr><td>${esc(r.name)}</td><td>${esc(r.unit)}</td><td>${r.stock}</td><td class="write"></td><td>${r.minimum}</td><td class="write"></td><td class="notes"></td></tr>`).join('')}`).join('');
  }else{
    body=Object.entries(grouped(prodRows)).map(([cat,rows])=>`<tr class="section"><td colspan="9">${esc(cat)}</td></tr>${rows.map(r=>`<tr><td>${esc(r.name)}</td><td>${esc(r.unit)}</td><td>${r.stock}</td><td class="write"></td><td>${r.minimum}</td><td>${opsNeed(r).toFixed(3).replace(/\.000$/,'')}</td><td class="write"></td><td class="write"></td><td class="notes"></td></tr>`).join('')}`).join('');
  }
  const title=kind==='stocktake'?'WEEKLY STOCK COUNT SHEET':'WEEKLY PRODUCTION SHEET';
  const instructions=kind==='stocktake'?'Write the real PHYSICAL COUNT in the blank box. Do not overwrite ERP stock. After counting, photograph the full page straight-on and upload it in ERP → Weekly ops.':'Write PHYSICAL START and ACTUAL PRODUCED. ERP stock, MINIMUM and NEED are printed references. After production, photograph the full page straight-on and upload it in ERP → Weekly ops.';
  const head=kind==='stocktake'?'<tr><th>Item</th><th>Unit</th><th>ERP stock</th><th>Physical count</th><th>Minimum</th><th>Need / buy</th><th>Notes</th></tr>':'<tr><th>Item</th><th>Unit</th><th>ERP stock</th><th>Physical start</th><th>Minimum</th><th>Need</th><th>Produced</th><th>Closing</th><th>Notes</th></tr>';
  win.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>${title}</title><style>@page{size:A4 portrait;margin:8mm}*{box-sizing:border-box}body{font-family:Arial,Helvetica,sans-serif;color:${dark};margin:0;background:white}.header{display:flex;justify-content:space-between;align-items:flex-end;border-bottom:4px solid ${brand};padding-bottom:7px;margin-bottom:7px}.brand{font-size:25px;font-weight:900;letter-spacing:1px}.brand span{color:${brand}}h1{font-size:18px;margin:0}.meta{font-size:9px;text-align:right}.instruction{font-size:8.5px;line-height:1.35;background:${cream};border-left:4px solid ${orange};padding:6px 8px;margin:7px 0 9px}table{width:100%;border-collapse:collapse;font-size:8.2px}thead{display:table-header-group}th{background:${dark};color:white;padding:5px 3px;text-align:left;border:1px solid ${dark}}td{padding:4px 3px;border:1px solid #aaa;height:24px;vertical-align:middle}.section td{background:#f0e8e1;color:${brand};font-weight:800;text-transform:uppercase;letter-spacing:.6px;height:19px}.write{background:#fffef5;min-width:15mm}.notes{min-width:25mm}.footer{font-size:8px;margin-top:7px;display:flex;justify-content:space-between}.no-print{margin:10px 0}@media print{.no-print{display:none}}</style></head><body><div class="header"><div><div class="brand">A <span>FUEGO</span></div><h1>${title}</h1></div><div class="meta"><b>${business}</b><br>Week commencing: ${dateAU(week)}<br>Counted by: __________________</div></div><div class="instruction">${instructions}</div><table><thead>${head}</thead><tbody>${body}</tbody></table><div class="footer"><span>Paper is the fast interface. The photo becomes the ERP record after human approval.</span><span>Sheet date: ${dateAU(week)}</span></div><div class="no-print"><button onclick="window.print()">Print</button></div><script>setTimeout(()=>window.print(),350)<\/script></body></html>`);win.document.close()
}
async function imageFileToDataUrl(file,maxDim=1800){
  const raw=await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=()=>reject(r.error);r.readAsDataURL(file)});
  if(!file.type.startsWith('image/'))return raw;
  try{const img=await new Promise((resolve,reject)=>{const i=new Image();i.onload=()=>resolve(i);i.onerror=reject;i.src=raw});const scale=Math.min(1,maxDim/Math.max(img.width,img.height));if(scale===1&&file.size<2200000)return raw;const c=document.createElement('canvas');c.width=Math.max(1,Math.round(img.width*scale));c.height=Math.max(1,Math.round(img.height*scale));c.getContext('2d').drawImage(img,0,0,c.width,c.height);return c.toDataURL('image/jpeg',.86)}catch(_){return raw}
}
function openOpsUpload(kind){
  const label=kind==='stocktake'?'WEEKLY STOCK COUNT':'WEEKLY PRODUCTION';const m=modal(`<div class="eyebrow">Analogue → ERP bridge</div><h3>UPLOAD ${label}</h3><p class="section-intro">Use one clear photo of the completed printed sheet. AI will read the handwriting and prepare a review. <strong>Stock will not change until you approve.</strong></p><form id="opsUploadForm"><div class="form-grid"><div class="field"><label>Sheet date / week</label><input class="input" type="date" id="opsSheetDate" value="${mondayOfWeek()}"></div><div class="field span-2"><label>Photo</label><input class="input" type="file" id="opsSheetFile" accept="image/jpeg,image/png,image/webp,image/*" required><small class="muted">Best result: whole page visible, straight-on, bright light, dark pen.</small></div><div class="field span-4"><label>Notes</label><input class="input" id="opsSheetNotes" placeholder="Optional: Saturday count, event prep, etc."></div></div><div class="notice">AI is deliberately not allowed to post stock by itself. You will see every matched item and quantity before approval.</div><div class="inline-actions"><button class="btn btn-ghost" type="button" data-close>Cancel</button><button class="btn btn-primary" id="opsScanBtn">Upload & read with AI</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;
  m.querySelector('#opsUploadForm').onsubmit=async e=>{e.preventDefault();const file=opsSheetFile.files?.[0];if(!file)return toast('Choose a photo first.',true);const btn=m.querySelector('#opsScanBtn');btn.disabled=true;btn.textContent='Reading handwriting…';let session=null;try{const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,'_'),path=`${state.user.id}/${Date.now()}-${kind}-${safe}`;const upload=await supabase.storage.from('ops-sheets').upload(path,file,{upsert:false});if(upload.error)throw upload.error;const ins=await supabase.from('ops_sheet_sessions').insert({user_id:state.user.id,sheet_type:kind,sheet_date:opsSheetDate.value||today(),status:'scanning',file_path:path,file_name:file.name,notes:opsSheetNotes.value.trim()}).select().single();if(ins.error)throw ins.error;session=ins.data;const fileData=await imageFileToDataUrl(file);const scan=await supabase.functions.invoke('ops-sheet-scan',{body:{session_id:session.id,sheet_type:kind,file_name:file.name,file_data_url:fileData}});if(scan.error)throw scan.error;if(scan.data?.ok===false)throw new Error(scan.data.error||'AI could not read the sheet');closeModal();await loadAll();const fresh=state.opsSessions.find(x=>x.id===session.id);if(fresh)openOpsReview(fresh)}catch(err){if(session?.id)await supabase.from('ops_sheet_sessions').update({status:'error',parse_error:err.message||String(err)}).eq('id',session.id);toast(`Weekly sheet scan failed: ${err.message||err}`,true);btn.disabled=false;btn.textContent='Upload & read with AI'} }
}
function opsTargetOptions(kind,selectedType,selectedId){
  const groups=kind==='stocktake'?[['Ingredients',state.inventory.filter(isActive).map(x=>({type:'ingredient',id:x.id,name:x.name}))],['Supplies',state.supplies.filter(isActive).map(x=>({type:'supply',id:x.id,name:x.name}))]]:[['Preparations',state.preparations.filter(isActive).map(x=>({type:'preparation',id:x.id,name:x.name}))],['Finished menu stock',state.recipes.filter(isActive).map(x=>({type:'recipe',id:x.id,name:x.product_name}))]];
  return groups.map(([label,rows])=>`<optgroup label="${label}">${rows.map(x=>`<option value="${x.type}:${x.id}" ${x.type===selectedType&&x.id===selectedId?'selected':''}>${esc(x.name)}</option>`).join('')}</optgroup>`).join('')
}
function openOpsReview(session){
  const kind=session.sheet_type,lines=state.opsLines.filter(x=>x.session_id===session.id);if(!lines.length)return toast(session.parse_error||'No readable lines were found for this sheet.',true);const posted=session.status==='posted';
  const rows=lines.map((x,i)=>{const targetId=x.item_type==='ingredient'?x.inventory_item_id:x.item_type==='supply'?x.supply_id:x.item_type==='preparation'?x.preparation_id:x.recipe_id;const confidence=Math.round(safeNum(x.ai_confidence)*100);if(posted)return `<tr><td>${esc(x.item_name)}</td><td>${esc(x.unit)}</td><td>${x.physical_qty??'—'}</td>${kind==='production'?`<td>${x.minimum_qty??0}</td><td>${x.produced_qty??0}</td>`:''}<td>${esc(x.notes||'')}</td></tr>`;return `<tr data-ops-row data-line-id="${x.id}"><td><select class="select ops-target">${opsTargetOptions(kind,x.item_type,targetId)}</select><small class="muted">AI ${confidence}%</small></td><td>${esc(x.unit)}</td><td><input class="input ops-physical" type="number" step=".001" min="0" value="${x.physical_qty??''}" placeholder="blank = no opening adjustment"></td>${kind==='production'?`<td>${safeNum(x.minimum_qty)}</td><td><input class="input ops-produced" type="number" step=".001" min="0" value="${safeNum(x.produced_qty)}"></td>`:''}<td><input class="input ops-note" value="${esc(x.notes||'')}"></td></tr>`}).join('');
  const m=modal(`<div class="eyebrow">Human approval required</div><h3>${kind==='stocktake'?'STOCK COUNT':'PRODUCTION'} · ${dateAU(session.sheet_date)}</h3><p class="section-intro">${posted?'This sheet has already been posted.':'Check every AI-read quantity against the photograph before posting. You can correct the product match and the number here.'}</p><div class="table-wrap"><table class="table"><thead><tr><th>Matched ERP item</th><th>Unit</th><th>${kind==='stocktake'?'Physical count':'Physical start'}</th>${kind==='production'?'<th>Minimum</th><th>Produced</th>':''}<th>Notes</th></tr></thead><tbody>${rows}</tbody></table></div>${posted?'':`<div class="notice">Approval is the only action that changes stock. ${kind==='production'?'If Physical start is filled, ERP reconciles opening stock first, then adds Produced.':'ERP records the difference as a stocktake adjustment in the stock movement ledger.'}</div><div class="inline-actions"><button class="btn btn-ghost" data-close>Cancel</button><button class="btn btn-primary" id="approveOpsSheet">Approve & update ERP</button></div>`}<div class="inline-actions" ${posted?'':'style="display:none"'}><button class="btn btn-ghost" data-close>Close</button></div>`);m.querySelectorAll('[data-close]').forEach(b=>b.onclick=closeModal);if(posted)return;
  m.querySelector('#approveOpsSheet').onclick=async()=>{const payload=[...m.querySelectorAll('[data-ops-row]')].map(row=>{const [item_type,target_id]=row.querySelector('.ops-target').value.split(':');return{line_id:row.dataset.lineId,item_type,target_id,physical_qty:nullableNum(row.querySelector('.ops-physical').value),produced_qty:kind==='production'?safeNum(row.querySelector('.ops-produced').value):null,notes:row.querySelector('.ops-note').value.trim()}}).filter(x=>kind==='stocktake'?x.physical_qty!==null:(x.physical_qty!==null||x.produced_qty>0));if(!payload.length)return toast('There are no quantities to post.',true);if(!confirm(`Post ${payload.length} reviewed line${payload.length===1?'':'s'} to ERP stock?`))return;const fn=kind==='stocktake'?'post_weekly_stocktake':'post_weekly_production';const {data,error}=await supabase.rpc(fn,{p_session_id:session.id,p_lines:payload});if(error)return toast(error.message,true);toast(`${kind==='stocktake'?'Stock count':'Production'} posted · ${data?.posted_lines??payload.length} lines`);closeModal();await loadAll();state.route='operations';state.opsTab='history';renderShell()}
}
function renderOpsHistory(el){
  const rows=state.opsSessions.slice().sort((a,b)=>String(b.created_at).localeCompare(String(a.created_at)));el.innerHTML=`<section class="panel"><div class="panel-head"><div><div class="panel-title">Weekly analogue history</div><div class="panel-sub">Every photographed sheet remains a traceable session. “Review” means it has not changed stock yet.</div></div></div>${rows.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Sheet</th><th>Status</th><th>File</th><th>Error / notes</th><th></th></tr></thead><tbody>${rows.map(x=>`<tr><td>${dateAU(x.sheet_date)}</td><td><span class="tag">${x.sheet_type==='stocktake'?'Stock count':'Production'}</span></td><td>${opsStatusTag(x.status)}</td><td>${esc(x.file_name||'—')}</td><td>${esc(x.parse_error||x.notes||'')}</td><td><div class="row-actions">${['review','posted'].includes(x.status)?`<button class="btn btn-secondary btn-sm" data-view-ops="${x.id}">${x.status==='review'?'Review':'View'}</button>`:''}</div></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No photographed weekly sheets yet.</div>'}</section>`;el.querySelectorAll('[data-view-ops]').forEach(b=>b.onclick=()=>openOpsReview(state.opsSessions.find(x=>x.id===b.dataset.viewOps)))
}

function renderFinance(el){const tabs=[['purchases','Purchase inbox'],['expenses','Expenses'],['invoices','Invoice archive'],['costs','Operating costs'],['capital','Capital']];const addLabel=state.financeTab==='purchases'?'Upload invoice / receipt':'+ Add';el.innerHTML=`<div class="toolbar"><div class="tabs">${tabs.map(t=>`<button class="tab ${state.financeTab===t[0]?'active':''}" data-fin-tab="${t[0]}">${t[1]}</button>`).join('')}</div><button class="btn btn-primary" id="finAddBtn">${addLabel}</button></div><div id="finPanel"></div>`;el.querySelectorAll('[data-fin-tab]').forEach(b=>b.onclick=()=>{state.financeTab=b.dataset.finTab;renderFinance(el)});finAddBtn.onclick=()=>({purchases:openPurchaseUpload,expenses:openExpenseModal,invoices:openInvoiceModal,costs:openCostModal,capital:openCapitalModal}[state.financeTab]||openPurchaseUpload)();renderFinanceTab(document.getElementById('finPanel'))}
function renderFinanceTab(el){if(state.financeTab==='purchases')return renderPurchases(el);if(state.financeTab==='invoices')return renderInvoices(el);if(state.financeTab==='costs')return renderCosts(el);if(state.financeTab==='capital')return renderCapital(el);renderExpenses(el)}

function invoiceStatus(i){return i?.status||'manual'}
function invoiceStatusTag(status){const map={scanning:['AI reading','tag-orange'],review:['Review','tag-orange'],posted:['Posted','tag-green'],error:['Scan error','tag-red'],manual:['Manual',''],draft:['Draft','']};const row=map[status]||[status,''];return `<span class="tag ${row[1]}">${esc(row[0])}</span>`}
function purchaseInvoices(){return state.invoices.filter(i=>['scanning','review','posted','error'].includes(invoiceStatus(i))).sort((a,b)=>String(b.created_at||'').localeCompare(String(a.created_at||'')))}
function renderPurchases(el){if(!tableFlags.invoice_lines||!tableFlags.purchase_item_mappings||!tableFlags.stock_movements)return schemaNotice(el,'Purchase inbox v2.5');const rows=purchaseInvoices(),review=rows.filter(i=>invoiceStatus(i)==='review').length,posted=rows.filter(i=>invoiceStatus(i)==='posted').length,errors=rows.filter(i=>invoiceStatus(i)==='error').length;el.innerHTML=`<section class="purchase-drop" id="purchaseDrop"><div class="eyebrow">AI purchase intake</div><h2>DROP THE INVOICE.<br><em>Review the stock.</em></h2><p>Upload a supplier invoice or receipt. A Fuego AI reads the header and every line, proposes a stock match, then waits for your approval before changing inventory.</p><button class="btn btn-primary" id="purchaseUploadBtn">Upload invoice / receipt</button><div class="muted" style="margin-top:10px">Images and text-based PDFs · nothing is posted to stock without your review</div></section><div class="kpi-grid" style="margin-top:15px"><div class="kpi"><div class="label">Waiting for review</div><div class="value ${review?'warn':''}">${review}</div></div><div class="kpi"><div class="label">Purchases posted</div><div class="value good">${posted}</div></div><div class="kpi"><div class="label">Scan problems</div><div class="value ${errors?'bad':''}">${errors}</div></div><div class="kpi"><div class="label">Remembered matches</div><div class="value ember">${state.purchaseMappings.length}</div></div></div><section class="panel"><div class="panel-head"><div><div class="panel-title">Purchase inbox</div><div class="panel-sub">Bread, brisket and Coke can increase stock. A one-off event purchase can stay expense-only and never enter the permanent stock list.</div></div></div>${rows.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Supplier / file</th><th>Status</th><th>Lines</th><th>Total</th><th></th></tr></thead><tbody>${rows.map(i=>{const lines=state.invoiceLines.filter(x=>x.invoice_id===i.id);return `<tr><td>${dateAU(i.invoice_date)}</td><td><strong>${esc(i.supplier||'AI scanning…')}</strong><div class="muted" style="font-size:9px">${esc(i.file_name||i.description||'')}</div>${i.parse_error?`<div class="bad" style="font-size:9px">${esc(i.parse_error)}</div>`:''}</td><td>${invoiceStatusTag(invoiceStatus(i))}</td><td>${lines.length}</td><td class="money">${money(i.total)}</td><td><div class="row-actions">${invoiceStatus(i)==='review'?`<button class="btn btn-primary btn-sm" data-review-invoice="${i.id}">Review & post</button>`:''}${invoiceStatus(i)==='error'?`<button class="btn btn-secondary btn-sm" data-rescan-invoice="${i.id}">Try scan again</button>`:''}${invoiceStatus(i)==='posted'?`<button class="btn btn-secondary btn-sm" data-view-invoice="${i.id}">View</button>`:''}</div></td></tr>`}).join('')}</tbody></table></div>`:'<div class="empty">No purchase invoices yet. Upload the next Harris Farm, butcher, bread or drinks invoice here.</div>'}</section>`;purchaseUploadBtn.onclick=openPurchaseUpload;purchaseDrop.ondragover=e=>{e.preventDefault();purchaseDrop.classList.add('dragging')};purchaseDrop.ondragleave=()=>purchaseDrop.classList.remove('dragging');purchaseDrop.ondrop=e=>{e.preventDefault();purchaseDrop.classList.remove('dragging');const file=e.dataTransfer.files?.[0];if(file)processPurchaseFile(file)};el.querySelectorAll('[data-review-invoice]').forEach(b=>b.onclick=()=>openInvoiceReview(b.dataset.reviewInvoice));el.querySelectorAll('[data-view-invoice]').forEach(b=>b.onclick=()=>openInvoiceReview(b.dataset.viewInvoice,true));el.querySelectorAll('[data-rescan-invoice]').forEach(b=>b.onclick=()=>toast('Upload the invoice again so AI receives the image/PDF content.',true))}
function openPurchaseUpload(){if(!tableFlags.invoice_lines)return toast('Apply the v2.5 SQL migration first.',true);const input=document.createElement('input');input.type='file';input.accept='image/*,application/pdf';input.onchange=()=>{const file=input.files?.[0];if(file)processPurchaseFile(file)};input.click()}
async function imageDataUrl(file){try{const bitmap=await createImageBitmap(file),max=1800,scale=Math.min(1,max/Math.max(bitmap.width,bitmap.height)),canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(bitmap.width*scale));canvas.height=Math.max(1,Math.round(bitmap.height*scale));canvas.getContext('2d').drawImage(bitmap,0,0,canvas.width,canvas.height);const blob=await new Promise(r=>canvas.toBlob(r,'image/jpeg',.82));return await new Promise((resolve,reject)=>{const fr=new FileReader();fr.onload=()=>resolve(fr.result);fr.onerror=reject;fr.readAsDataURL(blob||file)})}catch{return await new Promise((resolve,reject)=>{const fr=new FileReader();fr.onload=()=>resolve(fr.result);fr.onerror=reject;fr.readAsDataURL(file)})}}
async function pdfText(file){const pdfjs=await import('https://cdn.jsdelivr.net/npm/pdfjs-dist@4.10.38/build/pdf.min.mjs');pdfjs.GlobalWorkerOptions.workerSrc='https://cdn.jsdelivr.net/npm/pdfjs-dist@4.10.38/build/pdf.worker.min.mjs';const pdf=await pdfjs.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise;let text='';for(let n=1;n<=Math.min(pdf.numPages,20);n++){const page=await pdf.getPage(n),content=await page.getTextContent();text+=`\n--- PAGE ${n} ---\n`+content.items.map(x=>x.str||'').join(' ')}return text.trim().slice(0,80000)}
async function processPurchaseFile(file){if(!file)return;const supported=file.type.startsWith('image/')||file.type==='application/pdf';if(!supported)return toast('Use an image or PDF invoice.',true);if(file.size>18*1024*1024)return toast('Invoice file is too large. Keep it under 18 MB.',true);toast('Uploading invoice…');const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,'_'),path=`${state.user.id}/purchases/${Date.now()}-${safe}`;const up=await supabase.storage.from('receipts').upload(path,file);if(up.error)return toast(`Upload failed: ${up.error.message}`,true);const {data:invoice,error:invErr}=await supabase.from('invoices').insert({user_id:state.user.id,invoice_date:today(),supplier:'AI scanning…',description:file.name,total:0,gst:0,file_path:path,file_name:file.name,status:'scanning',source:'ai_upload'}).select().single();if(invErr)return toast(invErr.message,true);try{toast('A Fuego AI is reading every line…');let body={invoice_id:invoice.id,file_name:file.name,mime_type:file.type};if(file.type==='application/pdf'){const text=await pdfText(file);if(!text||text.length<20)throw new Error('This PDF appears to be scanned/image-only. Upload a photo or screenshot of the invoice.');body.document_text=text}else body.file_data_url=await imageDataUrl(file);const {data,error}=await supabase.functions.invoke('invoice-scan',{body});if(error)throw error;if(!data?.ok)throw new Error(data?.error||'AI scan did not complete');await loadAll();const refreshed=state.invoices.find(x=>x.id===invoice.id);if(invoiceStatus(refreshed)==='review'){toast('Invoice read. Review the stock matches.');openInvoiceReview(invoice.id)}else toast(refreshed?.parse_error||'Invoice scan needs attention.',true)}catch(err){await supabase.from('invoices').update({status:'error',parse_error:String(err?.message||err)}).eq('id',invoice.id);await loadAll();toast(String(err?.message||err),true)}}
function purchaseTargetOptions(line){const currentType=line.target_type||line.suggested_target_type||'expense_only',ing=state.inventory.map(i=>`<option value="ingredient:${i.id}" ${currentType==='ingredient'&&line.inventory_item_id===i.id?'selected':''}>Ingredient · ${esc(i.name)}${isActive(i)?'':' · inactive'}</option>`).join(''),sup=state.supplies.map(x=>`<option value="supply:${x.id}" ${currentType==='supply'&&line.supply_id===x.id?'selected':''}>Supply · ${esc(x.name)}${isActive(x)?'':' · inactive'}</option>`).join('');return `<option value="expense_only:" ${currentType==='expense_only'?'selected':''}>Expense only · do not add to stock</option><optgroup label="INGREDIENT STOCK">${ing}</optgroup><optgroup label="SUPPLY STOCK">${sup}</optgroup>`}
function lineTargetUnit(line){const item=state.inventory.find(i=>i.id===line.inventory_item_id)||state.supplies.find(s=>s.id===line.supply_id);return item?.unit||line.stock_unit||'unit'}
function openInvoiceReview(invoiceId,readOnly=false){const invoice=state.invoices.find(i=>i.id===invoiceId);if(!invoice)return;const lines=state.invoiceLines.filter(x=>x.invoice_id===invoiceId);const posted=invoiceStatus(invoice)==='posted';readOnly=readOnly||posted;const events=`<option value="">No specific event</option>${state.locations.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}`;const m=modal(`<div class="eyebrow">AI invoice review</div><h3>${posted?'POSTED PURCHASE':'CHECK BEFORE STOCK MOVES'}</h3><form id="invoiceReviewForm"><div class="form-grid"><div class="field"><label>Date</label><input class="input" type="date" id="reviewDate" value="${esc(invoice.invoice_date||today())}" ${readOnly?'disabled':''}></div><div class="field span-2"><label>Supplier</label><input class="input" id="reviewSupplier" value="${esc(invoice.supplier||'')}" ${readOnly?'disabled':''}></div><div class="field"><label>Total</label><input class="input" type="number" step=".01" id="reviewTotal" value="${safeNum(invoice.total)}" ${readOnly?'disabled':''}></div><div class="field"><label>GST</label><input class="input" type="number" step=".01" id="reviewGst" value="${safeNum(invoice.gst)}" ${readOnly?'disabled':''}></div><div class="field"><label>Payment</label><select class="select" id="reviewPayment" ${readOnly?'disabled':''}><option value="card">Card</option><option value="cash">Cash</option><option value="bank_transfer">Bank transfer</option><option value="other">Other</option></select></div></div><div class="notice">AI proposes the match. You decide what enters permanent stock. For a special event purchase, choose <strong>Expense only</strong>; it stays on the invoice and expense history but does not create or increase a stock item. Tick “Remember this match” for recurring supplier descriptions.</div><div class="invoice-review-list">${lines.map((line,idx)=>{const unit=lineTargetUnit(line),target=line.target_type||line.suggested_target_type||'expense_only',confidence=Math.round(safeNum(line.match_confidence)*100);return `<article class="invoice-line-card" data-line-card="${line.id}"><div class="invoice-line-head"><div><strong>${esc(line.raw_description)}</strong><div class="muted">Invoice qty ${safeNum(line.qty)} ${esc(line.unit||'')} · ${money(line.line_total)}${confidence?` · AI match ${confidence}%`:''}</div></div><span class="tag">Line ${idx+1}</span></div><div class="form-grid"><div class="field span-2"><label>Where does this go?</label><select class="select line-target" data-line-id="${line.id}" ${readOnly?'disabled':''}>${purchaseTargetOptions(line)}</select></div><div class="field"><label>Add to stock</label><input class="input line-stock-qty" data-line-id="${line.id}" type="number" step=".001" value="${target==='expense_only'?0:safeNum(line.stock_qty||line.qty)}" ${readOnly?'disabled':''}></div><div class="field"><label>Stock unit</label><input class="input line-stock-unit" data-line-id="${line.id}" value="${esc(unit)}" readonly></div><div class="field span-2"><label>Event / one-off allocation</label><select class="select line-event" data-line-id="${line.id}" ${readOnly?'disabled':''}>${events}</select></div><div class="field span-2"><label class="check-label"><input type="checkbox" class="line-remember" data-line-id="${line.id}" ${line.remember_mapping?'checked':''} ${readOnly?'disabled':''}> Remember this supplier description next time</label></div></div></article>`}).join('')}</div><div class="inline-actions"><button class="btn btn-ghost" type="button" data-close>${readOnly?'Close':'Cancel'}</button>${readOnly?'':`<button class="btn btn-primary" type="submit">Approve invoice & update stock</button>`}</div></form>`);m.querySelector('[data-close]').onclick=closeModal;for(const line of lines){const eventEl=m.querySelector(`.line-event[data-line-id="${line.id}"]`);if(eventEl)eventEl.value=line.location_event_id||''}m.querySelectorAll('.line-target').forEach(sel=>sel.onchange=()=>{const [type,id]=sel.value.split(':'),card=sel.closest('.invoice-line-card'),qty=card.querySelector('.line-stock-qty'),unit=card.querySelector('.line-stock-unit');if(type==='expense_only'){qty.value=0;qty.disabled=true;unit.value='—'}else{qty.disabled=false;const target=type==='ingredient'?state.inventory.find(i=>i.id===id):state.supplies.find(s=>s.id===id);unit.value=target?.unit||'unit';if(safeNum(qty.value)<=0)qty.value=1}});if(readOnly)return;invoiceReviewForm.onsubmit=async e=>{e.preventDefault();const payload=[];for(const line of lines){const card=m.querySelector(`[data-line-card="${line.id}"]`),sel=card.querySelector('.line-target'),[target_type,target_id]=sel.value.split(':'),stock_qty=safeNum(card.querySelector('.line-stock-qty').value),stock_unit=card.querySelector('.line-stock-unit').value,event_id=card.querySelector('.line-event').value||null,remember_mapping=card.querySelector('.line-remember').checked;if(target_type!=='expense_only'&&stock_qty<=0)return toast(`Enter stock quantity for ${line.raw_description}.`,true);payload.push({line_id:line.id,raw_description:line.raw_description,normalized_description:normalizeProductName(line.raw_description),target_type,inventory_item_id:target_type==='ingredient'?target_id:null,supply_id:target_type==='supply'?target_id:null,stock_qty,target_unit:stock_unit,event_id,line_total:safeNum(line.line_total),remember_mapping})}const submit=m.querySelector('button[type="submit"]');submit.disabled=true;submit.textContent='Posting purchase…';const {data,error}=await supabase.rpc('post_purchase_invoice',{p_invoice_id:invoice.id,p_invoice_date:reviewDate.value,p_supplier:reviewSupplier.value.trim(),p_total:safeNum(reviewTotal.value),p_gst:safeNum(reviewGst.value),p_payment_method:reviewPayment.value,p_category:'Purchases & stock',p_lines:payload});if(error){submit.disabled=false;submit.textContent='Approve invoice & update stock';return toast(error.message,true)}toast(`Purchase posted · ${data?.stock_lines??0} stock lines updated`);closeModal();await loadAll()}}
function renderExpenses(el){const total=state.expenses.reduce((s,e)=>s+safeNum(e.amount),0),gst=state.expenses.reduce((s,e)=>s+splitGST(e.amount,e.gst_claimable).gst,0);el.innerHTML=`<div class="kpi-grid"><div class="kpi"><div class="label">Logged expenses</div><div class="value">${money(total)}</div></div><div class="kpi"><div class="label">GST credits est.</div><div class="value good">${money(gst)}</div></div><div class="kpi"><div class="label">Missing receipts</div><div class="value ${state.expenses.some(e=>!e.receipt_url)?'warn':''}">${state.expenses.filter(e=>!e.receipt_url&&!e.receipt_filename).length}</div></div><div class="kpi"><div class="label">Suppliers</div><div class="value ember">${new Set(state.expenses.map(e=>e.supplier).filter(Boolean)).size}</div></div></div><section class="panel"><div class="panel-head"><div><div class="panel-title">Expense register</div><div class="panel-sub">Every manual expense can be corrected after saving.</div></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Supplier</th><th>Category</th><th>Payment</th><th>GST</th><th>Tax</th><th>Amount</th><th></th></tr></thead><tbody>${state.expenses.map(e=>`<tr><td>${dateAU(e.date)}</td><td>${esc(e.supplier||'—')}</td><td><span class="tag">${esc(e.category)}</span></td><td>${esc(e.payment_method)}</td><td>${e.gst_claimable?money(splitGST(e.amount,true).gst):'—'}</td><td><span class="tag ${e.tax_deductible===false?'tag-orange':'tag-green'}">${e.tax_deductible===false?'Non-deductible':'Deductible'}</span></td><td class="money">${money(e.amount)}</td><td><div class="row-actions"><button class="btn btn-secondary btn-sm" data-edit-exp="${e.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-exp="${e.id}">Delete</button></div></td></tr>`).join('')}</tbody></table></div>${state.expenses.length?'':'<div class="empty">No expenses yet.</div>'}</section>`;el.querySelectorAll('[data-edit-exp]').forEach(b=>b.onclick=()=>openExpenseModal(state.expenses.find(x=>x.id===b.dataset.editExp)));el.querySelectorAll('[data-del-exp]').forEach(b=>b.onclick=()=>deleteRow('expenses',b.dataset.delExp,'expense'))}
function openExpenseModal(record=null){const editing=!!record;const cats=['Meat','Bread','Produce','Packaging','Sauces/Ingredients','Fuel','Gas','Equipment','Repairs & Maintenance','Event Fees','Council/Permits','Insurance','Marketing','Accounting/Bookkeeping','Bank Fees','Wages','Superannuation','Other'],methods=['card','cash','bank transfer','other'];const m=modal(`<div class="eyebrow">Money out</div><h3>${editing?'EDIT':'ADD'} EXPENSE</h3><form id="expForm"><div class="form-grid"><div class="field"><label>Date</label><input class="input" type="date" id="expDate" value="${esc(record?.date||today())}"></div><div class="field span-2"><label>Supplier</label><input class="input" id="expSupplier" value="${esc(record?.supplier||'')}"></div><div class="field"><label>Amount incl GST</label><input class="input" type="number" step=".01" id="expAmount" value="${editing?safeNum(record.amount):''}" required></div><div class="field"><label>Category</label><select class="select" id="expCat">${cats.map(x=>`<option ${optionSelected(record?.category||'Meat',x)}>${x}</option>`).join('')}</select></div><div class="field"><label>Payment</label><select class="select" id="expMethod">${methods.map(x=>`<option ${optionSelected(record?.payment_method||'card',x)}>${x}</option>`).join('')}</select></div><div class="field"><label>GST claimable</label><select class="select" id="expGst"><option value="true" ${record?.gst_claimable!==false?'selected':''}>Yes</option><option value="false" ${record?.gst_claimable===false?'selected':''}>No</option></select></div><div class="field"><label>Tax deductible</label><select class="select" id="expTaxDeductible"><option value="true" ${record?.tax_deductible!==false?'selected':''}>Yes</option><option value="false" ${record?.tax_deductible===false?'selected':''}>No</option></select></div><div class="field"><label>${editing?'Replace receipt (optional)':'Receipt'}</label><input class="input" type="file" id="expReceipt" accept="image/*,application/pdf">${editing&&record?.receipt_filename?`<small class="muted">Current: ${esc(record.receipt_filename)}</small>`:''}</div><div class="field span-4"><label>Notes</label><input class="input" id="expNotes" value="${esc(record?.notes||'')}"></div></div><div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Save expense'}</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;expForm.onsubmit=async e=>{e.preventDefault();let receipt_url=record?.receipt_url||null,receipt_filename=record?.receipt_filename||null;const file=expReceipt.files?.[0];if(file){const path=`${state.user.id}/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;const up=await supabase.storage.from('receipts').upload(path,file);if(!up.error){receipt_url=path;receipt_filename=file.name}else toast(`Receipt upload skipped: ${up.error.message}`,true)}const row={date:expDate.value,supplier:expSupplier.value.trim(),category:expCat.value,payment_method:expMethod.value,amount:safeNum(expAmount.value),gst_claimable:expGst.value==='true',tax_deductible:expTaxDeductible.value==='true',receipt_url,receipt_filename,notes:expNotes.value.trim()};const q=editing?supabase.from('expenses').update(row).eq('id',record.id):supabase.from('expenses').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Expense updated':'Expense added');closeModal();await loadAll()}}
function renderInvoices(el){if(!tableFlags.invoices)return schemaNotice(el,'Invoices');const total=state.invoices.reduce((s,i)=>s+safeNum(i.total),0);el.innerHTML=`<div class="kpi-grid"><div class="kpi"><div class="label">Invoices</div><div class="value ember">${state.invoices.length}</div></div><div class="kpi"><div class="label">Total invoiced</div><div class="value">${money(total)}</div></div><div class="kpi"><div class="label">GST</div><div class="value good">${money(state.invoices.reduce((s,i)=>s+safeNum(i.gst),0))}</div></div><div class="kpi"><div class="label">AI purchases posted</div><div class="value">${state.invoices.filter(i=>invoiceStatus(i)==='posted').length}</div></div></div><section class="panel"><div class="panel-head"><div><div class="panel-title">Invoice archive</div><div class="panel-sub">Purchase Inbox is the working area. This is the permanent invoice register.</div></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Supplier</th><th>File / description</th><th>Status</th><th>Total</th><th>GST</th><th></th></tr></thead><tbody>${state.invoices.map(i=>`<tr><td>${dateAU(i.invoice_date)}</td><td>${esc(i.supplier)}</td><td>${esc(i.file_name||i.description||'')}</td><td>${invoiceStatusTag(invoiceStatus(i))}</td><td class="money">${money(i.total)}</td><td>${money(i.gst)}</td><td><div class="row-actions">${['review','posted'].includes(invoiceStatus(i))?`<button class="btn btn-secondary btn-sm" data-open-inv="${i.id}">${invoiceStatus(i)==='review'?'Review':'View'}</button>`:`<button class="btn btn-secondary btn-sm" data-edit-inv="${i.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-inv="${i.id}">Delete</button>`}</div></td></tr>`).join('')}</tbody></table></div>${state.invoices.length?'':'<div class="empty">No invoices yet.</div>'}</section>`;el.querySelectorAll('[data-open-inv]').forEach(b=>b.onclick=()=>openInvoiceReview(b.dataset.openInv,invoiceStatus(state.invoices.find(x=>x.id===b.dataset.openInv))==='posted'));el.querySelectorAll('[data-edit-inv]').forEach(b=>b.onclick=()=>openInvoiceModal(state.invoices.find(x=>x.id===b.dataset.editInv)));el.querySelectorAll('[data-del-inv]').forEach(b=>b.onclick=()=>deleteRow('invoices',b.dataset.delInv,'invoice'))}
function openInvoiceModal(record=null){if(!tableFlags.invoices)return toast('Apply the included Supabase migration first.',true);const editing=!!record;const m=modal(`<div class="eyebrow">Supplier register</div><h3>${editing?'EDIT':'ADD'} INVOICE</h3><form id="invForm"><div class="form-grid"><div class="field"><label>Date</label><input class="input" type="date" id="invDate" value="${esc(record?.invoice_date||today())}"></div><div class="field span-2"><label>Supplier</label><input class="input" id="invSupplier" value="${esc(record?.supplier||'')}" required></div><div class="field"><label>Total</label><input class="input" type="number" step=".01" id="invTotal" value="${editing?safeNum(record.total):''}" required></div><div class="field"><label>GST</label><input class="input" type="number" step=".01" id="invGst" value="${editing?safeNum(record.gst):0}"></div><div class="field"><label>Category</label><input class="input" id="invCat" value="${esc(record?.category||'')}"></div><div class="field span-2"><label>Description</label><input class="input" id="invDesc" value="${esc(record?.description||'')}"></div></div><div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Save invoice'}</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;invForm.onsubmit=async e=>{e.preventDefault();const row={invoice_date:invDate.value,supplier:invSupplier.value.trim(),total:safeNum(invTotal.value),gst:safeNum(invGst.value),category:invCat.value.trim(),description:invDesc.value.trim()};const q=editing?supabase.from('invoices').update(row).eq('id',record.id):supabase.from('invoices').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Invoice updated':'Invoice added');closeModal();await loadAll()}}
function renderCosts(el){if(!tableFlags.operating_costs)return schemaNotice(el,'Operating costs');const included=state.opCosts.filter(opCostIncluded),weekly=included.reduce((s,c)=>s+weeklyCost(c),0),monthly=weekly*52/12;el.innerHTML=`<div class="kpi-grid"><div class="kpi"><div class="label">Weekly recurring run rate</div><div class="value">${money(weekly)}</div></div><div class="kpi"><div class="label">Monthly recurring run rate</div><div class="value">${money(monthly)}</div></div><div class="kpi"><div class="label">Included in profit</div><div class="value ember">${included.length}</div><div class="meta">Switch off a line if the same cost is already logged in Expenses</div></div><div class="kpi"><div class="label">Daily break-even overhead</div><div class="value warn">${money(weekly/4)}</div><div class="meta">Across 4 trading days · recurring overhead only</div></div></div><section class="panel"><div class="notice" style="margin-bottom:12px">Weekly, monthly and annual operating costs are now accrued into Home net operating profit. Per-event costs are not auto-accrued: record those as an Expense on the event date. Set start/end dates for accurate historical monthly reports.</div><div class="table-wrap"><table class="table"><thead><tr><th>Cost</th><th>Frequency</th><th>Start</th><th>Status</th><th>Amount</th><th>Weekly equiv.</th><th></th></tr></thead><tbody>${state.opCosts.map(c=>`<tr><td><strong>${esc(c.name)}</strong><small style="display:block;color:var(--muted)">${esc(c.category||'')}</small></td><td>${esc(c.frequency)}</td><td>${c.start_date?dateAU(c.start_date):`<span class="muted">${dateAU((c.created_at||'').slice(0,10))} auto</span>`}</td><td><span class="tag ${c.active===false?'':'tag-green'}">${c.active===false?'Inactive':'Active'}</span> ${c.include_in_profit===false?'<span class="tag tag-orange">Excluded from profit</span>':''}</td><td>${money(c.amount)}</td><td class="money">${c.frequency==='per_event'?'—':money(weeklyCost(c))}</td><td><div class="row-actions"><button class="btn btn-secondary btn-sm" data-edit-cost="${c.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-cost="${c.id}">Delete</button></div></td></tr>`).join('')}</tbody></table></div></section>`;el.querySelectorAll('[data-edit-cost]').forEach(b=>b.onclick=()=>openCostModal(state.opCosts.find(x=>x.id===b.dataset.editCost)));el.querySelectorAll('[data-del-cost]').forEach(b=>b.onclick=()=>deleteRow('operating_costs',b.dataset.delCost,'operating cost'))}
function openCostModal(record=null){if(!tableFlags.operating_costs)return toast('Apply the included Supabase migration first.',true);const editing=!!record,freqs=['weekly','monthly','annual','per_event'];const m=modal(`<div class="eyebrow">Recurring run rate</div><h3>${editing?'EDIT':'ADD'} OPERATING COST</h3><form id="costForm"><div class="form-grid"><div class="field span-2"><label>Name</label><input class="input" id="costName" value="${esc(record?.name||'')}" required></div><div class="field"><label>Amount</label><input class="input" type="number" step=".01" id="costAmount" value="${editing?safeNum(record.amount):''}" required></div><div class="field"><label>Frequency</label><select class="select" id="costFreq">${freqs.map(x=>`<option ${optionSelected(record?.frequency||'weekly',x)}>${x}</option>`).join('')}</select></div><div class="field"><label>Start date</label><input class="input" type="date" id="costStart" value="${esc(record?.start_date||'')}"><small class="muted">Blank starts from the date this cost was created in ERP. Set the real start date for historical accuracy.</small></div><div class="field"><label>End date</label><input class="input" type="date" id="costEnd" value="${esc(record?.end_date||'')}"></div><div class="field"><label>Status</label><select class="select" id="costActive"><option value="true" ${record?.active!==false?'selected':''}>Active</option><option value="false" ${record?.active===false?'selected':''}>Inactive</option></select></div><div class="field"><label>Include in Home profit</label><select class="select" id="costProfit"><option value="true" ${record?.include_in_profit!==false?'selected':''}>Yes</option><option value="false" ${record?.include_in_profit===false?'selected':''}>No · already recorded in Expenses</option></select></div><div class="field span-2"><label>Category</label><input class="input" id="costCat" value="${esc(record?.category||'')}"></div></div><div class="notice">Avoid double counting: if this recurring cost is also entered every month as an actual Expense, set “Include in Home profit” to No.</div><div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Save cost'}</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;costForm.onsubmit=async e=>{e.preventDefault();const row={name:costName.value.trim(),amount:safeNum(costAmount.value),frequency:costFreq.value,category:costCat.value.trim(),start_date:costStart.value||null,end_date:costEnd.value||null,active:costActive.value==='true',include_in_profit:costProfit.value==='true'};const q=editing?supabase.from('operating_costs').update(row).eq('id',record.id):supabase.from('operating_costs').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Operating cost updated':'Operating cost added');closeModal();await loadAll()}}

function renderCapital(el){if(!tableFlags.capital_items)return schemaNotice(el,'Capital');const contributions=state.capital.filter(c=>c.movement_type==='contribution').reduce((s,c)=>s+Math.abs(safeNum(c.amount)),0),withdrawals=state.capital.filter(c=>c.movement_type==='withdrawal').reduce((s,c)=>s+Math.abs(safeNum(c.amount)),0),investments=state.capital.filter(c=>!c.movement_type||c.movement_type==='asset_investment').reduce((s,c)=>s+Math.abs(safeNum(c.amount)),0),netFunding=contributions-withdrawals;el.innerHTML=`<section class="hero-grid"><div class="hero-panel"><div class="eyebrow">Owner funding</div><h2>FUND THE FIRE.</h2><div class="hero-total">${money(netFunding)} <small>NET CAPITAL CONTRIBUTED</small></div></div><div class="hero-aside"><div class="signal-card"><div class="label">Capital in</div><div class="value good">${money(contributions)}</div><div class="hint">Owner contributions. Not revenue and never included in profit.</div></div><div class="signal-card"><div class="label">Capital out</div><div class="value ${withdrawals?'warn':''}">${money(withdrawals)}</div><div class="hint">Owner withdrawals / drawings.</div></div></div></section><div class="kpi-grid" style="margin-top:15px"><div class="kpi"><div class="label">Legacy / asset investment</div><div class="value ember">${money(investments)}</div><div class="meta">Equipment, truck, fitout and other capital items</div></div><div class="kpi"><div class="label">Net owner funding</div><div class="value">${money(netFunding)}</div><div class="meta">Contributions minus withdrawals</div></div><div class="kpi"><div class="label">Capital in stock</div><div class="value">${money(stockCapital().total)}</div></div><div class="kpi"><div class="label">Accounting treatment</div><div class="value" style="font-size:22px">NOT SALES</div><div class="meta">Capital movements affect cash, not operating profit</div></div></div><section class="panel" style="margin-top:15px"><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Type</th><th>Description</th><th>Category</th><th>Notes</th><th>Amount</th><th></th></tr></thead><tbody>${state.capital.slice().sort((a,b)=>String(b.investment_date).localeCompare(String(a.investment_date))).map(c=>{const type=c.movement_type||'asset_investment';return`<tr><td>${dateAU(c.investment_date)}</td><td><span class="tag ${type==='contribution'?'tag-green':type==='withdrawal'?'tag-orange':''}">${type==='contribution'?'Capital in':type==='withdrawal'?'Capital out':'Asset / investment'}</span></td><td>${esc(c.name)}</td><td>${esc(c.category)}</td><td>${esc(c.notes||'')}</td><td class="money">${type==='withdrawal'?'-':''}${money(Math.abs(safeNum(c.amount)))}</td><td><div class="row-actions"><button class="btn btn-secondary btn-sm" data-edit-cap="${c.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-cap="${c.id}">Delete</button></div></td></tr>`}).join('')}</tbody></table></div>${state.capital.length?'':'<div class="empty">No capital movements recorded yet.</div>'}</section>`;el.querySelectorAll('[data-edit-cap]').forEach(b=>b.onclick=()=>openCapitalModal(state.capital.find(x=>x.id===b.dataset.editCap)));el.querySelectorAll('[data-del-cap]').forEach(b=>b.onclick=()=>deleteRow('capital_items',b.dataset.delCap,'capital item'))}
function openCapitalModal(record=null){if(!tableFlags.capital_items)return toast('Apply the included Supabase migration first.',true);const editing=!!record,cats=['Owner funding','Truck','Equipment','Fitout','Licences','Branding','Stock','Deposits','Technology','Other'],type=record?.movement_type||'asset_investment';const m=modal(`<div class="eyebrow">Capital & funding</div><h3>${editing?'EDIT':'ADD'} CAPITAL MOVEMENT</h3><form id="capForm"><div class="form-grid"><div class="field span-2"><label>Movement type</label><select class="select" id="capType"><option value="contribution" ${optionSelected(type,'contribution')}>Capital contribution · money into business</option><option value="withdrawal" ${optionSelected(type,'withdrawal')}>Capital withdrawal / drawings · money out</option><option value="asset_investment" ${optionSelected(type,'asset_investment')}>Asset / founder investment · track invested capital</option></select></div><div class="field span-2"><label>Description</label><input class="input" id="capName" value="${esc(record?.name||'')}" placeholder="Owner cash injection, fryer, truck fitout..." required></div><div class="field"><label>Amount</label><input class="input" type="number" min="0" step=".01" id="capAmount" value="${editing?Math.abs(safeNum(record.amount)):''}" required></div><div class="field"><label>Date</label><input class="input" type="date" id="capDate" value="${esc(record?.investment_date||today())}"></div><div class="field"><label>Category</label><select class="select" id="capCat">${cats.map(x=>`<option ${optionSelected(record?.category||(type==='contribution'?'Owner funding':'Other'),x)}>${x}</option>`).join('')}</select></div><div class="field span-2"><label>Notes</label><input class="input" id="capNotes" value="${esc(record?.notes||'')}"></div></div><div class="notice">Capital contributions are cash funding, not sales. They appear in cash movement reports but never increase operating profit.</div><div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Save movement'}</button></div></form>`);m.querySelector('[data-close]').onclick=closeModal;capForm.onsubmit=async e=>{e.preventDefault();const row={name:capName.value.trim(),amount:Math.abs(safeNum(capAmount.value)),investment_date:capDate.value,category:capCat.value,movement_type:capType.value,notes:capNotes.value.trim()};const q=editing?supabase.from('capital_items').update(row).eq('id',record.id):supabase.from('capital_items').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Capital movement updated':'Capital movement added');closeModal();await loadAll()}}

function renderReports(el){const rows=monthlyPerformance(12),latest=rows.at(-1)||{sales:0,expenses:0,recurring:0,costs:0,net:0,capital:0,cash:0};el.innerHTML=`<section class="hero-grid"><div class="hero-panel"><div class="eyebrow">Recorded month-by-month performance</div><h2>KNOW THE TREND.</h2><div class="hero-total">${money(latest.net)} <small>${esc(latest.label||'CURRENT MONTH')} NET OPERATING PROFIT</small></div></div><div class="hero-aside"><div class="signal-card"><div class="label">Current month sales</div><div class="value">${money(latest.sales)}</div></div><div class="signal-card"><div class="label">Current month total costs</div><div class="value">${money(latest.costs)}</div><div class="hint">Logged expenses + accrued recurring overhead.</div></div></div></section>
<div class="kpi-grid" style="margin-top:15px"><div class="kpi"><div class="label">Logged expenses</div><div class="value">${money(latest.expenses)}</div></div><div class="kpi"><div class="label">Recurring overhead</div><div class="value warn">${money(latest.recurring)}</div></div><div class="kpi"><div class="label">Capital movement</div><div class="value ${latest.capital>=0?'good':'warn'}">${money(latest.capital)}</div><div class="meta">Shown separately from profit</div></div><div class="kpi"><div class="label">Net cash movement</div><div class="value ${latest.cash>=0?'good':'bad'}">${money(latest.cash)}</div><div class="meta">Operating profit + owner funding / withdrawals</div></div></div>
<section class="panel"><div class="panel-head"><div><div class="panel-title">Sales vs operating costs · 12 months</div><div class="panel-sub">Costs = expenses actually logged + recurring overhead accrued from Operating costs.</div></div></div>${groupedMonthlySvg(rows)}</section>
<section class="panel" style="margin-top:15px"><div class="panel-head"><div><div class="panel-title">Net operating profit by month</div><div class="panel-sub">Capital contributions and withdrawals are deliberately excluded from profit.</div></div></div>${netMonthlySvg(rows)}</section>
<section class="panel" style="margin-top:15px"><div class="panel-head"><div><div class="panel-title">Monthly P&L comparison</div><div class="panel-sub">Recorded data only. Current month is month-to-date.</div></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Month</th><th>Sales</th><th>Logged expenses</th><th>Recurring overhead</th><th>Total costs</th><th>Net operating profit</th><th>Capital +/-</th><th>Net cash movement</th></tr></thead><tbody>${rows.slice().reverse().map(r=>`<tr><td><strong>${esc(r.label)}</strong></td><td class="money">${money(r.sales)}</td><td>${money(r.expenses)}</td><td>${money(r.recurring)}</td><td>${money(r.costs)}</td><td class="money ${r.net>=0?'good':'bad'}">${money(r.net)}</td><td class="${r.capital>=0?'good':'warn'}">${money(r.capital)}</td><td class="money ${r.cash>=0?'good':'bad'}">${money(r.cash)}</td></tr>`).join('')}</tbody></table></div><div class="notice" style="margin-top:14px">For accurate history, give each recurring Operating cost a start date and turn “Include in Home profit” off when the same charge is already entered in Expenses. Manual sales are added on top of Shopify only when marked “Additional / non-Shopify sale”. Shopify history shown here is limited to the orders currently stored in A Fuego ERP.</div></section>`}

function financialYearInfo(d=new Date()){
  const startYear=d.getMonth()>=6?d.getFullYear():d.getFullYear()-1;
  const start=new Date(startYear,6,1),end=new Date(startYear+1,5,30,23,59,59,999);
  const elapsed=Math.max(1,Math.floor((d-start)/86400000)+1),total=Math.max(1,Math.floor((end-start)/86400000)+1),remaining=Math.max(0,total-elapsed);
  return{startYear,start:`${startYear}-07-01`,end:`${startYear+1}-06-30`,elapsed,total,remaining,label:`FY ${startYear}–${String(startYear+1).slice(-2)}`};
}
function residentIncomeTax(income,fyStartYear){
  const x=Math.max(0,safeNum(income)),lowRate=fyStartYear>=2027?.14:fyStartYear>=2026?.15:.16;
  if(x<=18200)return 0;
  if(x<=45000)return(x-18200)*lowRate;
  const at45=26800*lowRate;
  if(x<=135000)return at45+(x-45000)*.30;
  const at135=at45+90000*.30;
  if(x<=190000)return at135+(x-135000)*.37;
  return at135+55000*.37+(x-190000)*.45;
}
function soleTraderTax(income,fyStartYear,includeMedicare=true){const x=Math.max(0,safeNum(income));return residentIncomeTax(x,fyStartYear)+(includeMedicare?x*.02:0)}
function taxableSalesAmount(gross){return state.settings?.gst_registered?safeNum(gross)/1.1:safeNum(gross)}
function deductibleExpenseAmount(e){if(e?.tax_deductible===false)return 0;const amount=safeNum(e?.amount);return state.settings?.gst_registered&&e?.gst_claimable?splitGST(amount,true).net:amount}
function taxSnapshot(){
  const fy=financialYearInfo(),inRange=v=>v>=fy.start&&v<=fy.end;
  const grossSales=state.shopifyOrders.length?state.shopifyOrders.filter(o=>inRange((o.processed_at||'').slice(0,10))).reduce((s,o)=>s+safeNum(o.total_price),0):state.sales.filter(x=>inRange(x.date)).reduce((s,x)=>s+safeNum(x.cash_sales)+safeNum(x.card_sales)+safeNum(x.other_sales),0);
  const taxableSales=taxableSalesAmount(grossSales),deductibleExpenses=state.expenses.filter(e=>inRange(e.date)).reduce((s,e)=>s+deductibleExpenseAmount(e),0),profit=taxableSales-deductibleExpenses;
  const cfg=state.taxSettings||{},structure=cfg.structure||'unconfirmed',other=safeNum(cfg.other_taxable_income_estimate),payg=safeNum(cfg.payg_instalments_paid),includeMedicare=cfg.include_medicare_levy!==false,companyRate=safeNum(cfg.company_tax_rate)||25;
  let taxYtd=0,projectedTax=0;
  const projectedProfit=Math.max(0,profit)*(fy.total/fy.elapsed);
  if(structure==='sole_trader'){
    taxYtd=Math.max(0,soleTraderTax(other+Math.max(0,profit),fy.startYear,includeMedicare)-soleTraderTax(other,fy.startYear,includeMedicare));
    projectedTax=Math.max(0,soleTraderTax(other+projectedProfit,fy.startYear,includeMedicare)-soleTraderTax(other,fy.startYear,includeMedicare));
  }else if(structure==='company'){
    taxYtd=Math.max(0,profit)*companyRate/100;projectedTax=projectedProfit*companyRate/100;
  }
  const reserved=state.taxReserveEntries.reduce((s,x)=>s+safeNum(x.amount),0),targetNow=Math.max(0,taxYtd-payg),gapNow=Math.max(0,targetNow-reserved),projectedTarget=Math.max(0,projectedTax-payg),projectedGap=Math.max(0,projectedTarget-reserved),weeksRemaining=Math.max(1,fy.remaining/7),weeklyReserve=projectedGap/weeksRemaining;
  return{fy,grossSales,taxableSales,deductibleExpenses,profit,projectedProfit,structure,other,payg,includeMedicare,companyRate,taxYtd,projectedTax,reserved,targetNow,gapNow,projectedTarget,projectedGap,weeklyReserve};
}
function renderTax(el){
  if(!tableFlags.tax_settings||!tableFlags.tax_reserve_entries)return schemaNotice(el,'Tax reserve v2.6');
  const t=taxSnapshot(),configured=t.structure!=='unconfirmed',progress=t.targetNow>0?Math.min(100,Math.max(0,t.reserved/t.targetNow*100)):0;
  el.innerHTML=`<section class="hero-grid"><div class="hero-panel"><div class="eyebrow">${esc(t.fy.label)} · Tax reserve</div><h2>KEEP THE ATO MONEY OUT OF THE FIRE.</h2><div class="hero-total">${configured?money(t.gapNow):'SET UP'} <small>${configured?'GAP TO TODAY\'S TARGET':'TAX STRUCTURE REQUIRED'}</small></div><span class="hero-delta">${configured?`${money(t.reserved)} already reserved`:'Choose sole trader or company below before relying on the estimate.'}</span></div><div class="hero-aside"><div class="signal-card"><div class="label">Est. tax on A Fuego profit YTD</div><div class="value ${configured?'ember':'muted'}">${configured?money(t.taxYtd):'—'}</div><div class="hint">Income tax estimate attributable to recorded A Fuego profit.</div></div><div class="signal-card"><div class="label">Weekly reserve at current pace</div><div class="value ${configured?'warn':'muted'}">${configured?money(t.weeklyReserve):'—'}</div><div class="hint">To reach the projected FY target by 30 June.</div></div></div></section>
  <div class="kpi-grid"><div class="kpi"><div class="label">Taxable sales YTD</div><div class="value">${money(t.taxableSales)}</div><div class="meta">${state.settings?.gst_registered?'GST-exclusive estimate':'Recorded gross sales'}</div></div><div class="kpi"><div class="label">Deductible expenses YTD</div><div class="value">${money(t.deductibleExpenses)}</div><div class="meta">Only expenses marked tax deductible</div></div><div class="kpi"><div class="label">Est. taxable profit YTD</div><div class="value ${t.profit>=0?'good':'bad'}">${money(t.profit)}</div></div><div class="kpi"><div class="label">Projected FY tax target</div><div class="value ember">${configured?money(t.projectedTarget):'—'}</div><div class="meta">Current pace projection</div></div></div>
  <div class="grid-2"><section class="panel"><div class="panel-head"><div><div class="panel-title">Tax reserve position</div><div class="panel-sub">Money actually set aside versus the estimate earned to date</div></div><button class="btn btn-primary btn-sm" id="addTaxReserve">+ Reserve movement</button></div><div class="tax-meter"><div class="tax-meter-head"><strong>${money(t.reserved)} reserved</strong><span>${configured?`${progress.toFixed(0)}% of today's target`:'Configure tax structure'}</span></div><div class="progress tax-progress"><span style="width:${progress}%"></span></div></div><div class="metrics-mini" style="margin-top:14px"><div class="metric-mini"><label>Target today</label><b>${configured?money(t.targetNow):'—'}</b></div><div class="metric-mini"><label>PAYG paid</label><b>${money(t.payg)}</b></div><div class="metric-mini"><label>Gap today</label><b class="${t.gapNow?'warn':'good'}">${configured?money(t.gapNow):'—'}</b></div></div><div class="table-wrap" style="margin-top:15px"><table class="table"><thead><tr><th>Date</th><th>Movement</th><th>Note</th><th>Amount</th><th></th></tr></thead><tbody>${state.taxReserveEntries.slice().sort((a,b)=>String(b.entry_date).localeCompare(String(a.entry_date))).map(x=>`<tr><td>${dateAU(x.entry_date)}</td><td><span class="tag ${safeNum(x.amount)>=0?'tag-green':'tag-orange'}">${safeNum(x.amount)>=0?'Reserved':'Withdrawn'}</span></td><td>${esc(x.note||'')}</td><td class="money">${money(x.amount)}</td><td><div class="row-actions"><button class="btn btn-secondary btn-sm" data-edit-tax-reserve="${x.id}">Edit</button><button class="btn btn-danger btn-sm" data-del-tax-reserve="${x.id}">Delete</button></div></td></tr>`).join('')}</tbody></table></div>${state.taxReserveEntries.length?'':'<div class="empty">No tax reserve movements recorded yet.</div>'}</section>
  <section class="panel"><div class="panel-title">Tax estimate settings</div><div class="panel-sub">A Fuego's legal structure has not been confirmed in the app, so choose it here.</div><form id="taxSettingsForm" style="margin-top:14px"><div class="form-grid"><div class="field span-2"><label>Tax structure</label><select class="select" id="taxStructure"><option value="unconfirmed" ${optionSelected(t.structure,'unconfirmed')}>Not confirmed</option><option value="sole_trader" ${optionSelected(t.structure,'sole_trader')}>Sole trader / individual ABN</option><option value="company" ${optionSelected(t.structure,'company')}>Company</option></select></div><div class="field span-2"><label>Expected other taxable income this FY</label><input class="input" type="number" step=".01" id="taxOtherIncome" value="${t.other}"><small class="muted">Salary or other taxable income. Used only to estimate the marginal tax attributable to A Fuego for a sole trader.</small></div><div class="field"><label>PAYG instalments paid YTD</label><input class="input" type="number" step=".01" id="taxPayg" value="${t.payg}"></div><div class="field"><label>Include approx. Medicare levy</label><select class="select" id="taxMedicare"><option value="true" ${t.includeMedicare?'selected':''}>Yes · 2% estimate</option><option value="false" ${!t.includeMedicare?'selected':''}>No</option></select></div><div class="field span-2"><label>Company tax rate</label><select class="select" id="taxCompanyRate"><option value="25" ${optionSelected(t.companyRate,25)}>25% · base rate estimate</option><option value="30" ${optionSelected(t.companyRate,30)}>30% · standard estimate</option></select></div></div><button class="btn btn-primary">Save tax settings</button></form><div class="notice" style="margin-top:15px">Operational estimate only. It does not model tax offsets, HELP/HECS, Medicare levy thresholds or surcharge, depreciation, stock-on-hand tax adjustments, carried-forward losses, Division 7A, PSI or every deduction rule. Confirm the year-end position with your registered tax agent.</div></section></div>
  <section class="panel" style="margin-top:15px"><div class="panel-head"><div><div class="panel-title">Current pace projection</div><div class="panel-sub">${t.fy.elapsed} days into ${esc(t.fy.label)} · projection is deliberately separated from tax already earned to date</div></div></div><div class="kpi-grid compact-kpis"><div class="kpi"><div class="label">Projected A Fuego profit</div><div class="value">${money(t.projectedProfit)}</div></div><div class="kpi"><div class="label">Projected tax</div><div class="value ember">${configured?money(t.projectedTax):'—'}</div></div><div class="kpi"><div class="label">Projected reserve gap</div><div class="value ${t.projectedGap?'warn':'good'}">${configured?money(t.projectedGap):'—'}</div></div><div class="kpi"><div class="label">FY end</div><div class="value">30 JUN</div><div class="meta">${t.fy.remaining} days remaining</div></div></div></section>`;
  addTaxReserve.onclick=()=>openTaxReserveModal();
  el.querySelectorAll('[data-edit-tax-reserve]').forEach(b=>b.onclick=()=>openTaxReserveModal(state.taxReserveEntries.find(x=>x.id===b.dataset.editTaxReserve)));
  el.querySelectorAll('[data-del-tax-reserve]').forEach(b=>b.onclick=()=>deleteRow('tax_reserve_entries',b.dataset.delTaxReserve,'tax reserve movement'));
  taxSettingsForm.onsubmit=async e=>{e.preventDefault();const row={user_id:state.user.id,structure:taxStructure.value,other_taxable_income_estimate:safeNum(taxOtherIncome.value),payg_instalments_paid:safeNum(taxPayg.value),include_medicare_levy:taxMedicare.value==='true',company_tax_rate:safeNum(taxCompanyRate.value)};const {error}=await supabase.from('tax_settings').upsert(row,{onConflict:'user_id'});if(error)return toast(error.message,true);toast('Tax settings saved');await loadAll()};
}
function openTaxReserveModal(record=null){
  const editing=!!record,current=safeNum(record?.amount),kind=current<0?'withdraw':'reserve';
  const m=modal(`<div class="eyebrow">Tax cash buffer</div><h3>${editing?'EDIT':'ADD'} RESERVE MOVEMENT</h3><form id="taxReserveForm"><div class="form-grid"><div class="field"><label>Date</label><input class="input" type="date" id="taxReserveDate" value="${esc(record?.entry_date||today())}"></div><div class="field"><label>Movement</label><select class="select" id="taxReserveKind"><option value="reserve" ${optionSelected(kind,'reserve')}>Reserve money</option><option value="withdraw" ${optionSelected(kind,'withdraw')}>Withdraw from reserve</option></select></div><div class="field"><label>Amount</label><input class="input" type="number" min="0" step=".01" id="taxReserveAmount" value="${editing?Math.abs(current):''}" required></div><div class="field span-2"><label>Note</label><input class="input" id="taxReserveNote" value="${esc(record?.note||'')}" placeholder="Transfer to tax savings account"></div></div><div class="inline-actions"><button class="btn btn-ghost" data-close type="button">Cancel</button><button class="btn btn-primary">${editing?'Save changes':'Save movement'}</button></div></form>`);
  m.querySelector('[data-close]').onclick=closeModal;taxReserveForm.onsubmit=async e=>{e.preventDefault();const sign=taxReserveKind.value==='withdraw'?-1:1,row={entry_date:taxReserveDate.value,amount:Math.abs(safeNum(taxReserveAmount.value))*sign,note:taxReserveNote.value.trim()};const q=editing?supabase.from('tax_reserve_entries').update(row).eq('id',record.id):supabase.from('tax_reserve_entries').insert({...row,user_id:state.user.id});const {error}=await q;if(error)return toast(error.message,true);toast(editing?'Reserve movement updated':'Reserve movement added');closeModal();await loadAll()};
}

function renderAssistant(el){el.innerHTML=`<div class="chat-shell"><div class="chat-head"><div><div class="panel-title">A FUEGO AI</div><div class="panel-sub">Grounded in your recorded business data</div></div><span class="tag tag-green">Data aware</span></div><div class="chat-messages" id="chatMsgs"><div class="msg assistant">Ask me about sales, food cost, expenses, missing receipts, GST estimates or the most expensive recipes. I will use the data currently recorded in A Fuego OS.</div></div><form class="chat-compose" id="chatForm"><input class="input" id="chatInput" placeholder="Why is my margin getting worse?" autocomplete="off"><button class="btn btn-primary">Ask</button></form></div>`;chatForm.onsubmit=async e=>{e.preventDefault();const q=chatInput.value.trim();if(!q)return;chatInput.value='';appendMsg(q,'user');appendMsg('Reading A Fuego data...','assistant','thinking');const answer=await answerQuestion(q);document.querySelector('[data-thinking]')?.remove();appendMsg(answer,'assistant')}}
function appendMsg(text,role,thinking=''){const d=document.createElement('div');d.className=`msg ${role}`;if(thinking)d.dataset.thinking='1';d.textContent=text;chatMsgs.appendChild(d);chatMsgs.scrollTop=chatMsgs.scrollHeight}

async function answerQuestion(q){
  try{const {data,error}=await supabase.functions.invoke('accounting-chat',{body:{message:q}});if(!error&&data?.answer)return data.answer;if(!error&&data?.content)return data.content}catch{}
  const c=computed(),low=q.toLowerCase();if(/priority|priorit|task|tarea|project|proyecto|overdue|blocked|bloquead/.test(low)){const ps=plannerStats(),next=plannerOpenTasks().sort(taskSort).slice(0,5);return `A Fuego has ${ps.open} open priorities: ${ps.overdue} overdue, ${ps.high} high/urgent and ${ps.blocked} blocked. Next items: ${next.length?next.map(t=>`${t.title}${t.due_date?` (${dateAU(t.due_date)})`:''}`).join('; '):'no open tasks recorded'}.`}if(/stock|inventory|inventario|capital.*stock/.test(low)){const bev=state.inventory.filter(i=>(i.notes||'').split(' · ')[0]==='Beverages').reduce((s,i)=>s+ingredientStockValue(i),0);return `Current recorded stock capital is ${money(c.stock.total)}: ${money(c.stock.ingredients)} in ingredients and beverages, ${money(c.stock.preparations)} in preparations and ${money(c.stock.supplies)} in supplies. Beverage stock value is ${money(bev)}.`}if(/match|alias|shopify.*menu/.test(low)){const rows=uniqueSalesProducts(),missing=rows.filter(x=>!x.match);return missing.length?`${missing.length} sales product names are not matched to a menu recipe: ${missing.slice(0,8).map(x=>x.raw).join(', ')}. Add those names as sales aliases in Edit recipe.`:`All ${rows.length} recorded sales product names currently match a menu recipe.`}if(/income tax|tax reserve|tax this year|annual tax|fiscal year tax/.test(low)){const t=taxSnapshot();if(t.structure==='unconfirmed')return `Tax structure is not confirmed yet. Open Tax and choose Sole trader or Company before relying on the estimate. Recorded FY taxable profit estimate is ${money(t.profit)}.`;return `For ${t.fy.label}, recorded A Fuego taxable profit is approximately ${money(t.profit)}. Estimated tax attributable to A Fuego profit earned to date is ${money(t.taxYtd)}; after recorded PAYG instalments the current reserve target is ${money(t.targetNow)}. You have recorded ${money(t.reserved)} as reserved, leaving a ${money(t.gapNow)} gap. At the current pace, the projected FY tax target is ${money(t.projectedTarget)}. Operational estimate only—confirm with your registered tax agent.`}if(/gst|bas/.test(low)){const salesGst=splitGST(c.monthSales,true).gst,credits=state.expenses.reduce((s,e)=>s+splitGST(e.amount,e.gst_claimable).gst,0);return `Based on recorded data, estimated GST collected is ${money(salesGst)} and estimated GST credits are ${money(credits)}. Net estimate: ${money(salesGst-credits)} payable. Verify GST treatment and BAS with your accountant.`}if(/food cost|margin|recipe/.test(low)){const rows=state.recipes.filter(isActive).map(r=>({name:r.product_name,cost:recipeCost(r),cp:safeNum(r.selling_price)?recipeCost(r)/safeNum(r.selling_price)*100:0})).sort((a,b)=>b.cp-a.cp);return rows.length?`Average theoretical food cost for the active menu is ${pct(c.avgFood)}. Highest cost pressure: ${rows.slice(0,3).map(r=>`${r.name} ${pct(r.cp)} (${money(r.cost)} cost)`).join('; ')}.`:'I need active recipes and ingredient costs before I can analyse food cost.'}if(/receipt/.test(low)){const missing=state.expenses.filter(e=>!e.receipt_url&&!e.receipt_filename);return missing.length?`${missing.length} expenses are missing receipts. Biggest examples: ${missing.sort((a,b)=>safeNum(b.amount)-safeNum(a.amount)).slice(0,5).map(e=>`${e.supplier||e.category} ${money(e.amount)}`).join(', ')}.`:'Every recorded expense currently has a receipt reference.'}if(/sale|revenue|takings/.test(low))return `Recorded month revenue is ${money(c.monthSales)}. Today's revenue is ${money(c.todaySales)}. ${state.shopifyOrders.length?'This includes the Shopify order feed.':'Shopify is not connected yet, so I am using manual sales days.'}`;if(/month|monthly|trend|report/.test(low)){const rows=monthlyPerformance(6);return `Last recorded months: ${rows.map(r=>`${r.label}: sales ${money(r.sales)}, total operating costs ${money(r.costs)}, net ${money(r.net)}`).join('; ')}. Capital funding is shown separately from operating profit.`}if(/expense|cost/.test(low))return `This month has ${money(c.monthExp)} in logged expenses plus ${money(c.monthOp)} in accrued recurring overhead, for ${money(c.monthCosts)} total operating costs. Estimated net operating profit is ${money(c.netProfit)}. Recurring costs are excluded when their “Include in Home profit” switch is off.`;return `I can answer this reliably once it maps to recorded A Fuego data. Right now I have ${state.sales.length} manual sales days, ${state.shopifyOrders.length} Shopify orders, ${state.expenses.length} expenses, ${state.recipes.filter(isActive).length} active recipes and ${money(c.stock.total)} recorded capital in stock. Try: “unmatched Shopify products”, “stock capital”, “highest food cost”, “missing receipts” or “GST this month”.`}

function renderSettings(el){el.innerHTML=`<div class="grid-2"><section class="panel"><div class="panel-title">Business</div><form id="settingsForm"><div class="field"><label>Business name</label><input class="input" id="setName" value="${esc(state.settings?.business_name||'A Fuego')}"></div><div class="field"><label>Region</label><input class="input" id="setRegion" value="${esc(state.settings?.region||'Sydney, NSW, Australia')}"></div><div class="field"><label>GST registered</label><select class="select" id="setGst"><option value="true" ${state.settings?.gst_registered?'selected':''}>Yes</option><option value="false" ${!state.settings?.gst_registered?'selected':''}>No</option></select></div><div class="field"><label>BAS cycle</label><select class="select" id="setBas"><option value="quarterly" ${state.settings?.bas_cycle==='quarterly'?'selected':''}>Quarterly</option><option value="monthly" ${state.settings?.bas_cycle==='monthly'?'selected':''}>Monthly</option></select></div><button class="btn btn-primary">Save settings</button></form></section><section class="panel"><div class="panel-title">Integrations</div><div class="list-row"><div class="grow"><strong>Cloud database</strong><small>Supabase · private RLS-backed data</small></div><span class="tag tag-green">Connected</span></div><div class="list-row"><div class="grow"><strong>Shopify POS</strong><small>Orders + order lines + webhook sync</small></div><span class="tag ${state.shopifyOrders.length?'tag-green':'tag-orange'}">${state.shopifyOrders.length?'Live':'Setup required'}</span></div><button class="btn btn-secondary btn-block" id="shopifySetup">Shopify setup</button><div class="notice" style="margin-top:12px">A Fuego OS never stores Shopify Admin tokens in localStorage or client code. Server secrets only.</div></section></div><section class="panel" style="margin-top:15px"><div class="panel-title">Database readiness</div><div class="grid-3">${Object.entries(tableFlags).map(([t,ok])=>`<div class="list-row"><div class="grow"><strong>${esc(t)}</strong></div><span class="tag ${ok?'tag-green':'tag-orange'}">${ok?'Ready':'Migration'}</span></div>`).join('')}</div></section>`;settingsForm.onsubmit=async e=>{e.preventDefault();const {error}=await supabase.from('business_settings').update({business_name:setName.value.trim(),region:setRegion.value.trim(),gst_registered:setGst.value==='true',bas_cycle:setBas.value}).eq('id',state.settings.id);if(error)return toast(error.message,true);toast('Settings saved');await loadAll()};shopifySetup.onclick=()=>openShopifyInfo()}

async function deleteRow(table,id,label='item'){if(!confirm(`Delete this ${label}? This cannot be undone.`))return;const {error}=await supabase.from(table).delete().eq('id',id);if(error)return toast(error.message,true);toast(`${label.charAt(0).toUpperCase()+label.slice(1)} deleted`);await loadAll()}
function modal(html){closeModal();const b=document.createElement('div');b.className='modal-backdrop';b.id='modalBack';b.innerHTML=`<div class="modal">${html}</div>`;b.onclick=e=>{if(e.target===b)closeModal()};document.body.appendChild(b);return b}
function closeModal(){document.getElementById('modalBack')?.remove()}

window.addEventListener('hashchange',()=>{const r=location.hash.replace('#','');if(pages[r]){state.route=r;if(state.user)renderShell()}});const initial=location.hash.replace('#','');if(pages[initial])state.route=initial;
boot().catch(err=>{console.error(err);markReady();app.innerHTML=`<div class="boot"><div class="brand-flame">⚠️</div><div>COULDN'T LIGHT THE GRILL</div><small>${esc(err?.message||String(err))}</small><button class="btn btn-primary" style="margin-top:16px" onclick="location.reload()">Retry</button></div>`});

})();
